Note: Compile-time Integer array literal. Indexing with `at index N` returns the
Note: Nth element via the RunaList header emitted in .data.
Note: TBL[2] = 30
Note: Expected exit code: 30

Constant TBL as Integer is Array[Integer, 4] is [10, 20, 30, 40]

Process called "main" returns Integer:
    Let v be TBL at index 2
    Return v
End Process
