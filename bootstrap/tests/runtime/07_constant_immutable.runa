Note: Constants must be readable but reject all mutation forms.
Note: This test reads the constant successfully. A separate negative test in the
Note: harness asserts the compiler rejects `Set LIMIT to ...` and `Increase LIMIT by N`.
Note: Expected exit code: 42

Process called "main" returns Integer:
    Constant LIMIT as Integer is 42
    Let read_value be LIMIT
    Return read_value
End Process
