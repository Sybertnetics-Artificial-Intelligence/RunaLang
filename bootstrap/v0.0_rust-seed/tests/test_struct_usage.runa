Type called "Person":
    name as String
    age as Integer
End Type

Let person be a value of type Person with name as "Alice", age as 30
Let name be person.name
Let age be person.age
Display name
Display age