Print "Testing builtin in function context"

Process called "test_builtin" that takes str as String returns Integer:
    Let len be length_of(str)
    Return len
End Process

Let result be test_builtin("Hello")
Print result

Print "Builtin context test complete"