Note: Validate context-sensitive keywords work correctly
Process called "main" returns Integer:
    Note: Test new context-sensitive tokens as variables
    Let count be 10
    Let pointer be 20
    Let error be 30
    Let array be 40

    Display count
    Display pointer
    Display error
    Display array

    Note: Verify Array keyword still works in type context
    Note: Using separate test to avoid parsing complexity

    Return 0
End Process
