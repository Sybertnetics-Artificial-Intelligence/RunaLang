Note: Test control flow: If/Otherwise/End If, While/End While
Process called "main" returns Integer:
    Let x be 10

    Note: Test If/Otherwise/End If
    If x is equal to 10:
        Display("PASS: If condition true")
    Otherwise:
        Display("FAIL: If condition true")
    End If

    If x is equal to 5:
        Display("FAIL: If condition false")
    Otherwise:
        Display("PASS: If condition false")
    End If

    If x is equal to 5:
        Display("FAIL: If condition false with Otherwise")
    Otherwise If x is equal to 10:
        Display("PASS: Else If condition true")
    Otherwise:
        Display("FAIL: Else If condition true")
    End If

    Note: Test While/End While
    Let counter be 0
    Let sum be 0
    While counter is less than 5:
        Set sum to sum plus counter
        Set counter to counter plus 1
    End While

    Display sum

    Return 0
End Process
