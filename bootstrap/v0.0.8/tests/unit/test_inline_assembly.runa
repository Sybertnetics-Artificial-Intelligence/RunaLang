Note: Test Inline Assembly with raw text mode
Note: This test demonstrates:
Note: 1. Raw text mode (no string literals needed)
Note: 2. Hash comments work inside inline assembly
Note: 3. Multiple assembly instructions
Note: 4. Proper formatting and indentation

Process called "main" returns Integer:
    Note: Initialize a variable
    Let x be 5

    Note: Use inline assembly to perform operations
    Inline Assembly:
        # Save the value of x (at -8(%rbp)) to %rbx
        movq -8(%rbp), %rbx

        # Add 37 to it (5 + 37 = 42)
        addq $37, %rbx

        # Store result back to x
        movq %rbx, -8(%rbp)
    End Assembly

    Note: x should now be 42
    Return x
End Process
