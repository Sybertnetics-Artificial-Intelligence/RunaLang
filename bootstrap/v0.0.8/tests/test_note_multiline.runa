Note: This is a multi-line comment block
that continues on the next line
and keeps going
until we hit the terminator
:End Note

Process called "main" takes argc as Integer, argv as Pointer returns Integer:
    Let x be 42
    print_integer(x)
    print_newline()
    Return 0
End Process
