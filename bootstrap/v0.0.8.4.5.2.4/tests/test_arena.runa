Note: Simple test to verify arena allocation works

Process called "main" takes argc as Integer, argv as Integer returns Integer:
    print_string("Testing arena allocation")

    Let x be 42
    Let y be x plus 8
    print_integer(y)

    Return 0
End Process
