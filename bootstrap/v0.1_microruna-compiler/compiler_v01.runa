Note: MicroRunaV01IntegratedCompiler

Process called "compile" that takes source_code as String returns String:
    Note: MainCompilationPipeline

    Let tokens be tokenize(source_code)
    Let ast be parse_program(tokens)
    Let checked_ast be typecheck_program(ast)
    Let assembly_code be generate_code(checked_ast)

    Return assembly_code
End Process

Process called "test_main":
    Note: CompilerEntry

    Let test_program be "Let x be 42\nDisplay to_string(x)"
    Let result be compile(test_program)
    Display result
End Process