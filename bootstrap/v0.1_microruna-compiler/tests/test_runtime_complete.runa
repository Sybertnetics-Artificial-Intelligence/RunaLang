Note: TestRuntimeComplete

Process called "test_main":
    Note: ComprehensiveRuntimeTest

    Let greeting be "Hello"
    Let world be " World"
    Let full_greeting be concat(greeting, world)
    Display full_greeting

    Let test_string be "Testing"
    Let str_length be length_of(test_string)
    Display to_string(str_length)

    Let first_char be char_at(test_string, 0)
    Display to_string(first_char)

    Let sub be substring(test_string, 0, 4)
    Display sub

    Let number be 42
    Let number_str be to_string(number)
    Display number_str

    Let combined be concat("Result: ", number_str)
    Display combined
End Process