Process called "main" takes argc as Integer, argv as Integer returns Integer:
    Print "=== Testing Basic Inline Assembly ===\n"

    Let result be 0

    Inline Assembly:
        "movq $42, %rax\n"     Note: Load immediate value 42 into rax
        "movq %rax, %0\n"      Note: Store result to output variable
        : "=r"(result)
        :
        : "rax"
    End Assembly

    Print "Inline assembly result: "
    Print integer_to_string(result)
    Print "\n"

    If result is equal to 42:
        Print "SUCCESS: Inline assembly working!\n"
        Return 0
    Otherwise:
        Print "FAILURE: Expected 42, got different value\n"
        Return 1
    End If
End Process