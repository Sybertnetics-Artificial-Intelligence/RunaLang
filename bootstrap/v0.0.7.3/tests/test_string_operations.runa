Process called "test_string_operations" returns Integer:
    Let str1 be "Hello"
    Let str2 be " World"
    Let concat_result be string_concat(str1, str2)
    Print concat_result

    Let compare_result be string_compare("apple", "banana")
    If compare_result is less than 0:
        Print "apple comes before banana"
    End

    Let equal_result be string_equals("test", "test")
    If equal_result is equal to 1:
        Print "Strings are equal"
    End

    Let num_str be "42"
    Let num_val be string_to_integer(num_str)
    Let doubled be num_val multiplied by 2
    Print doubled

    Let int_val be 123
    Let str_val be integer_to_string(int_val)
    Print str_val

    Let haystack be "The quick brown fox"
    Let needle be "brown"
    Let find_pos be string_find(haystack, needle)
    Print find_pos

    Let original be "Hello World"
    Let replaced be string_replace(original, "World", "Runa")
    Print replaced

    Let padded be "  spaces around  "
    Let trimmed be string_trim(padded)
    Print trimmed

    Let test_str be "Testing"
    Let length be string_length(test_str)
    Print length

    Let char_test be "ABCDEF"
    Let char_at_2 be string_char_at(char_test, 2)

    Let substr_test be "Substring Test"
    Let substr be string_substring(substr_test, 3, 6)
    Print substr

    Let dividend be 17
    Let divisor be 5
    Let remainder be dividend modulo by divisor
    Print remainder

    Let digit_str be "5"
    Let is_digit_result be is_digit(digit_str)
    If is_digit_result is equal to 1:
        Print "It's a digit"
    End

    Let alpha_str be "A"
    Let is_alpha_result be is_alpha(alpha_str)
    If is_alpha_result is equal to 1:
        Print "It's alphabetic"
    End

    Let space_str be " "
    Let is_space_result be is_whitespace(space_str)
    If is_space_result is equal to 1:
        Print "It's whitespace"
    End

    Return 0
End