Process called "main" takes argc as Integer, argv as Integer returns Integer:
    Let x be 10

    If x is less than 10:
        print_string("x is less than 10")
    Otherwise If x is equal to 10:
        print_string("x is equal to 10")
    Otherwise If x is greater than 10:
        print_string("x is greater than 10")
    Otherwise:
        print_string("no idea")
    End If

    Return 0
End Process
