# Test Inline Assembly: x86-64 instructions
Process called "main" returns Integer:
    # Test inline assembly - load a value into a register and return it
    Let result be 0

    Inline Assembly:
        "movq $42, %rax\n"    Note: Load value 42 into rax
    End Assembly

    # The value in %rax (42) becomes the return value
    Return result
End Process
