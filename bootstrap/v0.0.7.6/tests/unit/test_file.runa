Process called "read_file_internal" takes filename as String returns String:
    Let content be runtime_read_file(filename)
    If content is equal to 0:
        Let error_msg be string_concat("[MAIN ERROR] Could not open input file", filename)
        print_string(error_msg)
        deallocate(error_msg)
        Return 0
    End If
    Return content
End Process

Process called "main" takes argc as Integer, argv as Pointer returns Integer:
    Let file_content be read_file_internal("test.runa")
    If file_content is equal to 0:
        Return 1
    End If
    deallocate(file_content)
    Return 0
End Process