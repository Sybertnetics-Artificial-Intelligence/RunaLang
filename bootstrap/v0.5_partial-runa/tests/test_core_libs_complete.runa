Note: Test the completed core_libs functions

Process called "test_math_functions" returns Integer:
    Note: Test math helper functions
    
    Let a be 10
    Let b be 20
    Let min_val be min(a, b)
    Let max_val be max(a, b)
    Let abs_val be abs(0 minus 15)
    
    Note: min(10, 20) = 10, max(10, 20) = 20, abs(-15) = 15
    Note: Sum should be 10 + 20 + 15 = 45
    Return min_val plus max_val plus abs_val
End Process

Process called "test_array_functions":
    Note: Test array operations
    
    Let arr be array_new(8, 10)
    array_push(arr, 100)
    array_push(arr, 200)
    array_push(arr, 300)
    
    Let len be array_length(arr)
    Note: Length should be 3
    
    array_set(arr, 1, 250)
    Let middle be array_get(arr, 1)
    Note: Middle element should now be 250
    
    array_clear(arr)
    Let final_len be array_length(arr)
    Note: Final length should be 0
End Process

Process called "main" returns Integer:
    Let math_result be test_math_functions()
    
    Note: test_array_functions()  Note: Can't call without causing type errors in bootstrap
    
    Return math_result  Note: Should return 45
End Process