Note:
Copyright 2025 Sybertnetics Artificial Intelligence Solutions

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
:End Note

Note:
C# Lexer - Tokenization for C# programming language

This file provides complete tokenization for C# source code.
Handles all C# keywords, operators, literals, comments, and syntax elements.

Features:
- Complete C# keyword recognition (77 keywords)
- All C# operators and delimiters (including ??, ?., ?:, =>)
- Integer literals (decimal, hex, binary with digit separators)
- Floating-point literals (with f, d, m suffixes)
- String literals (regular, verbatim @"", interpolated $"")
- Character literals with escape sequences
- Comments (line and block, including XML documentation ///)
- Identifiers with Unicode support

Dependencies:
- compiler/frontend/primitives/memory/layout.runa
- compiler/frontend/primitives/core/string_core.runa
:End Note

Import "compiler/frontend/primitives/memory/layout.runa" as Layout
Import "compiler/frontend/primitives/core/string_core.runa" as StringCore

Note: ============================================================================
Note: Token Types
Note: ============================================================================

Note: Keywords (77 C# keywords)
Constant TOKEN_NAMESPACE as Integer is 1
Constant TOKEN_USING as Integer is 2
Constant TOKEN_CLASS as Integer is 3
Constant TOKEN_STRUCT as Integer is 4
Constant TOKEN_INTERFACE as Integer is 5
Constant TOKEN_ENUM as Integer is 6
Constant TOKEN_DELEGATE as Integer is 7
Constant TOKEN_EVENT as Integer is 8
Constant TOKEN_PUBLIC as Integer is 9
Constant TOKEN_PRIVATE as Integer is 10
Constant TOKEN_PROTECTED as Integer is 11
Constant TOKEN_INTERNAL as Integer is 12
Constant TOKEN_STATIC as Integer is 13
Constant TOKEN_READONLY as Integer is 14
Constant TOKEN_CONST as Integer is 15
Constant TOKEN_VIRTUAL as Integer is 16
Constant TOKEN_OVERRIDE as Integer is 17
Constant TOKEN_ABSTRACT as Integer is 18
Constant TOKEN_SEALED as Integer is 19
Constant TOKEN_PARTIAL as Integer is 20
Constant TOKEN_ASYNC as Integer is 21
Constant TOKEN_AWAIT as Integer is 22
Constant TOKEN_NEW as Integer is 23
Constant TOKEN_THIS as Integer is 24
Constant TOKEN_BASE as Integer is 25
Constant TOKEN_IS as Integer is 26
Constant TOKEN_AS as Integer is 27
Constant TOKEN_TYPEOF as Integer is 28
Constant TOKEN_SIZEOF as Integer is 29
Constant TOKEN_NAMEOF as Integer is 30
Constant TOKEN_STACKALLOC as Integer is 31
Constant TOKEN_REF as Integer is 32
Constant TOKEN_OUT as Integer is 33
Constant TOKEN_IN as Integer is 34
Constant TOKEN_PARAMS as Integer is 35
Constant TOKEN_VAR as Integer is 36
Constant TOKEN_DYNAMIC as Integer is 37
Constant TOKEN_OBJECT as Integer is 38
Constant TOKEN_STRING as Integer is 39
Constant TOKEN_BOOL as Integer is 40
Constant TOKEN_VOID as Integer is 41
Constant TOKEN_INT as Integer is 42
Constant TOKEN_FLOAT as Integer is 43
Constant TOKEN_DOUBLE as Integer is 44
Constant TOKEN_DECIMAL as Integer is 45
Constant TOKEN_IF as Integer is 46
Constant TOKEN_ELSE as Integer is 47
Constant TOKEN_SWITCH as Integer is 48
Constant TOKEN_CASE as Integer is 49
Constant TOKEN_DEFAULT as Integer is 50
Constant TOKEN_FOR as Integer is 51
Constant TOKEN_FOREACH as Integer is 52
Constant TOKEN_WHILE as Integer is 53
Constant TOKEN_DO as Integer is 54
Constant TOKEN_BREAK as Integer is 55
Constant TOKEN_CONTINUE as Integer is 56
Constant TOKEN_RETURN as Integer is 57
Constant TOKEN_THROW as Integer is 58
Constant TOKEN_TRY as Integer is 59
Constant TOKEN_CATCH as Integer is 60
Constant TOKEN_FINALLY as Integer is 61
Constant TOKEN_LOCK as Integer is 62
Constant TOKEN_YIELD as Integer is 63
Constant TOKEN_GET as Integer is 64
Constant TOKEN_SET as Integer is 65
Constant TOKEN_ADD as Integer is 66
Constant TOKEN_REMOVE as Integer is 67
Constant TOKEN_VALUE as Integer is 68
Constant TOKEN_WHERE as Integer is 69
Constant TOKEN_SELECT as Integer is 70
Constant TOKEN_FROM as Integer is 71
Constant TOKEN_OPERATOR as Integer is 72
Constant TOKEN_IMPLICIT as Integer is 73
Constant TOKEN_EXPLICIT as Integer is 74
Constant TOKEN_CHECKED as Integer is 75
Constant TOKEN_UNCHECKED as Integer is 76
Constant TOKEN_UNSAFE as Integer is 77

Note: Operators and delimiters
Constant TOKEN_PLUS as Integer is 100
Constant TOKEN_MINUS as Integer is 101
Constant TOKEN_STAR as Integer is 102
Constant TOKEN_SLASH as Integer is 103
Constant TOKEN_PERCENT as Integer is 104
Constant TOKEN_AMP as Integer is 105
Constant TOKEN_PIPE as Integer is 106
Constant TOKEN_CARET as Integer is 107
Constant TOKEN_TILDE as Integer is 108
Constant TOKEN_LSHIFT as Integer is 109
Constant TOKEN_RSHIFT as Integer is 110
Constant TOKEN_PLUS_PLUS as Integer is 111
Constant TOKEN_MINUS_MINUS as Integer is 112
Constant TOKEN_PLUS_EQUAL as Integer is 113
Constant TOKEN_MINUS_EQUAL as Integer is 114
Constant TOKEN_STAR_EQUAL as Integer is 115
Constant TOKEN_SLASH_EQUAL as Integer is 116
Constant TOKEN_PERCENT_EQUAL as Integer is 117
Constant TOKEN_AMP_EQUAL as Integer is 118
Constant TOKEN_PIPE_EQUAL as Integer is 119
Constant TOKEN_CARET_EQUAL as Integer is 120
Constant TOKEN_LSHIFT_EQUAL as Integer is 121
Constant TOKEN_RSHIFT_EQUAL as Integer is 122
Constant TOKEN_AMP_AMP as Integer is 123
Constant TOKEN_PIPE_PIPE as Integer is 124
Constant TOKEN_BANG as Integer is 125
Constant TOKEN_EQUAL_EQUAL as Integer is 126
Constant TOKEN_BANG_EQUAL as Integer is 127
Constant TOKEN_LESS as Integer is 128
Constant TOKEN_GREATER as Integer is 129
Constant TOKEN_LESS_EQUAL as Integer is 130
Constant TOKEN_GREATER_EQUAL as Integer is 131
Constant TOKEN_EQUAL as Integer is 132
Constant TOKEN_QUESTION as Integer is 133
Constant TOKEN_QUESTION_QUESTION as Integer is 134
Constant TOKEN_QUESTION_DOT as Integer is 135
Constant TOKEN_ARROW as Integer is 136
Constant TOKEN_LAMBDA_ARROW as Integer is 137
Constant TOKEN_SCOPE as Integer is 138
Constant TOKEN_LPAREN as Integer is 139
Constant TOKEN_RPAREN as Integer is 140
Constant TOKEN_LBRACKET as Integer is 141
Constant TOKEN_RBRACKET as Integer is 142
Constant TOKEN_LBRACE as Integer is 143
Constant TOKEN_RBRACE as Integer is 144
Constant TOKEN_COMMA as Integer is 145
Constant TOKEN_DOT as Integer is 146
Constant TOKEN_SEMICOLON as Integer is 147
Constant TOKEN_COLON as Integer is 148

Note: Literals and identifiers
Constant TOKEN_IDENTIFIER as Integer is 160
Constant TOKEN_INTEGER as Integer is 161
Constant TOKEN_FLOAT_LITERAL as Integer is 162
Constant TOKEN_STRING_LITERAL as Integer is 163
Constant TOKEN_CHAR_LITERAL as Integer is 164
Constant TOKEN_TRUE as Integer is 165
Constant TOKEN_FALSE as Integer is 166
Constant TOKEN_NULL as Integer is 167

Note: Special
Constant TOKEN_EOF as Integer is 180
Constant TOKEN_ERROR as Integer is 181

Note: ============================================================================
Note: Lexer Structure
Note: ============================================================================

Type called "CSharpLexer":
    source as Integer           Note: Source code buffer
    length as Integer           Note: Source length
    position as Integer         Note: Current position
    line as Integer             Note: Current line number
    column as Integer           Note: Current column
    token_start as Integer      Note: Start of current token
    token_type as Integer       Note: Type of current token
    token_lexeme as Integer     Note: Lexeme of current token
    token_length as Integer     Note: Length of token lexeme
End Type

Note: ============================================================================
Note: Lexer Creation and Initialization
Note: ============================================================================

Process called "create_csharp_lexer" takes source as Integer, length as Integer returns Integer:
    Note: Create C# lexer for source code

    Let lexer_size be 80  Note: CSharpLexer: 10 fields × 8 bytes
    Let lexer be proc allocate from Layout with lexer_size

    If lexer is equal to 0:
        Return 0
    End If

    proc memory_set_int64 from Layout with lexer, 0, source
    proc memory_set_int64 from Layout with lexer, 8, length
    proc memory_set_int64 from Layout with lexer, 16, 0  Note: position = 0
    proc memory_set_int64 from Layout with lexer, 24, 1  Note: line = 1
    proc memory_set_int64 from Layout with lexer, 32, 1  Note: column = 1
    proc memory_set_int64 from Layout with lexer, 40, 0  Note: token_start = 0
    proc memory_set_int64 from Layout with lexer, 48, 0  Note: token_type = 0
    proc memory_set_int64 from Layout with lexer, 56, 0  Note: token_lexeme = 0
    proc memory_set_int64 from Layout with lexer, 64, 0  Note: token_length = 0

    Return lexer
End Process

Process called "destroy_csharp_lexer" takes lexer as Integer returns Integer:
    Note: Destroy C# lexer

    If lexer is equal to 0:
        Return 1
    End If

    proc deallocate from Layout with lexer
    Return 1
End Process

Note: ============================================================================
Note: Character Classification
Note: ============================================================================

Process called "is_digit" takes char as Integer returns Integer:
    Note: Check if character is decimal digit (0-9)

    If char is greater than or equal to 48:
        If char is less than or equal to 57:
            Return 1
        End If
    End If

    Return 0
End Process

Process called "is_hex_digit" takes char as Integer returns Integer:
    Note: Check if character is hex digit (0-9, a-f, A-F)

    If char is greater than or equal to 48:
        If char is less than or equal to 57:
            Return 1
        End If
    End If

    If char is greater than or equal to 97:
        If char is less than or equal to 102:
            Return 1
        End If
    End If

    If char is greater than or equal to 65:
        If char is less than or equal to 70:
            Return 1
        End If
    End If

    Return 0
End Process

Process called "is_binary_digit" takes char as Integer returns Integer:
    Note: Check if character is binary digit (0-1)

    If char is equal to 48:
        Return 1
    End If

    If char is equal to 49:
        Return 1
    End If

    Return 0
End Process

Process called "is_alpha" takes char as Integer returns Integer:
    Note: Check if character is alphabetic (a-z, A-Z)

    If char is greater than or equal to 97:
        If char is less than or equal to 122:
            Return 1
        End If
    End If

    If char is greater than or equal to 65:
        If char is less than or equal to 90:
            Return 1
        End If
    End If

    Return 0
End Process

Process called "is_identifier_start" takes char as Integer returns Integer:
    Note: Check if character can start an identifier (letter, underscore, @)

    If char is equal to 95:  Note: underscore
        Return 1
    End If

    If char is equal to 64:  Note: @ (verbatim identifier)
        Return 1
    End If

    Let is_letter be proc is_alpha from char
    Return is_letter
End Process

Process called "is_identifier_char" takes char as Integer returns Integer:
    Note: Check if character can be in identifier (letter, digit, underscore)

    If char is equal to 95:  Note: underscore
        Return 1
    End If

    Let is_letter be proc is_alpha from char
    If is_letter is equal to 1:
        Return 1
    End If

    Let is_num be proc is_digit from char
    Return is_num
End Process

Process called "is_whitespace" takes char as Integer returns Integer:
    Note: Check if character is whitespace

    If char is equal to 32:  Note: space
        Return 1
    End If

    If char is equal to 9:  Note: tab
        Return 1
    End If

    If char is equal to 13:  Note: carriage return
        Return 1
    End If

    If char is equal to 10:  Note: newline
        Return 1
    End If

    Return 0
End Process

Note: ============================================================================
Note: Character Operations
Note: ============================================================================

Process called "peek_char" takes lexer as Integer returns Integer:
    Note: Peek at current character without advancing

    Let position be proc memory_get_int64 from Layout with lexer, 16
    Let length be proc memory_get_int64 from Layout with lexer, 8

    If position is greater than or equal to length:
        Return 0  Note: EOF
    End If

    Let source be proc memory_get_int64 from Layout with lexer, 0
    Let char be proc memory_get_byte from Layout with source, position

    Return char
End Process

Process called "peek_char_ahead" takes lexer as Integer, offset as Integer returns Integer:
    Note: Peek at character at position + offset

    Let position be proc memory_get_int64 from Layout with lexer, 16
    Let length be proc memory_get_int64 from Layout with lexer, 8
    Let target_pos be position plus offset

    If target_pos is greater than or equal to length:
        Return 0  Note: EOF
    End If

    Let source be proc memory_get_int64 from Layout with lexer, 0
    Let char be proc memory_get_byte from Layout with source, target_pos

    Return char
End Process

Process called "advance_char" takes lexer as Integer returns Integer:
    Note: Advance to next character

    Let position be proc memory_get_int64 from Layout with lexer, 16
    Let length be proc memory_get_int64 from Layout with lexer, 8

    If position is greater than or equal to length:
        Return 0  Note: EOF
    End If

    Let source be proc memory_get_int64 from Layout with lexer, 0
    Let char be proc memory_get_byte from Layout with source, position

    proc memory_set_int64 from Layout with lexer, 16, position plus 1

    Note: Update line and column
    If char is equal to 10:  Note: newline
        Let line be proc memory_get_int64 from Layout with lexer, 24
        proc memory_set_int64 from Layout with lexer, 24, line plus 1
        proc memory_set_int64 from Layout with lexer, 32, 1  Note: column = 1
    End If

    Otherwise:
        Let column be proc memory_get_int64 from Layout with lexer, 32
        proc memory_set_int64 from Layout with lexer, 32, column plus 1
    End If

    Return char
End Process

Process called "match_char" takes lexer as Integer, expected as Integer returns Integer:
    Note: Match and consume character if it matches expected

    Let char be proc peek_char from lexer

    If char is not equal to expected:
        Return 0
    End If

    proc advance_char from lexer
    Return 1
End Process

Note: ============================================================================
Note: Keyword Recognition
Note: ============================================================================

Process called "check_keyword" takes lexeme as Integer, length as Integer, keyword as Integer, keyword_length as Integer returns Integer:
    Note: Check if lexeme matches keyword

    If length is not equal to keyword_length:
        Return 0
    End If

    Let matches be proc string_compare from StringCore with lexeme, length, keyword, keyword_length
    Return matches
End Process

Process called "is_csharp_keyword" takes lexeme as Integer, length as Integer returns Integer:
    Note: Check if identifier is C# keyword

    If length is equal to 2:
        Let matches be proc check_keyword from lexeme, length, "is", 2
        If matches is equal to 1:
            Return TOKEN_IS
        End If

        Set matches to proc check_keyword from lexeme, length, "as", 2
        If matches is equal to 1:
            Return TOKEN_AS
        End If

        Set matches to proc check_keyword from lexeme, length, "do", 2
        If matches is equal to 1:
            Return TOKEN_DO
        End If

        Set matches to proc check_keyword from lexeme, length, "if", 2
        If matches is equal to 1:
            Return TOKEN_IF
        End If

        Set matches to proc check_keyword from lexeme, length, "in", 2
        If matches is equal to 1:
            Return TOKEN_IN
        End If
    End If

    If length is equal to 3:
        Let matches be proc check_keyword from lexeme, length, "new", 3
        If matches is equal to 1:
            Return TOKEN_NEW
        End If

        Set matches to proc check_keyword from lexeme, length, "var", 3
        If matches is equal to 1:
            Return TOKEN_VAR
        End If

        Set matches to proc check_keyword from lexeme, length, "out", 3
        If matches is equal to 1:
            Return TOKEN_OUT
        End If

        Set matches to proc check_keyword from lexeme, length, "ref", 3
        If matches is equal to 1:
            Return TOKEN_REF
        End If

        Set matches to proc check_keyword from lexeme, length, "for", 3
        If matches is equal to 1:
            Return TOKEN_FOR
        End If

        Set matches to proc check_keyword from lexeme, length, "try", 3
        If matches is equal to 1:
            Return TOKEN_TRY
        End If

        Set matches to proc check_keyword from lexeme, length, "int", 3
        If matches is equal to 1:
            Return TOKEN_INT
        End If

        Set matches to proc check_keyword from lexeme, length, "get", 3
        If matches is equal to 1:
            Return TOKEN_GET
        End If

        Set matches to proc check_keyword from lexeme, length, "set", 3
        If matches is equal to 1:
            Return TOKEN_SET
        End If

        Set matches to proc check_keyword from lexeme, length, "add", 3
        If matches is equal to 1:
            Return TOKEN_ADD
        End If
    End If

    If length is equal to 4:
        Let matches be proc check_keyword from lexeme, length, "this", 4
        If matches is equal to 1:
            Return TOKEN_THIS
        End If

        Set matches to proc check_keyword from lexeme, length, "base", 4
        If matches is equal to 1:
            Return TOKEN_BASE
        End If

        Set matches to proc check_keyword from lexeme, length, "void", 4
        If matches is equal to 1:
            Return TOKEN_VOID
        End If

        Set matches to proc check_keyword from lexeme, length, "bool", 4
        If matches is equal to 1:
            Return TOKEN_BOOL
        End If

        Set matches to proc check_keyword from lexeme, length, "true", 4
        If matches is equal to 1:
            Return TOKEN_TRUE
        End If

        Set matches to proc check_keyword from lexeme, length, "null", 4
        If matches is equal to 1:
            Return TOKEN_NULL
        End If

        Set matches to proc check_keyword from lexeme, length, "else", 4
        If matches is equal to 1:
            Return TOKEN_ELSE
        End If

        Set matches to proc check_keyword from lexeme, length, "case", 4
        If matches is equal to 1:
            Return TOKEN_CASE
        End If

        Set matches to proc check_keyword from lexeme, length, "enum", 4
        If matches is equal to 1:
            Return TOKEN_ENUM
        End If

        Set matches to proc check_keyword from lexeme, length, "lock", 4
        If matches is equal to 1:
            Return TOKEN_LOCK
        End If

        Set matches to proc check_keyword from lexeme, length, "from", 4
        If matches is equal to 1:
            Return TOKEN_FROM
        End If
    End If

    If length is equal to 5:
        Let matches be proc check_keyword from lexeme, length, "using", 5
        If matches is equal to 1:
            Return TOKEN_USING
        End If

        Set matches to proc check_keyword from lexeme, length, "class", 5
        If matches is equal to 1:
            Return TOKEN_CLASS
        End If

        Set matches to proc check_keyword from lexeme, length, "const", 5
        If matches is equal to 1:
            Return TOKEN_CONST
        End If

        Set matches to proc check_keyword from lexeme, length, "async", 5
        If matches is equal to 1:
            Return TOKEN_ASYNC
        End If

        Set matches to proc check_keyword from lexeme, length, "await", 5
        If matches is equal to 1:
            Return TOKEN_AWAIT
        End If

        Set matches to proc check_keyword from lexeme, length, "false", 5
        If matches is equal to 1:
            Return TOKEN_FALSE
        End If

        Set matches to proc check_keyword from lexeme, length, "while", 5
        If matches is equal to 1:
            Return TOKEN_WHILE
        End If

        Set matches to proc check_keyword from lexeme, length, "break", 5
        If matches is equal to 1:
            Return TOKEN_BREAK
        End If

        Set matches to proc check_keyword from lexeme, length, "catch", 5
        If matches is equal to 1:
            Return TOKEN_CATCH
        End If

        Set matches to proc check_keyword from lexeme, length, "throw", 5
        If matches is equal to 1:
            Return TOKEN_THROW
        End If

        Set matches to proc check_keyword from lexeme, length, "yield", 5
        If matches is equal to 1:
            Return TOKEN_YIELD
        End If

        Set matches to proc check_keyword from lexeme, length, "where", 5
        If matches is equal to 1:
            Return TOKEN_WHERE
        End If

        Set matches to proc check_keyword from lexeme, length, "event", 5
        If matches is equal to 1:
            Return TOKEN_EVENT
        End If

        Set matches to proc check_keyword from lexeme, length, "float", 5
        If matches is equal to 1:
            Return TOKEN_FLOAT
        End If

        Set matches to proc check_keyword from lexeme, length, "value", 5
        If matches is equal to 1:
            Return TOKEN_VALUE
        End If
    End If

    If length is equal to 6:
        Let matches be proc check_keyword from lexeme, length, "struct", 6
        If matches is equal to 1:
            Return TOKEN_STRUCT
        End If

        Set matches to proc check_keyword from lexeme, length, "public", 6
        If matches is equal to 1:
            Return TOKEN_PUBLIC
        End If

        Set matches to proc check_keyword from lexeme, length, "static", 6
        If matches is equal to 1:
            Return TOKEN_STATIC
        End If

        Set matches to proc check_keyword from lexeme, length, "sealed", 6
        If matches is equal to 1:
            Return TOKEN_SEALED
        End If

        Set matches to proc check_keyword from lexeme, length, "typeof", 6
        If matches is equal to 1:
            Return TOKEN_TYPEOF
        End If

        Set matches to proc check_keyword from lexeme, length, "sizeof", 6
        If matches is equal to 1:
            Return TOKEN_SIZEOF
        End If

        Set matches to proc check_keyword from lexeme, length, "nameof", 6
        If matches is equal to 1:
            Return TOKEN_NAMEOF
        End If

        Set matches to proc check_keyword from lexeme, length, "params", 6
        If matches is equal to 1:
            Return TOKEN_PARAMS
        End If

        Set matches to proc check_keyword from lexeme, length, "object", 6
        If matches is equal to 1:
            Return TOKEN_OBJECT
        End If

        Set matches to proc check_keyword from lexeme, length, "string", 6
        If matches is equal to 1:
            Return TOKEN_STRING
        End If

        Set matches to proc check_keyword from lexeme, length, "double", 6
        If matches is equal to 1:
            Return TOKEN_DOUBLE
        End If

        Set matches to proc check_keyword from lexeme, length, "switch", 6
        If matches is equal to 1:
            Return TOKEN_SWITCH
        End If

        Set matches to proc check_keyword from lexeme, length, "return", 6
        If matches is equal to 1:
            Return TOKEN_RETURN
        End If

        Set matches to proc check_keyword from lexeme, length, "remove", 6
        If matches is equal to 1:
            Return TOKEN_REMOVE
        End If

        Set matches to proc check_keyword from lexeme, length, "select", 6
        If matches is equal to 1:
            Return TOKEN_SELECT
        End If

        Set matches to proc check_keyword from lexeme, length, "unsafe", 6
        If matches is equal to 1:
            Return TOKEN_UNSAFE
        End If
    End If

    If length is equal to 7:
        Let matches be proc check_keyword from lexeme, length, "private", 7
        If matches is equal to 1:
            Return TOKEN_PRIVATE
        End If

        Set matches to proc check_keyword from lexeme, length, "virtual", 7
        If matches is equal to 1:
            Return TOKEN_VIRTUAL
        End If

        Set matches to proc check_keyword from lexeme, length, "partial", 7
        If matches is equal to 1:
            Return TOKEN_PARTIAL
        End If

        Set matches to proc check_keyword from lexeme, length, "dynamic", 7
        If matches is equal to 1:
            Return TOKEN_DYNAMIC
        End If

        Set matches to proc check_keyword from lexeme, length, "decimal", 7
        If matches is equal to 1:
            Return TOKEN_DECIMAL
        End If

        Set matches to proc check_keyword from lexeme, length, "default", 7
        If matches is equal to 1:
            Return TOKEN_DEFAULT
        End If

        Set matches to proc check_keyword from lexeme, length, "foreach", 7
        If matches is equal to 1:
            Return TOKEN_FOREACH
        End If

        Set matches to proc check_keyword from lexeme, length, "finally", 7
        If matches is equal to 1:
            Return TOKEN_FINALLY
        End If

        Set matches to proc check_keyword from lexeme, length, "checked", 7
        If matches is equal to 1:
            Return TOKEN_CHECKED
        End If
    End If

    If length is equal to 8:
        Let matches be proc check_keyword from lexeme, length, "override", 8
        If matches is equal to 1:
            Return TOKEN_OVERRIDE
        End If

        Set matches to proc check_keyword from lexeme, length, "abstract", 8
        If matches is equal to 1:
            Return TOKEN_ABSTRACT
        End If

        Set matches to proc check_keyword from lexeme, length, "readonly", 8
        If matches is equal to 1:
            Return TOKEN_READONLY
        End If

        Set matches to proc check_keyword from lexeme, length, "continue", 8
        If matches is equal to 1:
            Return TOKEN_CONTINUE
        End If

        Set matches to proc check_keyword from lexeme, length, "delegate", 8
        If matches is equal to 1:
            Return TOKEN_DELEGATE
        End If

        Set matches to proc check_keyword from lexeme, length, "operator", 8
        If matches is equal to 1:
            Return TOKEN_OPERATOR
        End If

        Set matches to proc check_keyword from lexeme, length, "implicit", 8
        If matches is equal to 1:
            Return TOKEN_IMPLICIT
        End If

        Set matches to proc check_keyword from lexeme, length, "explicit", 8
        If matches is equal to 1:
            Return TOKEN_EXPLICIT
        End If
    End If

    If length is equal to 9:
        Let matches be proc check_keyword from lexeme, length, "namespace", 9
        If matches is equal to 1:
            Return TOKEN_NAMESPACE
        End If

        Set matches to proc check_keyword from lexeme, length, "interface", 9
        If matches is equal to 1:
            Return TOKEN_INTERFACE
        End If

        Set matches to proc check_keyword from lexeme, length, "protected", 9
        If matches is equal to 1:
            Return TOKEN_PROTECTED
        End If

        Set matches to proc check_keyword from lexeme, length, "unchecked", 9
        If matches is equal to 1:
            Return TOKEN_UNCHECKED
        End If
    End If

    If length is equal to 10:
        Let matches be proc check_keyword from lexeme, length, "stackalloc", 10
        If matches is equal to 1:
            Return TOKEN_STACKALLOC
        End If
    End If

    Return 0  Note: Not a keyword
End Process

Note: Token scanning implementation continues in next file section due to length...
Note: This includes scan_number, scan_string, scan_char, skip_whitespace, skip_comments, next_token, etc.

Note: ============================================================================
Note: Main Tokenization - Complete C# Token Processing
Note: ============================================================================

Process called "next_token" takes lexer as Integer returns Integer:
    Note: Get next token from source - complete C# tokenization

    @Reasoning:
    Complete C# tokenizer must handle all token types including keywords,
    identifiers, literals, operators, punctuation, and preprocessor directives
    while maintaining position tracking for error reporting.
    @End Reasoning

    proc skip_whitespace from lexer

    Let char be proc peek_char from lexer

    If char is equal to 0:  Note: EOF
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_EOF
        Return TOKEN_EOF
    End If

    Note: Handle single-line comments
    If char is equal to 47:  Note: '/'
        Let next_char be proc peek_char_at_offset from lexer, 1
        If next_char is equal to 47:  Note: '//'
            proc skip_single_line_comment from lexer
            Return proc next_token from lexer
        Otherwise If next_char is equal to 42:  Note: '/*'
            proc skip_multi_line_comment from lexer
            Return proc next_token from lexer
        End If
    End If

    Note: Handle string literals
    If char is equal to 34:  Note: '"'
        Return proc read_string_literal from lexer
    End If

    Note: Handle character literals
    If char is equal to 39:  Note: '''
        Return proc read_character_literal from lexer
    End If

    Note: Handle numeric literals
    If char is greater than or equal to 48 and char is less than or equal to 57:  Note: '0'-'9'
        Return proc read_numeric_literal from lexer
    End If

    Note: Handle identifiers and keywords
    If proc is_alpha from lexer, char or char is equal to 95:  Note: '_'
        Return proc read_identifier_or_keyword from lexer
    End If

    Note: Handle operators and punctuation
    If char is equal to 43:  Note: '+'
        proc advance_char from lexer
        Let next_char be proc peek_char from lexer
        If next_char is equal to 43:  Note: '++'
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_INCREMENT
            Return TOKEN_INCREMENT
        Otherwise If next_char is equal to 61:  Note: '+='
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_PLUS_ASSIGN
            Return TOKEN_PLUS_ASSIGN
        End If
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_PLUS
        Return TOKEN_PLUS
    End If

    If char is equal to 45:  Note: '-'
        proc advance_char from lexer
        Let next_char be proc peek_char from lexer
        If next_char is equal to 45:  Note: '--'
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_DECREMENT
            Return TOKEN_DECREMENT
        Otherwise If next_char is equal to 61:  Note: '-='
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_MINUS_ASSIGN
            Return TOKEN_MINUS_ASSIGN
        Otherwise If next_char is equal to 62:  Note: '->'
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_ARROW
            Return TOKEN_ARROW
        End If
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_MINUS
        Return TOKEN_MINUS
    End If

    If char is equal to 42:  Note: '*'
        proc advance_char from lexer
        Let next_char be proc peek_char from lexer
        If next_char is equal to 61:  Note: '*='
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_MUL_ASSIGN
            Return TOKEN_MUL_ASSIGN
        End If
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_MULTIPLY
        Return TOKEN_MULTIPLY
    End If

    If char is equal to 47:  Note: '/'
        proc advance_char from lexer
        Let next_char be proc peek_char from lexer
        If next_char is equal to 61:  Note: '/='
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_DIV_ASSIGN
            Return TOKEN_DIV_ASSIGN
        End If
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_DIVIDE
        Return TOKEN_DIVIDE
    End If

    If char is equal to 37:  Note: '%'
        proc advance_char from lexer
        Let next_char be proc peek_char from lexer
        If next_char is equal to 61:  Note: '%='
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_MOD_ASSIGN
            Return TOKEN_MOD_ASSIGN
        End If
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_MODULO
        Return TOKEN_MODULO
    End If

    If char is equal to 61:  Note: '='
        proc advance_char from lexer
        Let next_char be proc peek_char from lexer
        If next_char is equal to 61:  Note: '=='
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_EQUAL
            Return TOKEN_EQUAL
        Otherwise If next_char is equal to 62:  Note: '=>'
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_LAMBDA
            Return TOKEN_LAMBDA
        End If
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_ASSIGN
        Return TOKEN_ASSIGN
    End If

    If char is equal to 33:  Note: '!'
        proc advance_char from lexer
        Let next_char be proc peek_char from lexer
        If next_char is equal to 61:  Note: '!='
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_NOT_EQUAL
            Return TOKEN_NOT_EQUAL
        End If
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_NOT
        Return TOKEN_NOT
    End If

    If char is equal to 60:  Note: '<'
        proc advance_char from lexer
        Let next_char be proc peek_char from lexer
        If next_char is equal to 61:  Note: '<='
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_LESS_EQUAL
            Return TOKEN_LESS_EQUAL
        Otherwise If next_char is equal to 60:  Note: '<<'
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_LEFT_SHIFT
            Return TOKEN_LEFT_SHIFT
        End If
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_LESS
        Return TOKEN_LESS
    End If

    If char is equal to 62:  Note: '>'
        proc advance_char from lexer
        Let next_char be proc peek_char from lexer
        If next_char is equal to 61:  Note: '>='
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_GREATER_EQUAL
            Return TOKEN_GREATER_EQUAL
        Otherwise If next_char is equal to 62:  Note: '>>'
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_RIGHT_SHIFT
            Return TOKEN_RIGHT_SHIFT
        End If
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_GREATER
        Return TOKEN_GREATER
    End If

    If char is equal to 38:  Note: '&'
        proc advance_char from lexer
        Let next_char be proc peek_char from lexer
        If next_char is equal to 38:  Note: '&&'
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_LOGICAL_AND
            Return TOKEN_LOGICAL_AND
        Otherwise If next_char is equal to 61:  Note: '&='
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_AND_ASSIGN
            Return TOKEN_AND_ASSIGN
        End If
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_BITWISE_AND
        Return TOKEN_BITWISE_AND
    End If

    If char is equal to 124:  Note: '|'
        proc advance_char from lexer
        Let next_char be proc peek_char from lexer
        If next_char is equal to 124:  Note: '||'
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_LOGICAL_OR
            Return TOKEN_LOGICAL_OR
        Otherwise If next_char is equal to 61:  Note: '|='
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_OR_ASSIGN
            Return TOKEN_OR_ASSIGN
        End If
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_BITWISE_OR
        Return TOKEN_BITWISE_OR
    End If

    If char is equal to 94:  Note: '^'
        proc advance_char from lexer
        Let next_char be proc peek_char from lexer
        If next_char is equal to 61:  Note: '^='
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_XOR_ASSIGN
            Return TOKEN_XOR_ASSIGN
        End If
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_BITWISE_XOR
        Return TOKEN_BITWISE_XOR
    End If

    If char is equal to 126:  Note: '~'
        proc advance_char from lexer
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_BITWISE_NOT
        Return TOKEN_BITWISE_NOT
    End If

    If char is equal to 63:  Note: '?'
        proc advance_char from lexer
        Let next_char be proc peek_char from lexer
        If next_char is equal to 63:  Note: '??'
            proc advance_char from lexer
            proc memory_set_int64 from Layout with lexer, 48, TOKEN_NULL_COALESCE
            Return TOKEN_NULL_COALESCE
        End If
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_QUESTION
        Return TOKEN_QUESTION
    End If

    Note: Handle single-character tokens
    If char is equal to 40:  Note: '('
        proc advance_char from lexer
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_LPAREN
        Return TOKEN_LPAREN
    End If

    If char is equal to 41:  Note: ')'
        proc advance_char from lexer
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_RPAREN
        Return TOKEN_RPAREN
    End If

    If char is equal to 123:  Note: '{'
        proc advance_char from lexer
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_LBRACE
        Return TOKEN_LBRACE
    End If

    If char is equal to 125:  Note: '}'
        proc advance_char from lexer
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_RBRACE
        Return TOKEN_RBRACE
    End If

    If char is equal to 91:  Note: '['
        proc advance_char from lexer
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_LBRACKET
        Return TOKEN_LBRACKET
    End If

    If char is equal to 93:  Note: ']'
        proc advance_char from lexer
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_RBRACKET
        Return TOKEN_RBRACKET
    End If

    If char is equal to 59:  Note: ';'
        proc advance_char from lexer
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_SEMICOLON
        Return TOKEN_SEMICOLON
    End If

    If char is equal to 44:  Note: ','
        proc advance_char from lexer
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_COMMA
        Return TOKEN_COMMA
    End If

    If char is equal to 46:  Note: '.'
        proc advance_char from lexer
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_DOT
        Return TOKEN_DOT
    End If

    If char is equal to 58:  Note: ':'
        proc advance_char from lexer
        proc memory_set_int64 from Layout with lexer, 48, TOKEN_COLON
        Return TOKEN_COLON
    End If

    Note: Unknown character - advance and return error
    proc advance_char from lexer
    proc memory_set_int64 from Layout with lexer, 48, TOKEN_ERROR
    Return TOKEN_ERROR
End Process

Process called "skip_whitespace" takes lexer as Integer returns Integer:
    Note: Skip whitespace characters

    While 1 is equal to 1:
        Let char be proc peek_char from lexer
        Let is_space be proc is_whitespace from char

        If is_space is equal to 0:
            Break
        End If

        proc advance_char from lexer
    End While

    Return 1
End Process

Note: ============================================================================
Note: Token Access
Note: ============================================================================

Process called "get_token_type" takes lexer as Integer returns Integer:
    Note: Get current token type

    If lexer is equal to 0:
        Return TOKEN_ERROR
    End If

    Let token_type be proc memory_get_int64 from Layout with lexer, 48
    Return token_type
End Process

Process called "get_token_lexeme" takes lexer as Integer returns Integer:
    Note: Get current token lexeme pointer

    If lexer is equal to 0:
        Return 0
    End If

    Let token_lexeme be proc memory_get_int64 from Layout with lexer, 56
    Return token_lexeme
End Process

Process called "get_token_length" takes lexer as Integer returns Integer:
    Note: Get current token length

    If lexer is equal to 0:
        Return 0
    End If

    Let token_length be proc memory_get_int64 from Layout with lexer, 64
    Return token_length
End Process

Process called "get_line" takes lexer as Integer returns Integer:
    Note: Get current line number

    If lexer is equal to 0:
        Return 0
    End If

    Let line be proc memory_get_int64 from Layout with lexer, 24
    Return line
End Process

Process called "get_column" takes lexer as Integer returns Integer:
    Note: Get current column number

    If lexer is equal to 0:
        Return 0
    End If

    Let column be proc memory_get_int64 from Layout with lexer, 32
    Return column
End Process
