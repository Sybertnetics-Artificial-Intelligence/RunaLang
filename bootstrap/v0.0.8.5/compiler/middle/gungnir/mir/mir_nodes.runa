Note:
Copyright 2025 Sybertnetics Artificial Intelligence Solutions

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
:End Note

Note:
This file handles specialized MIR node definitions and instruction-specific structures.

This file performs the following tasks:
- Process MIR specialized node definitions for different instruction types
- Handle MIR phi nodes for SSA value merging at control flow join points
- Manage MIR binary/unary operation nodes with type information
- Process MIR memory operation nodes (load, store, alloca)
- Handle MIR control flow nodes (branch, jump, return, call)

This file is essential because of the following reasons:
- Specialized MIR nodes provide instruction-specific data and behavior
- Proper phi node handling enables correct SSA form construction
- Specialized nodes enable efficient instruction creation and manipulation
- Type-specific structures simplify optimization pass implementation

This file consists of the following functions/features/operation types:
- Specialized MIR node type definitions (phi, binary, unary, memory, control flow)
- Creation functions for each specialized node type
- Access functions for instruction-specific fields
- Debug printing functions for specialized node formats

Dependencies:
- Imports core MIR types from mir.runa
- Imports HIR types for type information
- Imports memory/layout.runa for OWNED HEAP allocation (Tier 3)
- Imports collections/list.runa for phi incoming value management
- Imports core/string_core.runa for debug printing
:End Note

Import "compiler/middle/gungnir/mir/mir.runa" as MIR
Import "compiler/middle/gungnir/hir/hir.runa" as HIR
Import "compiler/frontend/primitives/memory/layout.runa" as Layout
Import "compiler/frontend/primitives/collections/list.runa" as List
Import "compiler/frontend/primitives/core/string_core.runa" as StringCore
Import "compiler/internal/string_utils.runa" as StringUtils

Note: ============================================================================
Note: MIR Metadata Structure
Note: Stores variable names, debug information, and type metadata for MIR nodes
Note: Using TIER 3 (OWNED HEAP) allocation
Note: ============================================================================

@Reasoning
MIR metadata stores source-level information for debugging, optimization hints,
and variable name tracking. This enables proper variable name lookups and 
provides complete type information for type checking.

The metadata structure includes:
- variable_name: Original source variable name
- source_location: File, line, column for debugging
- type_metadata: Complete type information pointer
- optimization_hints: Compiler hints (inline, unroll, etc.)
@End Reasoning

Type called "MIRMetadata":
    variable_name as Integer         Note: String pointer to original variable name
    source_file as Integer           Note: String pointer to source file path
    source_line as Integer           Note: Line number in source file
    source_column as Integer         Note: Column number in source file
    type_metadata as Integer         Note: Complete HIRTypeInfo pointer
    optimization_hints as Integer    Note: Optimization hint flags
    debug_info as Integer            Note: Additional debug information pointer
End Type

Note: Size: 7 fields * 8 bytes = 56 bytes per MIRMetadata

Note: ============================================================================
Note: Specialized MIR Node Type Definitions
Note: These types extend the base MIRNode with instruction-specific data
Note: Using TIER 3 (OWNED HEAP) allocation for all structures
Note: ============================================================================

Note:
OD-1 PREFIX EMBEDDING (owner ruling 2026-06-10, task #432):
Every specialized record below is allocated as a SINGLE block whose first
72 bytes are the canonical MIRNode header — opcode@0, value_id@8,
operand1@16, operand2@24, operand3@32, type_info@40, next@48,
parent_block@56, metadata@64 — and whose extended fields follow at byte
offset 72 and beyond. The record pointer IS the MIRNode pointer: a plain
header consumer (opcode dispatch, terminator scan, operand walk) reads the
record directly with the MIRNode accessors, and the extended accessors in
this file read the type-specific payload at 72+.

The Type blocks below predate the ruling and still list a "base" first
field for historical reference. Under prefix embedding that slot no longer
exists: each Type's listed field at former offset K (K >= 8) now lives at
byte offset K + 64. The authoritative byte-offset table is Part 1.1 of
docs/dev/MIR_RECONCILIATION_PLAN.md; the constructors in this file are the
implementation of record.

Record sizes under prefix embedding (header 72 B + extended payload):
  MIRPhiNode 112, MIRBinaryOp 104, MIRUnaryOp 104 (96 B payload + 8 B pad
  so the 20002-20016 band has one uniform clone size), MIRLoad 112,
  MIRStore 112, MIRCall 144, MIRBranch 112, MIRJump 80, MIRReturn 88,
  MIRAlloca 104, MIRConst 112, MIRSwitch 112, MIRAtomicOp 120 (fence
  shares the layout), MIRVectorOp 128, MIRInvoke 136, MIRLandingPad 120,
  MIRThrow 88, MIRDebugValue 120, MIRDebugLocation 112.
MIR.clone_instruction sizes its copies from this table; the two MUST stay
in lockstep.
:End Note

@Reasoning
Phi nodes are critical for SSA form. They merge values from different control
flow paths at join points (blocks with multiple predecessors). Each phi node
maintains a list of (predecessor_block_id, value_id) pairs to specify which
value to use depending on which predecessor was taken.

Example: %3 = phi i32 [%1, block_0], [%2, block_1]
This means %3 gets value %1 if coming from block_0, or %2 if from block_1.
@End Reasoning


Type called "MIRPhiNode":
    base as Integer                Note: Base MIRNode pointer
    result_value_id as Integer     Note: SSA being defined by this phi
    incoming_pairs as Integer      Note: List of (block_id, value_id) tuples
    incoming_count as Integer      Note: Number of incoming values
    result_type as Integer         Note: Pointer to HIRTypeInfo for result
    parent_block as Integer        Note: Block containing this phi
End Type

Note: Size: 6 fields * 8 bytes = 48 bytes per MIRPhiNode

@Reasoning
Binary operations (add, sub, mul, div, comparisons, logical ops) are the
most common instructions in SSA form. We store both operands, the result,
and type information for type checking and optimization.
@End Reasoning

Type called "MIRBinaryOp":
    base as Integer                Note: Base MIRNode pointer
    result_value_id as Integer     Note: Result SSA temporary
    left_operand as Integer        Note: Left operand value ID
    right_operand as Integer       Note: Right operand value ID
    result_type as Integer         Note: Result HIRTypeInfo pointer
End Type

Note: Size: 5 fields * 8 bytes = 40 bytes per MIRBinaryOp

@Reasoning
Unary operations (negation, bitwise NOT, type casts, etc.) have a single
operand and produce a single result. Simpler than binary ops but still
require type tracking.
@End Reasoning

Type called "MIRUnaryOp":
    base as Integer                Note: Base MIRNode pointer
    result_value_id as Integer     Note: Result SSA temporary
    operand as Integer             Note: Operand value ID
    result_type as Integer         Note: Result HIRTypeInfo pointer
End Type

Note: Size: 4 fields * 8 bytes = 32 bytes per MIRUnaryOp

@Reasoning
Load instructions read from memory. We track the address value (which must
be a pointer type), an optional offset for struct field access, the result
value, and whether the load is volatile (for memory-mapped I/O).
@End Reasoning

Type called "MIRLoad":
    base as Integer                Note: Base MIRNode pointer
    result_value_id as Integer     Note: Loaded value SSA temporary
    address_value_id as Integer    Note: Address to load from (pointer value)
    offset as Integer              Note: Byte offset from address (for fields)
    result_type as Integer         Note: Type being loaded (HIRTypeInfo)
    is_volatile as Integer         Note: Boolean: 1 if volatile, 0 otherwise
End Type

Note: Size: 6 fields * 8 bytes = 48 bytes per MIRLoad

@Reasoning
Store instructions write to memory. We track the address, the value being
stored, an optional offset, and volatility. Store instructions do not
produce a value (side-effect only).
@End Reasoning

Type called "MIRStore":
    base as Integer                Note: Base MIRNode pointer
    address_value_id as Integer    Note: Address to store to (pointer value)
    value_to_store as Integer      Note: Value SSA to store
    offset as Integer              Note: Byte offset from address (for fields)
    value_type as Integer          Note: Type being stored (HIRTypeInfo)
    is_volatile as Integer         Note: Boolean: 1 if volatile, 0 otherwise
End Type

Note: Size: 6 fields * 8 bytes = 48 bytes per MIRStore

@Reasoning
Function calls are complex instructions that can have multiple arguments
and produce a return value. We track the callee (function name or indirect
pointer), all argument values, return type, and whether this is a tail call
(eligible for tail call optimization).
@End Reasoning

Type called "MIRCall":
    base as Integer                Note: Base MIRNode pointer
    result_value_id as Integer     Note: Return value SSA (0 if void)
    callee as Integer              Note: Function name string or value ID (indirect)
    arguments as Integer           Note: List of argument value IDs
    argument_count as Integer      Note: Number of arguments
    return_type as Integer         Note: Return HIRTypeInfo pointer
    is_indirect as Integer         Note: Boolean: 1 if indirect call, 0 if direct
    is_tail_call as Integer        Note: Boolean: 1 if tail call, 0 otherwise
End Type

Note: Size: 8 fields * 8 bytes = 64 bytes per MIRCall

@Reasoning
Conditional branches are terminator instructions that end a basic block and
transfer control to one of two successor blocks based on a boolean condition.
Branch weights provide hints for branch prediction and profile-guided optimization.
@End Reasoning

Type called "MIRBranch":
    base as Integer                Note: Base MIRNode pointer
    condition_value_id as Integer  Note: Condition SSA (boolean)
    true_block_id as Integer       Note: Target block if condition is true
    false_block_id as Integer      Note: Target block if condition is false
    branch_weight_true as Integer  Note: Branch prediction weight for true (0-100)
    branch_weight_false as Integer Note: Branch prediction weight for false (0-100)
End Type

Note: Size: 6 fields * 8 bytes = 48 bytes per MIRBranch

@Reasoning
Unconditional jumps transfer control to a single target block. They are
terminator instructions and the simplest form of control flow.
@End Reasoning

Type called "MIRJump":
    base as Integer                Note: Base MIRNode pointer
    target_block_id as Integer     Note: Target block ID
End Type

Note: Size: 2 fields * 8 bytes = 16 bytes per MIRJump


@Reasoning
Return instructions end function execution and optionally return a value.
They are terminator instructions. Returns with no value (void functions)
have return_value_id = 0.
@End Reasoning


Type called "MIRReturn":
    base as Integer                Note: Base MIRNode pointer
    return_value_id as Integer     Note: Value to return (0 if void)
    return_type as Integer         Note: Return HIRTypeInfo pointer
End Type

Note: Size: 3 fields * 8 bytes = 24 bytes per MIRReturn

@Reasoning
Alloca instructions allocate memory on the stack. They produce a pointer value
and specify the type being allocated, the number of elements (for arrays),
and alignment requirements. Stack allocations are automatically freed when
the function returns.
@End Reasoning

Type called "MIRAlloca":
    base as Integer                Note: Base MIRNode pointer
    result_value_id as Integer     Note: Pointer SSA (result)
    element_type as Integer        Note: Type being allocated (HIRTypeInfo)
    count_value_id as Integer      Note: Number of elements (value ID, 1 for single)
    alignment as Integer           Note: Alignment requirement in bytes
End Type

Note: Size: 5 fields * 8 bytes = 40 bytes per MIRAlloca

@Reasoning
Constant instructions produce compile-time constant values. These are critical
for constant propagation and folding optimizations. We support both numeric
constants and string literal constants.
@End Reasoning

Type called "MIRConst":
    base as Integer                Note: Base MIRNode pointer
    result_value_id as Integer     Note: Constant SSA temporary
    constant_value as Integer      Note: The constant value (for numerics)
    value_type as Integer          Note: Constant HIRTypeInfo pointer
    is_string as Integer           Note: Boolean: 1 if string constant, 0 otherwise
    string_ptr as Integer          Note: If string constant, pointer to string data
End Type

Note: Size: 6 fields * 8 bytes = 48 bytes per MIRConst

@Reasoning
Switch instructions provide efficient multi-way branching based on a single value.
Each case maps a constant value to a target block. An optional default case
handles values that don't match any explicit case. This is critical for
implementing match expressions and switch statements efficiently.
@End Reasoning

Type called "MIRSwitch":
    base as Integer                Note: Base MIRNode pointer
    value_id as Integer            Note: Value being switched on
    cases as Integer               Note: List of (case_value, block_id) pairs
    case_count as Integer          Note: Number of cases
    default_block_id as Integer    Note: Default case block (fallthrough)
    value_type as Integer          Note: Type of switched value (HIRTypeInfo)
End Type

Note: Size: 6 fields * 8 bytes = 48 bytes per MIRSwitch

@Reasoning
Atomic operations provide thread-safe memory access with specified memory ordering.
Critical for lock-free data structures and concurrent programming. Memory ordering
controls visibility guarantees: relaxed, acquire, release, acq_rel, seq_cst.
@End Reasoning

Type called "MIRAtomicOp":
    base as Integer                Note: Base MIRNode pointer
    result_value_id as Integer     Note: Result SSA (0 for stores)
    address_value_id as Integer    Note: Address of atomic variable (pointer)
    operand_value_id as Integer    Note: Operand value (for RMW operations)
    expected_value_id as Integer   Note: Expected value (for CAS only, 0 otherwise)
    memory_order as Integer        Note: Memory ordering (0=relaxed, 1=acquire, 2=release, 3=acq_rel, 4=seq_cst)
    result_type as Integer         Note: Result HIRTypeInfo pointer
End Type

Note: Size: 7 fields * 8 bytes = 56 bytes per MIRAtomicOp

@Reasoning
Vector operations enable SIMD parallelism for high-performance numerical computing.
A single instruction operates on multiple data elements simultaneously. Vector width
is specified by element count (2, 4, 8, 16, etc.). Essential for optimization of
mathematical, graphics, and scientific workloads.
@End Reasoning

Type called "MIRVectorOp":
    base as Integer                Note: Base MIRNode pointer
    result_value_id as Integer     Note: Result vector SSA temporary
    operand1_value_id as Integer   Note: First operand vector
    operand2_value_id as Integer   Note: Second operand vector (0 for unary ops)
    operand3_value_id as Integer   Note: Third operand (for FMA, shuffle masks)
    vector_width as Integer        Note: Number of elements in vector
    element_type as Integer        Note: Element HIRTypeInfo pointer
    mask_data as Integer           Note: Shuffle mask data (for shuffle ops)
End Type

Note: Size: 8 fields * 8 bytes = 64 bytes per MIRVectorOp

@Reasoning
Exception handling enables structured error propagation and resource cleanup.
Invoke calls a function with exception handlers, landingpad receives exceptions,
resume continues propagation, throw raises new exceptions. Critical for
implementing try/catch/finally semantics and RAII patterns.
@End Reasoning

Type called "MIRInvoke":
    base as Integer                Note: Base MIRNode pointer
    result_value_id as Integer     Note: Return value SSA (0 if void)
    callee as Integer              Note: Function name string or value ID
    arguments as Integer           Note: List of argument value IDs
    argument_count as Integer      Note: Number of arguments
    return_type as Integer         Note: Return HIRTypeInfo pointer
    normal_block_id as Integer     Note: Block for normal return
    exception_block_id as Integer  Note: Block for exception handling
    is_indirect as Integer         Note: Boolean: 1 if indirect call, 0 if direct
End Type

Note: Size: 9 fields * 8 bytes = 72 bytes per MIRInvoke

Type called "MIRLandingPad":
    base as Integer                Note: Base MIRNode pointer
    result_value_id as Integer     Note: Exception object SSA temporary
    exception_type as Integer      Note: Expected exception HIRTypeInfo pointer
    catch_clauses as Integer       Note: List of (type_id, block_id) pairs for catch
    clause_count as Integer        Note: Number of catch clauses
    cleanup_block_id as Integer    Note: Cleanup/finally block (0 if none)
    is_cleanup as Integer          Note: Boolean: 1 if cleanup pad, 0 if catch pad
End Type

Note: Size: 7 fields * 8 bytes = 56 bytes per MIRLandingPad

Type called "MIRThrow":
    base as Integer                Note: Base MIRNode pointer
    exception_value_id as Integer  Note: Exception object to throw
    exception_type as Integer      Note: Exception HIRTypeInfo pointer
End Type

Note: Size: 3 fields * 8 bytes = 24 bytes per MIRThrow

@Reasoning
Debug information enables source-level debugging by mapping IR instructions back
to original source locations, variable names, and scopes. Essential for debuggers,
profilers, and error reporting with meaningful stack traces.
@End Reasoning

Type called "MIRDebugValue":
    base as Integer                Note: Base MIRNode pointer
    value_id as Integer            Note: SSA value being annotated
    variable_name as Integer       Note: Pointer to source variable name string
    source_file as Integer         Note: Pointer to source file path string
    source_line as Integer         Note: Source line number
    source_column as Integer       Note: Source column number
    scope_id as Integer            Note: Lexical scope identifier
End Type

Note: Size: 7 fields * 8 bytes = 56 bytes per MIRDebugValue

Type called "MIRDebugLocation":
    base as Integer                Note: Base MIRNode pointer
    source_file as Integer         Note: Pointer to source file path string
    source_line as Integer         Note: Source line number
    source_column as Integer       Note: Source column number
    scope_id as Integer            Note: Lexical scope identifier
    inlined_at as Integer          Note: Location where inlined (0 if not inlined)
End Type

Note: Size: 6 fields * 8 bytes = 48 bytes per MIRDebugLocation

Note: ============================================================================
Note: MIR Metadata Creation and Access Functions
Note: ============================================================================

Process called "create_mir_metadata" that takes variable_name as Integer, source_file as Integer, source_line as Integer, source_column as Integer, type_metadata as Integer returns Integer:
    @Reasoning
    Creates MIRMetadata structure with complete variable and debug information.
    @End Reasoning

    Note: Allocate MIRMetadata structure (7 fields * 8 bytes = 56 bytes)
    Let metadata_ptr be proc allocate from Layout with 56

    If metadata_ptr is equal to 0:
        Return 0
    End If

    Note: Set variable name (offset 0)
    proc memory_set_int64 from Layout with metadata_ptr, 0, variable_name

    Note: Set source file (offset 8)
    proc memory_set_int64 from Layout with metadata_ptr, 8, source_file

    Note: Set source line (offset 16)
    proc memory_set_int64 from Layout with metadata_ptr, 16, source_line

    Note: Set source column (offset 24)
    proc memory_set_int64 from Layout with metadata_ptr, 24, source_column

    Note: Set type metadata (offset 32)
    proc memory_set_int64 from Layout with metadata_ptr, 32, type_metadata

    Note: Initialize optimization hints to 0 (offset 40)
    proc memory_set_int64 from Layout with metadata_ptr, 40, 0

    Note: Initialize debug_info to 0 (offset 48)
    proc memory_set_int64 from Layout with metadata_ptr, 48, 0

    Return metadata_ptr
End Process

Process called "get_metadata_variable_name" that takes metadata_ptr as Integer returns Integer:
    @Reasoning
    Gets the original source variable name from MIR metadata.
    Returns string pointer, or 0 if no metadata or no variable name.
    @End Reasoning

    If metadata_ptr is equal to 0:
        Return 0
    End If

    Note: Get variable_name (offset 0)
    Let variable_name be proc memory_get_int64 from Layout with metadata_ptr, 0
    Return variable_name
End Process

Process called "set_metadata_variable_name" that takes metadata_ptr as Integer, variable_name as Integer returns Integer:
    @Reasoning
    Sets the variable name in MIR metadata.
    @End Reasoning

    If metadata_ptr is equal to 0:
        Return 0
    End If

    Note: Set variable_name (offset 0)
    proc memory_set_int64 from Layout with metadata_ptr, 0, variable_name
    Return 1
End Process

Process called "get_metadata_type_info" that takes metadata_ptr as Integer returns Integer:
    @Reasoning
    Gets the complete type information from MIR metadata.
    Returns HIRTypeInfo pointer, or 0 if no metadata or no type info.
    @End Reasoning

    If metadata_ptr is equal to 0:
        Return 0
    End If

    Note: Get type_metadata (offset 32)
    Let type_info be proc memory_get_int64 from Layout with metadata_ptr, 32
    Return type_info
End Process

Process called "set_metadata_type_info" that takes metadata_ptr as Integer, type_info as Integer returns Integer:
    @Reasoning
    Sets the complete type information in MIR metadata.
    @End Reasoning

    If metadata_ptr is equal to 0:
        Return 0
    End If

    Note: Set type_metadata (offset 32)
    proc memory_set_int64 from Layout with metadata_ptr, 32, type_info
    Return 1
End Process

Note: ============================================================================
Note: Specialized MIR Node Creation Functions
Note: ============================================================================


@Reasoning
SSA construction needs a value-merge primitive at every control-flow join.
The phi node is that primitive: it ties a single SSA result to one incoming
value per predecessor block. Creating the phi up front (with no incoming
pairs yet) decouples node allocation from the predecessor walk performed
by the dominance-frontier algorithm, which lets the SSA renamer add pairs
in any order without reallocating the node.

The 48-byte layout is fixed: offset 0 base MIRNode pointer, offset 8 result
value id, offset 16 incoming-pairs List, offset 24 incoming count, offset 32
result type, offset 40 parent block id. Storing the count separately from
the List length is intentional: it lets the verifier cross-check List
integrity in O(1).
@End Reasoning

@Implementation
Creates a phi node for SSA form. Phi nodes are placed at the beginning of
basic blocks with multiple predecessors. Incoming values are added after
creation using add_phi_incoming.
@End Implementation


Process called "create_mir_phi_node" that takes result_value_id as Integer, result_type as Integer, parent_block as Integer returns Integer:
    Note: OD-1 prefix embedding: single 112-byte allocation. Header (MIRNode,
    Note: 72 B) first; extended fields after at 72+: result_value_id at 72,
    Note: incoming_pairs at 80, incoming_count at 88, result_type at 96,
    Note: parent_block at 104. The record IS the node.
    Let phi_size be 112
    Let phi be proc allocate from Layout with phi_size

    If phi is equal to 0:
        Return 0
    End If

    proc init_mir_node_header from MIR with phi, MIR.MIR_PHI
    proc memory_set_int64 from Layout with phi, 72, result_value_id
    proc memory_set_int64 from Layout with phi, 96, result_type
    proc memory_set_int64 from Layout with phi, 104, parent_block

    Let incoming_pairs be proc create from List
    proc memory_set_int64 from Layout with phi, 80, incoming_pairs
    proc memory_set_int64 from Layout with phi, 88, 0

    proc set_mir_node_value_id from MIR with phi, result_value_id
    proc set_mir_node_type from MIR with phi, result_type
    proc set_mir_node_parent_block from MIR with phi, parent_block

    Return phi
End Process


@Reasoning
The SSA renamer discovers (predecessor, value) pairs incrementally as it
walks the dominance frontier. This proc is the append point that records
each pair. Encoding the pair as two consecutive List elements (block then
value) avoids a separate pair-struct allocation while keeping both ordering
guarantees (block-N at even index, value-N at odd index) trivially testable.

The redundant count stored at offset 24 lets later phases (verifier, dead-phi
elimination, copy propagation) read pair count without dividing list length
by two — useful because list length is itself an int64 load through Layout.
@End Reasoning

@Implementation
Adds an incoming value to a phi node from a specific predecessor block.
This builds the phi node's merge table mapping predecessors to values.
@End Implementation

Process called "add_phi_incoming" that takes phi as Integer, block_id as Integer, value_id as Integer returns Integer:
    If phi is equal to 0:
        Return 0
    End If

    Note: Prefix-embedded layout: incoming_pairs at 80, incoming_count at 88.
    Let incoming_pairs be proc memory_get_int64 from Layout with phi, 80
    proc append from List with incoming_pairs, block_id
    proc append from List with incoming_pairs, value_id

    Let incoming_count be proc memory_get_int64 from Layout with phi, 88
    Set incoming_count to incoming_count plus 1
    proc memory_set_int64 from Layout with phi, 88, incoming_count

    Return 1
End Process


@Reasoning
Binary operations dominate the instruction stream in any realistic program,
so the specialized node trades a few bytes of indirection (base MIRNode at
offset 0) for direct field access to left/right operands and result type
without dispatching through the generic operand-N getters in mir.runa.
This shape is what every binary-op rewriter (constant folding, GVN, algebraic
identities) expects: two operands and a result, all reachable in two loads.

The opcode is mirrored into the base node so generic walkers can treat the
binop as a plain MIRNode when they only care about opcode and successors.
@End Reasoning

@Implementation
Creates a binary operation instruction (add, sub, mul, div, comparisons, etc.).
The opcode specifies which binary operation to perform.
@End Implementation


Process called "create_mir_binary_op" that takes opcode as Integer, result_value_id as Integer, left_op as Integer, right_op as Integer, result_type as Integer returns Integer:
    Note: OD-1 prefix embedding: single 104-byte allocation. Header 0-71;
    Note: extended fields: result_value_id at 72, left_operand at 80,
    Note: right_operand at 88, result_type at 96.
    Let binop_size be 104
    Let binop be proc allocate from Layout with binop_size

    If binop is equal to 0:
        Return 0
    End If

    proc init_mir_node_header from MIR with binop, opcode
    proc memory_set_int64 from Layout with binop, 72, result_value_id
    proc memory_set_int64 from Layout with binop, 80, left_op
    proc memory_set_int64 from Layout with binop, 88, right_op
    proc memory_set_int64 from Layout with binop, 96, result_type

    proc set_mir_node_value_id from MIR with binop, result_value_id
    proc set_mir_node_operand from MIR with binop, 1, left_op
    proc set_mir_node_operand from MIR with binop, 2, right_op
    proc set_mir_node_type from MIR with binop, result_type

    Return binop
End Process


@Reasoning
Unary operations need only one operand slot, so the specialized layout is
intentionally smaller than binop (32 bytes vs 40). This matters for cache
density during peephole and combine passes that iterate every instruction:
keeping unary nodes compact lets them share cache lines with neighboring
nodes in the basic-block instruction list.

Casts and conversions are also represented as unary ops with their target
type held in result_type — the conversion semantics live in the opcode,
not the type field, so the type field can be reused for value-numbering
hashes without conflicting with cast direction.
@End Reasoning

@Implementation
Creates a unary operation instruction (negation, bitwise NOT, casts, etc.).
The opcode specifies which unary operation to perform.
@End Implementation


Process called "create_mir_unary_op" that takes opcode as Integer, result_value_id as Integer, operand as Integer, result_type as Integer returns Integer:
    Note: OD-1 prefix embedding: single 104-byte allocation (96 B of live
    Note: layout + 8 B pad so unary records share one clone size with the
    Note: binary records that occupy the same 20002-20016 / 20400-20408
    Note: opcode bands). Header 0-71; extended fields: result_value_id at
    Note: 72, operand at 80, result_type at 88; offset 96 is zeroed pad.
    Let unop_size be 104
    Let unop be proc allocate from Layout with unop_size

    If unop is equal to 0:
        Return 0
    End If

    proc init_mir_node_header from MIR with unop, opcode
    proc memory_set_int64 from Layout with unop, 72, result_value_id
    proc memory_set_int64 from Layout with unop, 80, operand
    proc memory_set_int64 from Layout with unop, 88, result_type
    proc memory_set_int64 from Layout with unop, 96, 0

    proc set_mir_node_value_id from MIR with unop, result_value_id
    proc set_mir_node_operand from MIR with unop, 1, operand
    proc set_mir_node_type from MIR with unop, result_type

    Return unop
End Process

@Reasoning
OD-5 (approved 2026-06-10): SSA destruction and copy-insertion passes
(ssa.runa destruct_ssa_form, critical-edge phi resolution, register
coalescing pre-passes) need a dedicated value-copy instruction. Encoding
the copy as MIR_COPY (20008) rather than reusing MIR_ADD-with-zero or a
degenerate cast keeps copy elimination trivially recognizable by opcode
and lets the LIR builder lower it to a single register move. MIR_COPY is
a plain 72-byte MIRNode: the destination SSA id lives in the header
value_id slot (offset 8) and the source value id in operand1 (offset 16) —
no extended fields exist, so every generic header consumer (operand walk,
DCE, used-value collection) handles copies with zero special cases.
@End Reasoning

@Implementation
Creates a MIR_COPY instruction: dest_value_id = src_value_id. Allocates
the canonical 72-byte header via MIR.create_mir_node, then writes the
destination into value_id and the source into operand1. type_info is left
0 — a copy is type-transparent; consumers read the type of the source
value when register width matters. Returns 0 on allocation failure.
@End Implementation

Process called "create_mir_copy" that takes dest_value_id as Integer, src_value_id as Integer returns Integer:
    Let copy be proc create_mir_node from MIR with MIR.MIR_COPY

    If copy is equal to 0:
        Return 0
    End If

    proc set_mir_node_value_id from MIR with copy, dest_value_id
    proc set_mir_node_operand from MIR with copy, 1, src_value_id

    Return copy
End Process

@Reasoning
Memory loads need explicit offset support so struct-field access can lower
to a single load instead of (add base offset) + (load). The optimizer relies
on this to recognize identical loads via the (address, offset, type) key
used by load-CSE and PRE — without the offset baked into the node, two
field accesses on the same base would look distinct.

The is_volatile flag is preserved at MIR level (rather than only checked
by the frontend) because optimization passes have to honor it: a volatile
load is opaque to PRE, reordering, and dead-load elimination.
@End Reasoning

@Implementation
Creates a load instruction that reads from memory. The address must be a
pointer-typed value. Offset allows direct field access within structs.
@End Implementation

Process called "create_mir_load" that takes result_value_id as Integer, address_value_id as Integer, offset as Integer, result_type as Integer, is_volatile as Integer returns Integer:
    Note: OD-1 prefix embedding: single 112-byte allocation. Header 0-71;
    Note: extended fields: result_value_id at 72, address_value_id at 80,
    Note: offset at 88, result_type at 96, is_volatile at 104.
    Let load_size be 112
    Let load be proc allocate from Layout with load_size

    If load is equal to 0:
        Return 0
    End If

    proc init_mir_node_header from MIR with load, MIR.MIR_LOAD
    proc memory_set_int64 from Layout with load, 72, result_value_id
    proc memory_set_int64 from Layout with load, 80, address_value_id
    proc memory_set_int64 from Layout with load, 88, offset
    proc memory_set_int64 from Layout with load, 96, result_type
    proc memory_set_int64 from Layout with load, 104, is_volatile

    proc set_mir_node_value_id from MIR with load, result_value_id
    proc set_mir_node_operand from MIR with load, 1, address_value_id
    proc set_mir_node_type from MIR with load, result_type

    Return load
End Process

@Reasoning
Stores are side-effecting and do not produce SSA values, so result_value_id
is intentionally absent from the parameter list. Mem2reg and store-sinking
need to recognize this — a store is identified by its (address, offset)
pair and the value flowing into it, not by any result.

Like loads, stores carry their own volatility bit and explicit byte offset
to keep struct-field stores collapsed into a single instruction. The
ordering invariant (stores execute in program order within a block unless
proven independent by alias analysis) is the responsibility of the
optimizer, not this constructor.
@End Reasoning

@Implementation
Creates a store instruction that writes to memory. The address must be a
pointer-typed value. Store instructions have side effects but produce no value.
@End Implementation

Process called "create_mir_store" that takes address_value_id as Integer, value_to_store as Integer, offset as Integer, value_type as Integer, is_volatile as Integer returns Integer:
    Note: OD-1 prefix embedding: single 112-byte allocation. Header 0-71;
    Note: extended fields: address_value_id at 72, value_to_store at 80,
    Note: offset at 88, value_type at 96, is_volatile at 104.
    Let store_size be 112
    Let store be proc allocate from Layout with store_size

    If store is equal to 0:
        Return 0
    End If

    proc init_mir_node_header from MIR with store, MIR.MIR_STORE
    proc memory_set_int64 from Layout with store, 72, address_value_id
    proc memory_set_int64 from Layout with store, 80, value_to_store
    proc memory_set_int64 from Layout with store, 88, offset
    proc memory_set_int64 from Layout with store, 96, value_type
    proc memory_set_int64 from Layout with store, 104, is_volatile

    proc set_mir_node_operand from MIR with store, 1, address_value_id
    proc set_mir_node_operand from MIR with store, 2, value_to_store
    proc set_mir_node_type from MIR with store, value_type

    Return store
End Process

@Reasoning
Calls are the single most complex MIR node: they sit at the boundary of
the optimizer's reasoning (a call can clobber memory, define new SSA
values, transfer control). Encoding direct vs indirect via a flag (rather
than two distinct opcodes) lets the inliner test eligibility with a single
load and treat callee as either a string-table lookup or an SSA value.

is_tail_call is recorded at construction time because it is decided by the
frontend or by tail-call analysis early in the pipeline; lower passes only
ever clear the flag (when a tail call gets out-of-lined). Storing it here
preserves the decision across all transformations until the LIR layer
finally consumes it for jmp-vs-call selection.
@End Reasoning

@Implementation
Creates a function call instruction. For direct calls, callee is a function
name string pointer. For indirect calls, callee is a value ID (function pointer).
Arguments are passed as a list of value IDs.
@End Implementation

Process called "create_mir_call" that takes result_value_id as Integer, callee as Integer, arguments as Integer, argument_count as Integer, return_type as Integer, is_indirect as Integer, is_tail_call as Integer returns Integer:
    Note: OD-1 prefix embedding: single 144-byte allocation. Header 0-71;
    Note: extended fields: result_value_id at 72, callee at 80, arguments
    Note: List at 88, argument_count at 96, return_type at 104, is_indirect
    Note: at 112, is_tail_call at 120, callee_is_external at 128 (P2-D FFI),
    Note: callee_convention_id at 136 (P2-D FFI). mir_call_set_ffi_metadata
    Note: populates the last two when the callee resolves to a
    Note: HIRExternalDeclaration. For indirect calls (is_indirect = 1) the
    Note: opcode is MIR_CALL_INDIRECT so opcode-dispatch consumers route to
    Note: the indirect lowering arm without reading the flag.
    Let call_size be 144
    Let call be proc allocate from Layout with call_size

    If call is equal to 0:
        Return 0
    End If

    Let call_opcode be MIR.MIR_CALL
    If is_indirect is not equal to 0:
        Set call_opcode to MIR.MIR_CALL_INDIRECT
    End If

    proc init_mir_node_header from MIR with call, call_opcode
    proc memory_set_int64 from Layout with call, 72, result_value_id
    proc memory_set_int64 from Layout with call, 80, callee
    proc memory_set_int64 from Layout with call, 88, arguments
    proc memory_set_int64 from Layout with call, 96, argument_count
    proc memory_set_int64 from Layout with call, 104, return_type
    proc memory_set_int64 from Layout with call, 112, is_indirect
    proc memory_set_int64 from Layout with call, 120, is_tail_call

    Note: P2-D FFI metadata (defaults: not-external, c-convention).
    proc memory_set_int64 from Layout with call, 128, 0  Note: callee_is_external
    proc memory_set_int64 from Layout with call, 136, 0  Note: callee_convention_id (0 = c)

    proc set_mir_node_value_id from MIR with call, result_value_id
    proc set_mir_node_type from MIR with call, return_type

    Return call
End Process

@Reasoning
P2-D introduced FFI dispatch through MIR, requiring the LIR lowering pass
to know two facts that are not derivable from the MIR_CALL opcode alone:
whether the target is an external (non-Runa) symbol, and which calling
convention it expects. Storing those two facts directly on the call node
keeps the LIR lowering pass purely local (one node, one decision) and
avoids a side-table lookup keyed by callee.

The is_external bit is distinct from is_indirect because external calls
may still be direct (named C functions) and Runa-internal calls may still
be indirect (function-pointer dispatch). Separating the two preserves all
four combinations cleanly.
@End Reasoning

Process called "mir_call_set_ffi_metadata" that takes call_node as Integer, is_external as Integer, convention_id as Integer returns Integer:
    Note: Sets the P2-D FFI metadata on a MIR_CALL node so LIR lowering can
    Note: dispatch through the callee's calling convention (vs. the caller's).
    Note: Prefix-embedded offsets: callee_is_external at 128,
    Note: callee_convention_id at 136.
    If call_node is equal to 0:
        Return 0
    End If
    proc memory_set_int64 from Layout with call_node, 128, is_external
    proc memory_set_int64 from Layout with call_node, 136, convention_id
    Return 1
End Process

@Reasoning
LIR lowering needs cheap access to the FFI external bit when emitting the
call site. This getter is the read counterpart to mir_call_set_ffi_metadata
and is used by the calling-convention selector in machine_code lowering.
Returns 0 (the default) for null inputs, matching the construction default
so callers can treat "missing metadata" and "explicitly internal" identically.
@End Reasoning

Process called "mir_call_get_callee_is_external" that takes call_node as Integer returns Integer:
    If call_node is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with call_node, 128
End Process

@Reasoning
Returns the calling-convention ID for the call's target. Convention IDs are
small integers (0 = C, others follow the FFI convention registry). LIR
lowering uses this ID to pick the prologue/epilogue and argument-marshaling
sequence for the call site. A 0 return on null input maps to the C
convention, which is the safe default for direct Runa-to-Runa calls.
@End Reasoning

Process called "mir_call_get_callee_convention_id" that takes call_node as Integer returns Integer:
    If call_node is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with call_node, 136
End Process

@Reasoning
The conditional branch is the workhorse terminator for control flow. Both
target block IDs live in the same instruction so that CFG construction can
extract both successors with a single node visit (rather than scanning a
separate successor list). Branch weights live here, not in a side table,
because profile-guided optimization, jump-threading, and basic-block
ordering all consult them during transformations that may delete or rewire
the branch — keeping the weights co-located prevents stale weight references.
@End Reasoning

@Implementation
Creates a conditional branch instruction (terminator). Transfers control to
true_block_id if condition is true, false_block_id otherwise. Branch weights
provide optimization hints (0-100 scale, higher = more likely).
@End Implementation

Process called "create_mir_branch" that takes condition_value_id as Integer, true_block_id as Integer, false_block_id as Integer, branch_weight_true as Integer, branch_weight_false as Integer returns Integer:
    Note: OD-1 prefix embedding: single 112-byte allocation. Header 0-71;
    Note: extended fields: condition_value_id at 72, true_block_id at 80,
    Note: false_block_id at 88, branch_weight_true at 96,
    Note: branch_weight_false at 104.
    Let branch_size be 112
    Let branch be proc allocate from Layout with branch_size

    If branch is equal to 0:
        Return 0
    End If

    proc init_mir_node_header from MIR with branch, MIR.MIR_BRANCH
    proc memory_set_int64 from Layout with branch, 72, condition_value_id
    proc memory_set_int64 from Layout with branch, 80, true_block_id
    proc memory_set_int64 from Layout with branch, 88, false_block_id
    proc memory_set_int64 from Layout with branch, 96, branch_weight_true
    proc memory_set_int64 from Layout with branch, 104, branch_weight_false

    proc set_mir_node_operand from MIR with branch, 1, condition_value_id

    Return branch
End Process

@Reasoning
The unconditional jump is the simplest terminator and is deliberately kept
distinct from a degenerate branch (with one target). This separation lets
CFG simplification recognize "single-successor block" by opcode without
having to compare branch targets, and it lets the LIR backend select the
shortest jump encoding for the target distance without rejecting cases
where both branch targets happen to be equal.
@End Reasoning

@Implementation
Creates an unconditional jump instruction (terminator). Transfers control
directly to target_block_id with no condition check.
@End Implementation

Process called "create_mir_jump" that takes target_block_id as Integer returns Integer:
    Note: OD-1 prefix embedding: single 80-byte allocation. Header 0-71;
    Note: extended field: target_block_id at 72.
    Let jump_size be 80
    Let jump be proc allocate from Layout with jump_size

    If jump is equal to 0:
        Return 0
    End If

    proc init_mir_node_header from MIR with jump, MIR.MIR_JUMP
    proc memory_set_int64 from Layout with jump, 72, target_block_id

    Return jump
End Process

@Reasoning
Returns are terminators that exit the current function. Storing return_type
on the node (rather than re-deriving it from the function signature each
time) lets the verifier confirm return-type compatibility locally and lets
the LIR backend pick the correct return convention (e.g., float vs int
return register) without re-walking up to the enclosing function.

A zero value_id means "no value" (void return) — distinct from "value
zero", which would carry a non-zero result_value_id pointing at a
const-zero MIR node.
@End Reasoning

@Implementation
Creates a return instruction (terminator). Ends function execution and
optionally returns a value. For void functions, return_value_id should be 0.
@End Implementation

Process called "create_mir_return" that takes return_value_id as Integer, return_type as Integer returns Integer:
    Note: OD-1 prefix embedding: single 88-byte allocation. Header 0-71;
    Note: extended fields: return_value_id at 72, return_type at 80.
    Let return_size be 88
    Let ret be proc allocate from Layout with return_size

    If ret is equal to 0:
        Return 0
    End If

    proc init_mir_node_header from MIR with ret, MIR.MIR_RETURN
    proc memory_set_int64 from Layout with ret, 72, return_value_id
    proc memory_set_int64 from Layout with ret, 80, return_type

    proc set_mir_node_operand from MIR with ret, 1, return_value_id
    proc set_mir_node_type from MIR with ret, return_type

    Return ret
End Process

@Reasoning
Alloca is the only MIR node that materializes stack memory; lowering it
becomes "subtract from rsp" or "claim frame slot" at LIR. Encoding both
the element type and the count value on the node lets register allocation
and frame-layout passes compute exact stack-slot size without re-deriving
the element width from the type system. Storing alignment separately
(rather than inferring it from element_type) supports over-aligned
allocations needed by SIMD types and explicit alignment requests.
@End Reasoning

@Implementation
Creates a stack allocation instruction. Allocates count elements of element_type
on the stack with specified alignment. Returns a pointer value. Memory is
automatically freed when function returns.
@End Implementation

Process called "create_mir_alloca" that takes result_value_id as Integer, element_type as Integer, count_value_id as Integer, alignment as Integer returns Integer:
    Note: OD-1 prefix embedding: single 104-byte allocation. Header 0-71;
    Note: extended fields: result_value_id at 72, element_type at 80,
    Note: count_value_id at 88, alignment at 96.
    Let alloca_size be 104
    Let alloca be proc allocate from Layout with alloca_size

    If alloca is equal to 0:
        Return 0
    End If

    proc init_mir_node_header from MIR with alloca, MIR.MIR_ALLOCA
    proc memory_set_int64 from Layout with alloca, 72, result_value_id
    proc memory_set_int64 from Layout with alloca, 80, element_type
    proc memory_set_int64 from Layout with alloca, 88, count_value_id
    proc memory_set_int64 from Layout with alloca, 96, alignment

    proc set_mir_node_value_id from MIR with alloca, result_value_id
    proc set_mir_node_operand from MIR with alloca, 1, count_value_id
    proc set_mir_node_type from MIR with alloca, element_type

    Return alloca
End Process

@Reasoning
Constants are SSA values like any other, but they carry their literal
payload directly on the node instead of through a defining instruction.
Splitting numeric vs string payloads via is_string lets numeric constants
be packed into the constant_value field (avoiding a string-table lookup
in the hot path of constant folding) while strings still resolve to a
pointer into the deduplicated string table.

Constant folding and value numbering hash the (value_type, constant_value)
pair for numeric constants and (value_type, string_ptr) for strings — the
flag selects which key to use without polymorphic dispatch.
@End Reasoning

@Implementation
Creates a constant value instruction. For numeric constants, set is_string=0
and store value in constant_value. For string constants, set is_string=1
and store pointer in string_ptr.
@End Implementation

Process called "create_mir_const" that takes result_value_id as Integer, constant_value as Integer, value_type as Integer, is_string as Integer, string_ptr as Integer returns Integer:
    Note: OD-1 prefix embedding: single 112-byte allocation. Header 0-71;
    Note: extended fields: result_value_id at 72, constant_value at 80,
    Note: value_type at 88, is_string at 96, string_ptr at 104.
    Let const_size be 112
    Let const be proc allocate from Layout with const_size

    If const is equal to 0:
        Return 0
    End If

    proc init_mir_node_header from MIR with const, MIR.MIR_CONST
    proc memory_set_int64 from Layout with const, 72, result_value_id
    proc memory_set_int64 from Layout with const, 80, constant_value
    proc memory_set_int64 from Layout with const, 88, value_type
    proc memory_set_int64 from Layout with const, 96, is_string
    proc memory_set_int64 from Layout with const, 104, string_ptr

    proc set_mir_node_value_id from MIR with const, result_value_id
    proc set_mir_node_type from MIR with const, value_type

    Return const
End Process

@Reasoning
Switch is a multi-target terminator with dense or sparse case tables. The
node stores cases in a List rather than a fixed-size array because case
counts vary enormously across functions and over-provisioning would waste
memory on the common small-switch case. Default block is mandatory — the
parser inserts an implicit default of "unreachable" if the source did not
provide one, so MIR always has a fully defined dispatch.

Switch lowering can later pick jump-table, binary-search, or chained-
compare based on case density; the MIR shape stays the same regardless.
@End Reasoning

@Implementation
Creates a switch instruction for multi-way branching. Cases are added after
creation by appending (case_value, block_id) pairs to the cases list.
Default block handles all unmatched values.
@End Implementation

Process called "create_mir_switch" that takes value_id as Integer, default_block_id as Integer, value_type as Integer returns Integer:
    Note: OD-1 prefix embedding: single 112-byte allocation. Header 0-71;
    Note: extended fields: value_id at 72, cases List at 80, case_count at
    Note: 88, default_block_id at 96, value_type at 104. Cases are a flat
    Note: pair List: case values in even slots, block IDs in odd slots.
    Let switch_size be 112
    Let switch be proc allocate from Layout with switch_size

    If switch is equal to 0:
        Return 0
    End If

    proc init_mir_node_header from MIR with switch, MIR.MIR_SWITCH
    proc memory_set_int64 from Layout with switch, 72, value_id
    proc memory_set_int64 from Layout with switch, 96, default_block_id
    proc memory_set_int64 from Layout with switch, 104, value_type

    Let cases be proc create from List
    proc memory_set_int64 from Layout with switch, 80, cases
    proc memory_set_int64 from Layout with switch, 88, 0

    proc set_mir_node_operand from MIR with switch, 1, value_id

    Return switch
End Process

@Reasoning
Mirrors add_phi_incoming but for switch dispatch: each case is two list
elements (value, block) appended together. The redundant case_count at
offset 24 is incremented in lockstep so that switch lowering can size its
jump-table or binary-search structure without dividing list length by 2.
This proc preserves insertion order, which matters for source-faithful
debugging and for some lowering heuristics that prefer the first matching
case to be the densest cluster.
@End Reasoning

@Implementation
Adds a case to a switch instruction. Case value is a compile-time constant
that maps to the target block. Switch will transfer control to the target
block if the switched value is equal to the case value.
@End Implementation

Process called "add_switch_case" that takes switch as Integer, case_value as Integer, block_id as Integer returns Integer:
    If switch is equal to 0:
        Return 0
    End If

    Note: Prefix-embedded layout: cases List at 80, case_count at 88.
    Let cases be proc memory_get_int64 from Layout with switch, 80
    proc append from List with cases, case_value
    proc append from List with cases, block_id

    Let case_count be proc memory_get_int64 from Layout with switch, 88
    Set case_count to case_count plus 1
    proc memory_set_int64 from Layout with switch, 88, case_count

    Return 1
End Process

@Reasoning
Atomic operations need a dedicated node because they are opaque to most
optimizations: they cannot be reordered, hoisted out of loops without
proof, or duplicated across paths. Recording memory_order on the node
lets each atomic carry its own ordering constraint instead of forcing
the whole basic block into the strongest order.

The expected_value_id slot is unused for plain atomic add/xor and only
populated for compare-and-swap; storing 0 there for non-CAS ops keeps the
layout uniform so generic walkers can treat all atomics with one shape.
@End Reasoning

@Implementation
Creates an atomic operation instruction with specified memory ordering.
Memory ordering: 0=relaxed, 1=acquire, 2=release, 3=acq_rel, 4=seq_cst.
For CAS operations, expected_value_id specifies the comparison value.
@End Implementation

Process called "create_mir_atomic_op" that takes opcode as Integer, result_value_id as Integer, address_value_id as Integer, operand_value_id as Integer, expected_value_id as Integer, memory_order as Integer, result_type as Integer returns Integer:
    Note: OD-1 prefix embedding: single 120-byte allocation. Header 0-71;
    Note: extended fields: result_value_id at 72, address_value_id at 80,
    Note: operand_value_id at 88, expected_value_id at 96, memory_order at
    Note: 104, result_type at 112.
    Let atomic_size be 120
    Let atomic be proc allocate from Layout with atomic_size

    If atomic is equal to 0:
        Return 0
    End If

    proc init_mir_node_header from MIR with atomic, opcode
    proc memory_set_int64 from Layout with atomic, 72, result_value_id
    proc memory_set_int64 from Layout with atomic, 80, address_value_id
    proc memory_set_int64 from Layout with atomic, 88, operand_value_id
    proc memory_set_int64 from Layout with atomic, 96, expected_value_id
    proc memory_set_int64 from Layout with atomic, 104, memory_order
    proc memory_set_int64 from Layout with atomic, 112, result_type

    proc set_mir_node_value_id from MIR with atomic, result_value_id
    proc set_mir_node_operand from MIR with atomic, 1, address_value_id
    proc set_mir_node_operand from MIR with atomic, 2, operand_value_id
    proc set_mir_node_type from MIR with atomic, result_type

    Return atomic
End Process

@Reasoning
Fences are pure ordering barriers — no memory traffic, no SSA result.
They share the atomic-op node layout (56 bytes) so that the same accessor
machinery and verifier code can inspect either. The unused slots are
zero-initialized rather than left untouched so that hash-based passes
(GVN, value numbering) get deterministic content when they ever happen
to canonicalize a fence node.
@End Reasoning

@Implementation
Creates a memory fence instruction. Fences ensure memory ordering without
performing loads or stores. Memory ordering determines synchronization
guarantees across threads.
@End Implementation

Process called "create_mir_fence" that takes memory_order as Integer returns Integer:
    Note: OD-1 prefix embedding: single 120-byte allocation sharing the
    Note: MIRAtomicOp layout. Header 0-71; extended fields zeroed except
    Note: memory_order at 104.
    Let fence_size be 120
    Let fence be proc allocate from Layout with fence_size

    If fence is equal to 0:
        Return 0
    End If

    proc init_mir_node_header from MIR with fence, MIR.MIR_FENCE
    proc memory_set_int64 from Layout with fence, 72, 0
    proc memory_set_int64 from Layout with fence, 80, 0
    proc memory_set_int64 from Layout with fence, 88, 0
    proc memory_set_int64 from Layout with fence, 96, 0
    proc memory_set_int64 from Layout with fence, 104, memory_order
    proc memory_set_int64 from Layout with fence, 112, 0

    Return fence
End Process

@Reasoning
Vector ops are scalarizable but expensive to scalarize, so MIR carries
them in native vector form down to LIR where the machine_code backend
picks SIMD instructions matching the architecture. Storing vector_width
and element_type separately (rather than a composite "VectorType") lets
the backend match against either dimension cheaply — e.g., "any 4-wide"
or "any f32" without unpacking.

The mask_data field is non-zero only for shuffle/blend opcodes; it stores
a packed byte mask describing the lane permutation. Other opcodes leave
it at 0 so the node layout remains uniform.
@End Reasoning

@Implementation
Creates a vector operation instruction. Vector width specifies number of
elements (2, 4, 8, 16, etc.). Element type specifies the type of each
element. Mask data is used for shuffle operations.
@End Implementation

Process called "create_mir_vector_op" that takes opcode as Integer, result_value_id as Integer, operand1 as Integer, operand2 as Integer, operand3 as Integer, vector_width as Integer, element_type as Integer, mask_data as Integer returns Integer:
    Note: OD-1 prefix embedding: single 128-byte allocation. Header 0-71;
    Note: extended fields: result_value_id at 72, operand1 at 80, operand2
    Note: at 88, operand3 at 96, vector_width at 104, element_type at 112,
    Note: mask_data at 120.
    Let vector_size be 128
    Let vector be proc allocate from Layout with vector_size

    If vector is equal to 0:
        Return 0
    End If

    proc init_mir_node_header from MIR with vector, opcode
    proc memory_set_int64 from Layout with vector, 72, result_value_id
    proc memory_set_int64 from Layout with vector, 80, operand1
    proc memory_set_int64 from Layout with vector, 88, operand2
    proc memory_set_int64 from Layout with vector, 96, operand3
    proc memory_set_int64 from Layout with vector, 104, vector_width
    proc memory_set_int64 from Layout with vector, 112, element_type
    proc memory_set_int64 from Layout with vector, 120, mask_data

    proc set_mir_node_value_id from MIR with vector, result_value_id
    proc set_mir_node_operand from MIR with vector, 1, operand1
    proc set_mir_node_operand from MIR with vector, 2, operand2
    proc set_mir_node_operand from MIR with vector, 3, operand3
    proc set_mir_node_type from MIR with vector, element_type

    Return vector
End Process

@Reasoning
Invoke is the exception-aware version of MIR_CALL: it terminates the
basic block and has two successor edges (normal return vs unwind). CFG
construction must see both edges to keep the dominance tree correct in
the presence of exceptional control flow, so they are stored directly
on the invoke node rather than derived from a side table of try/catch
scopes. Treating invoke as a terminator (rather than a call followed by
a branch) ensures exception edges cannot be lost by mid-block optimizations.
@End Reasoning

@Implementation
Creates an invoke instruction that calls a function with exception handling.
Normal block is taken on successful return, exception block is taken if
an exception is thrown during the call.
@End Implementation

Process called "create_mir_invoke" that takes result_value_id as Integer, callee as Integer, arguments as Integer, argument_count as Integer, return_type as Integer, normal_block_id as Integer, exception_block_id as Integer, is_indirect as Integer returns Integer:
    Note: OD-1 prefix embedding: single 136-byte allocation. Header 0-71;
    Note: extended fields: result_value_id at 72, callee at 80, arguments
    Note: List at 88, argument_count at 96, return_type at 104,
    Note: normal_block_id at 112, exception_block_id at 120, is_indirect
    Note: at 128.
    Let invoke_size be 136
    Let invoke be proc allocate from Layout with invoke_size

    If invoke is equal to 0:
        Return 0
    End If

    proc init_mir_node_header from MIR with invoke, MIR.MIR_INVOKE
    proc memory_set_int64 from Layout with invoke, 72, result_value_id
    proc memory_set_int64 from Layout with invoke, 80, callee
    proc memory_set_int64 from Layout with invoke, 88, arguments
    proc memory_set_int64 from Layout with invoke, 96, argument_count
    proc memory_set_int64 from Layout with invoke, 104, return_type
    proc memory_set_int64 from Layout with invoke, 112, normal_block_id
    proc memory_set_int64 from Layout with invoke, 120, exception_block_id
    proc memory_set_int64 from Layout with invoke, 128, is_indirect

    proc set_mir_node_value_id from MIR with invoke, result_value_id
    proc set_mir_node_type from MIR with invoke, return_type

    Return invoke
End Process

@Reasoning
Landing pads are the catch-block prologues for exception dispatch. They
must be SSA values because subsequent code in the handler reads the
exception object through the landing pad's result_value_id. The catch
clauses list grows by add_catch_clause and matches what the unwinder
will inspect at runtime to pick a handler.

is_cleanup distinguishes cleanup-only pads (run finally/destructors then
re-raise) from match-arm pads (test the thrown type, dispatch on match).
The runtime needs the bit to decide whether to continue unwinding.
@End Reasoning

@Implementation
Creates a landing pad instruction for exception handling. Landing pads are
placed at the beginning of exception handler blocks. They receive the
exception object and determine which catch clause to execute.
@End Implementation

Process called "create_mir_landingpad" that takes result_value_id as Integer, exception_type as Integer, cleanup_block_id as Integer, is_cleanup as Integer returns Integer:
    Note: OD-1 prefix embedding: single 120-byte allocation. Header 0-71;
    Note: extended fields: result_value_id at 72, exception_type at 80,
    Note: catch_clauses List at 88, clause_count at 96, cleanup_block_id at
    Note: 104, is_cleanup at 112.
    Let landingpad_size be 120
    Let landingpad be proc allocate from Layout with landingpad_size

    If landingpad is equal to 0:
        Return 0
    End If

    proc init_mir_node_header from MIR with landingpad, MIR.MIR_LANDINGPAD
    proc memory_set_int64 from Layout with landingpad, 72, result_value_id
    proc memory_set_int64 from Layout with landingpad, 80, exception_type
    proc memory_set_int64 from Layout with landingpad, 104, cleanup_block_id
    proc memory_set_int64 from Layout with landingpad, 112, is_cleanup

    Let catch_clauses be proc create from List
    proc memory_set_int64 from Layout with landingpad, 88, catch_clauses
    proc memory_set_int64 from Layout with landingpad, 96, 0

    proc set_mir_node_value_id from MIR with landingpad, result_value_id
    proc set_mir_node_type from MIR with landingpad, exception_type

    Return landingpad
End Process

@Reasoning
Catch clauses share the (type, block) two-element encoding used by phi
incoming pairs and switch cases — keeping the same shape lets all three
benefit from the same List utilities and verifier checks. Append order
matters: the runtime tests clauses in insertion order, so the parser
preserves source order when emitting them.
@End Reasoning

@Implementation
Adds a catch clause to a landing pad. Type ID specifies which exception
type this clause catches. Block ID is the target handler block.
@End Implementation

Process called "add_catch_clause" that takes landingpad as Integer, type_id as Integer, block_id as Integer returns Integer:
    If landingpad is equal to 0:
        Return 0
    End If

    Note: Prefix-embedded layout: catch_clauses at 88, clause_count at 96.
    Let catch_clauses be proc memory_get_int64 from Layout with landingpad, 88
    proc append from List with catch_clauses, type_id
    proc append from List with catch_clauses, block_id

    Let clause_count be proc memory_get_int64 from Layout with landingpad, 96
    Set clause_count to clause_count plus 1
    proc memory_set_int64 from Layout with landingpad, 96, clause_count

    Return 1
End Process

@Reasoning
Throw is a terminator with no static successor — control leaves the
current function (and possibly stack frames above it) by stack unwinding.
The verifier treats it specially: a block ending in MIR_THROW has zero
CFG-visible successors but is not unreachable. The exception_type field
gives the unwinder enough information to start matching against catch
clauses in enclosing landing pads.
@End Reasoning

@Implementation
Creates a throw instruction that raises an exception. The exception value
is typically an object containing error information. Control transfers to
the nearest enclosing exception handler.
@End Implementation

Process called "create_mir_throw" that takes exception_value_id as Integer, exception_type as Integer returns Integer:
    Note: OD-1 prefix embedding: single 88-byte allocation. Header 0-71;
    Note: extended fields: exception_value_id at 72, exception_type at 80.
    Let throw_size be 88
    Let throw be proc allocate from Layout with throw_size

    If throw is equal to 0:
        Return 0
    End If

    proc init_mir_node_header from MIR with throw, MIR.MIR_THROW
    proc memory_set_int64 from Layout with throw, 72, exception_value_id
    proc memory_set_int64 from Layout with throw, 80, exception_type

    proc set_mir_node_operand from MIR with throw, 1, exception_value_id
    proc set_mir_node_type from MIR with throw, exception_type

    Return throw
End Process

@Reasoning
Debug values bridge SSA back to source semantics so debuggers and crash
dumps can show "this value is variable foo declared at file:line:col".
They are pseudo-instructions: they don't compute anything, they only
annotate. Keeping them in the instruction stream (rather than a parallel
side table) means they ride along through every optimization without
needing separate update logic — if an optimization moves the underlying
value, the debug node moves with it because it sits in the same block.
@End Reasoning

@Implementation
Creates a debug value annotation that associates an SSA value with a source
variable name and location. Essential for debugger variable inspection.
@End Implementation

Process called "create_mir_debug_value" that takes value_id as Integer, variable_name as Integer, source_file as Integer, source_line as Integer, source_column as Integer, scope_id as Integer returns Integer:
    Note: OD-1 prefix embedding: single 120-byte allocation. Header 0-71;
    Note: extended fields: value_id at 72, variable_name at 80, source_file
    Note: at 88, source_line at 96, source_column at 104, scope_id at 112.
    Let debug_size be 120
    Let debug be proc allocate from Layout with debug_size

    If debug is equal to 0:
        Return 0
    End If

    proc init_mir_node_header from MIR with debug, MIR.MIR_DEBUG_VALUE
    proc memory_set_int64 from Layout with debug, 72, value_id
    proc memory_set_int64 from Layout with debug, 80, variable_name
    proc memory_set_int64 from Layout with debug, 88, source_file
    proc memory_set_int64 from Layout with debug, 96, source_line
    proc memory_set_int64 from Layout with debug, 104, source_column
    proc memory_set_int64 from Layout with debug, 112, scope_id

    proc set_mir_node_operand from MIR with debug, 1, value_id

    Return debug
End Process

@Reasoning
Debug locations carry the source position attached to one MIR instruction.
They are the input to stack-trace generation and to source-line stepping
in a debugger. The inlined_at field is critical for correct backtraces in
inlined code: it stores the call site that triggered the inlining, so
the debugger can show both the inlined source location and the call site
from which it was inlined (giving a proper "inlined from foo:42" frame).
@End Reasoning

@Implementation
Creates a debug location marker that annotates an instruction with its
source location. Enables accurate stack traces and source-level debugging.
@End Implementation

Process called "create_mir_debug_location" that takes source_file as Integer, source_line as Integer, source_column as Integer, scope_id as Integer, inlined_at as Integer returns Integer:
    Note: OD-1 prefix embedding: single 112-byte allocation. Header 0-71;
    Note: extended fields: source_file at 72, source_line at 80,
    Note: source_column at 88, scope_id at 96, inlined_at at 104.
    Let loc_size be 112
    Let loc be proc allocate from Layout with loc_size

    If loc is equal to 0:
        Return 0
    End If

    proc init_mir_node_header from MIR with loc, MIR.MIR_DEBUG_LOCATION
    proc memory_set_int64 from Layout with loc, 72, source_file
    proc memory_set_int64 from Layout with loc, 80, source_line
    proc memory_set_int64 from Layout with loc, 88, source_column
    proc memory_set_int64 from Layout with loc, 96, scope_id
    proc memory_set_int64 from Layout with loc, 104, inlined_at

    Return loc
End Process

Note: ============================================================================
Note: Specialized Node Access Functions
Note: ============================================================================

@Reasoning
Returns the MIR opcode of an instruction. Called from MIR optimization passes
(inlining cost analysis, dead code elimination, common subexpression elimination)
that need to dispatch on opcode without depending on the specialized node accessors
in mir.runa. Centralizing opcode access here lets MIRNodes serve as the canonical
access boundary for all MIR instruction introspection used by middle-end passes.

The opcode is stored at byte offset 0 of every MIRNode (verified by the MIRNode
type definition in mir.runa: opcode is the first field, occupying bytes 0-7).
Specialized nodes (MIRPhiNode, MIRBinaryOp, etc.) wrap a base MIRNode pointer
at offset 0 — but callers that hold a specialized-node pointer should use
MIR.get_mir_node_opcode on the base pointer they extracted, not this proc.
This proc expects a MIRNode pointer (the kind stored in basic-block instruction
lists by mir.add_instruction_to_block).
@End Reasoning

@Implementation
Performs a single 8-byte load at offset 0 of the instruction pointer. Defends
against null inputs by returning 0 (matching the sentinel used by every other
accessor in this file and by MIR.get_mir_node_opcode in mir.runa). No allocation,
no dispatch, no auxiliary lookups — purely a typed memory read through the
Layout primitive so allocator-aware semantics (Tier 3 OWNED HEAP) are honored.
@End Implementation

@Performance_Hints
Hot path: called once per instruction in inlining cost analysis (inlining.runa
line 405) and once per instruction during inlining transform iteration (line 637).
For a function with N instructions visited by the inliner, this runs O(N) times.
Single int64 load is a candidate for inlining by the backend; the null check
adds one compare-and-branch but is predicted not-taken in production input.
No heap traffic, no syscalls, no string formatting — strictly arithmetic.
@End Performance_Hints

@Security_Scope
Read-only operation on caller-provided pointer. Validates non-null before
dereferencing. Does not modify the instruction, does not expose internal
state beyond the opcode value, does not propagate caller pointers. The
opcode value returned is a numeric constant in the documented MIR opcode
range (20000-20999) — leaking it carries no security implication beyond
revealing the structural shape of the MIR, which is intentional public API.
No untrusted-input path: callers in the optimization pipeline operate on
MIR produced by the compiler itself, not on user-controlled byte streams.
@End Security_Scope

Process called "get_opcode" that takes inst_ptr as Integer returns Integer:
    If inst_ptr is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with inst_ptr, 0
End Process

@Reasoning
The phi pair-count field (prefix-embedded offset 88) is the cheap source
of truth for how many predecessor edges this phi merges. CFG dominance
verification and copy-propagation read this constantly while scanning a
function's phi nodes, so making it a single int64 load (rather than
computing list-length / 2) keeps it O(1) and branch-free.
@End Reasoning

Process called "get_phi_incoming_count" that takes phi as Integer returns Integer:
    If phi is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with phi, 88
End Process

@Reasoning
Decodes the value half of the Nth phi incoming pair. Pairs are stored as
two consecutive list elements (block at 2N, value at 2N+1) so the index
math compresses one multiply and one add. The SSA renamer and copy
propagation both need value lookup keyed by pair index.
@End Reasoning

Process called "get_phi_incoming_value" that takes phi as Integer, index as Integer returns Integer:
    If phi is equal to 0:
        Return 0
    End If

    Let incoming_pairs be proc memory_get_int64 from Layout with phi, 80
    Let pair_index be index multiplied by 2 plus 1
    Return proc get from List with incoming_pairs, pair_index
End Process

@Reasoning
Block side of the phi pair (slot 2N in the prefix-embedded pair List at
offset 80). CFG-aware passes
(loop simplification, edge splitting) iterate pairs by predecessor, so
having direct block lookup avoids constructing a temporary tuple just
to read one half.
@End Reasoning

Process called "get_phi_incoming_block" that takes phi as Integer, index as Integer returns Integer:
    If phi is equal to 0:
        Return 0
    End If

    Let incoming_pairs be proc memory_get_int64 from Layout with phi, 80
    Let pair_index be index multiplied by 2
    Return proc get from List with incoming_pairs, pair_index
End Process

@Reasoning
Write-side companion of get_phi_incoming_value (Wave 4 addition under the
MIR reconciliation plan's missing-accessor authority). Trivial-phi
elimination and copy propagation must redirect a phi's incoming VALUE to
a replacement SSA id without disturbing the paired predecessor block.
Without this mutator those passes would have to poke the interleaved
pair List at raw offsets, which the canonical contract forbids.
@End Reasoning

@Implementation
Writes new_value_id into the value half (slot 2*index + 1) of the
prefix-embedded incoming-pairs List at offset 80, leaving the block half
(slot 2*index) untouched. Bounds are enforced against incoming_count at
offset 88. Returns 1 on a successful write, 0 for a null phi, a missing
pair List, or an out-of-range index.
@End Implementation

Process called "set_phi_incoming_value" that takes phi as Integer, index as Integer, new_value_id as Integer returns Integer:
    If phi is equal to 0:
        Return 0
    End If

    If index is less than 0:
        Return 0
    End If

    Let incoming_count be proc memory_get_int64 from Layout with phi, 88
    If index is greater than or equal to incoming_count:
        Return 0
    End If

    Let incoming_pairs be proc memory_get_int64 from Layout with phi, 80
    If incoming_pairs is equal to 0:
        Return 0
    End If

    Let pair_index be index multiplied by 2 plus 1
    proc set from List with incoming_pairs, pair_index, new_value_id
    Return 1
End Process

@Reasoning
Coherent value-use rewrite for prefix-embedded records (Wave 4 addition
under the MIR reconciliation plan's missing-accessor authority). Under
OD-1, an instruction's value-id uses are carried twice: in the canonical
header operand slots (16/24/32) and in the opcode-specific extended
fields at 72+ that the constructors duplicate them into. A rename or
replacement that updates only the header would desynchronize every
extended-payload reader (lir_builder lowering, target updaters), and one
that updates only the extended field would desynchronize every generic
header consumer. This mutator is the single write point that keeps both
views coherent; SSA renaming and trivial-phi elimination route every
use rewrite through it.
@End Reasoning

@Implementation
Writes new_value_id into the 1-based header operand slot via
MIR.set_mir_node_operand, then mirrors the write into the extended-record
slot that the constructor for this opcode family duplicated the use into
(per the as-built Part 1.1 layout table):
- binary band 20002-20006 / 20010-20016 minus NOT, and comparisons
  20100-20105 (create_mir_binary_op): slot 1 maps to left at offset 80,
  slot 2 maps to right at offset 88
- unary NEG 20007 / NOT 20013 and the cast band 20400-20408
  (create_mir_unary_op): slot 1 maps to operand at offset 80
- LOAD 20200: slot 1 maps to address at offset 80
- STORE 20201: slot 1 maps to address at offset 72, slot 2 maps to the
  stored value at offset 80
- ALLOCA 20202: slot 1 maps to count at offset 88
- BRANCH 20301 / RETURN 20303 / SWITCH 20306 / THROW 20703: slot 1 maps
  to the record's leading value field at offset 72
- atomic band 20500-20510 (create_mir_atomic_op): slot 1 maps to address
  at offset 80, slot 2 maps to operand at offset 88
- vector band 20600-20622 (create_mir_vector_op): slots 1/2/3 map to
  offsets 80/88/96
- MIR_COPY 20008 and every family whose constructor mirrors nothing
  (CONST, JUMP, CALL/CALL_INDIRECT whose uses live in the argument List):
  header-only write.
Returns 1 on a successful header write (the mirror write shares its
fate), 0 for null instruction or out-of-range slot.
@End Implementation

Process called "set_mir_node_used_value" that takes inst_ptr as Integer, operand_index as Integer, new_value_id as Integer returns Integer:
    If inst_ptr is equal to 0:
        Return 0
    End If

    Note: Header slot first (canonical use record, 1-based index)
    Let header_written be proc set_mir_node_operand from MIR with inst_ptr, operand_index, new_value_id
    If header_written is equal to 0:
        Return 0
    End If

    Let opcode be proc memory_get_int64 from Layout with inst_ptr, 0

    Note: Binary band (ADD..MOD, AND..SAR minus NOT) and comparisons:
    Note: left at 80, right at 88
    Let is_binary be 0
    If opcode is greater than or equal to 20002:
        If opcode is less than or equal to 20006:
            Set is_binary to 1
        End If
    End If
    If opcode is greater than or equal to 20010:
        If opcode is less than or equal to 20016:
            If opcode is not equal to 20013:
                Set is_binary to 1
            End If
        End If
    End If
    If opcode is greater than or equal to 20100:
        If opcode is less than or equal to 20105:
            Set is_binary to 1
        End If
    End If
    If is_binary is equal to 1:
        If operand_index is equal to 1:
            proc memory_set_int64 from Layout with inst_ptr, 80, new_value_id
        End If
        If operand_index is equal to 2:
            proc memory_set_int64 from Layout with inst_ptr, 88, new_value_id
        End If
        Return 1
    End If

    Note: Unary ops (NEG, NOT) and the cast band: operand at 80
    Let is_unary be 0
    If opcode is equal to 20007:
        Set is_unary to 1
    End If
    If opcode is equal to 20013:
        Set is_unary to 1
    End If
    If opcode is greater than or equal to 20400:
        If opcode is less than or equal to 20408:
            Set is_unary to 1
        End If
    End If
    If is_unary is equal to 1:
        If operand_index is equal to 1:
            proc memory_set_int64 from Layout with inst_ptr, 80, new_value_id
        End If
        Return 1
    End If

    Note: LOAD: address at 80
    If opcode is equal to 20200:
        If operand_index is equal to 1:
            proc memory_set_int64 from Layout with inst_ptr, 80, new_value_id
        End If
        Return 1
    End If

    Note: STORE: address at 72, value at 80
    If opcode is equal to 20201:
        If operand_index is equal to 1:
            proc memory_set_int64 from Layout with inst_ptr, 72, new_value_id
        End If
        If operand_index is equal to 2:
            proc memory_set_int64 from Layout with inst_ptr, 80, new_value_id
        End If
        Return 1
    End If

    Note: ALLOCA: count at 88
    If opcode is equal to 20202:
        If operand_index is equal to 1:
            proc memory_set_int64 from Layout with inst_ptr, 88, new_value_id
        End If
        Return 1
    End If

    Note: BRANCH condition / RETURN value / SWITCH value / THROW value: at 72
    Let leading_value_record be 0
    If opcode is equal to 20301:
        Set leading_value_record to 1
    End If
    If opcode is equal to 20303:
        Set leading_value_record to 1
    End If
    If opcode is equal to 20306:
        Set leading_value_record to 1
    End If
    If opcode is equal to 20703:
        Set leading_value_record to 1
    End If
    If leading_value_record is equal to 1:
        If operand_index is equal to 1:
            proc memory_set_int64 from Layout with inst_ptr, 72, new_value_id
        End If
        Return 1
    End If

    Note: Atomic band: address at 80, operand at 88
    If opcode is greater than or equal to 20500:
        If opcode is less than or equal to 20510:
            If operand_index is equal to 1:
                proc memory_set_int64 from Layout with inst_ptr, 80, new_value_id
            End If
            If operand_index is equal to 2:
                proc memory_set_int64 from Layout with inst_ptr, 88, new_value_id
            End If
            Return 1
        End If
    End If

    Note: Vector band: op1 at 80, op2 at 88, op3 at 96
    If opcode is greater than or equal to 20600:
        If opcode is less than or equal to 20622:
            If operand_index is equal to 1:
                proc memory_set_int64 from Layout with inst_ptr, 80, new_value_id
            End If
            If operand_index is equal to 2:
                proc memory_set_int64 from Layout with inst_ptr, 88, new_value_id
            End If
            If operand_index is equal to 3:
                proc memory_set_int64 from Layout with inst_ptr, 96, new_value_id
            End If
            Return 1
        End If
    End If

    Note: Header-only families (COPY, CONST, JUMP, CALL argument-List users)
    Return 1
End Process

@Reasoning
Left operand of a binary op (prefix-embedded offset 80). Constant folding,
algebraic identity rules, and reassociation all read both sides, so
giving each side a separate direct accessor avoids exposing the binop's
internal layout to optimization passes.
@End Reasoning

Process called "get_binary_op_left" that takes binop as Integer returns Integer:
    If binop is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with binop, 80
End Process

@Reasoning
Right operand (prefix-embedded offset 88). For non-commutative ops (sub, div, shift)
left-vs-right ordering is semantic and must be preserved verbatim by
every transform — distinct accessors make that ordering explicit at the
call site instead of relying on positional convention.
@End Reasoning

Process called "get_binary_op_right" that takes binop as Integer returns Integer:
    If binop is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with binop, 88
End Process

@Reasoning
Sole operand of a unary op (prefix-embedded offset 80). The accessor exists so that
unary-specific passes (cast-folding, neg-of-neg removal) can address the
operand without knowing the underlying layout. Returning 0 on a null
input matches the rest of the accessor family.
@End Reasoning

Process called "get_unary_op_operand" that takes unop as Integer returns Integer:
    If unop is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with unop, 80
End Process

@Reasoning
Address operand of a load (prefix-embedded offset 80). Alias analysis and
load-CSE both partition loads by address; pulling the address through a
named accessor isolates this layout decision from those passes so future
layout changes (e.g., switching to inline immediate addresses) require
edits only here.
@End Reasoning

Process called "get_load_address" that takes load as Integer returns Integer:
    If load is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with load, 80
End Process

@Reasoning
Byte offset added to the address (prefix-embedded offset 88). Two loads with the same
base address but different offsets are distinct memory operations and
load-CSE must not merge them — exposing this field independently from
the address lets passes treat (address, offset) as a single key.
@End Reasoning

Process called "get_load_offset" that takes load as Integer returns Integer:
    If load is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with load, 88
End Process

@Reasoning
Result type of the load (prefix-embedded offset 96). Required for type-aware optimization
(e.g., load-store forwarding only fires when the store's value type
matches the load's result type) and for LIR lowering, which needs the
type to choose the right load opcode (movb/movw/movl/movq on x86).
@End Reasoning

Process called "get_load_type" that takes load as Integer returns Integer:
    If load is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with load, 96
End Process

@Reasoning
Address operand of a store (prefix-embedded offset 72). Stores share the
alias-analysis and load-store-forwarding machinery with loads, so they
expose their target address through the same accessor pattern. Offset 72
(not 80 like loads) reflects the fact that stores carry no result value,
freeing the first extended slot for the address itself.
@End Reasoning

Process called "get_store_address" that takes store as Integer returns Integer:
    If store is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with store, 72
End Process

@Reasoning
Value being stored (prefix-embedded offset 80). Store-to-load forwarding rewrites
subsequent loads of the same address to use this value directly,
so reading it through a named accessor lets that pass remain
oblivious to the store's underlying byte layout.
@End Reasoning

Process called "get_store_value" that takes store as Integer returns Integer:
    If store is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with store, 80
End Process

@Reasoning
Byte offset (prefix-embedded offset 88) added to the store address. Two stores to the
same address with different offsets are distinct (e.g., field stores
inside one struct); exposing offset independently lets passes treat
(address, offset) as the alias-disambiguation key.
@End Reasoning

Process called "get_store_offset" that takes store as Integer returns Integer:
    If store is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with store, 88
End Process

@Reasoning
Callee field (prefix-embedded offset 80). Interpretation depends on is_call_indirect:
for direct calls, this is a string-table pointer to the function name;
for indirect calls, it is an SSA value id. Inliner and devirtualization
passes both call this to decide whether they can statically resolve
the target.
@End Reasoning

Process called "get_call_callee" that takes call as Integer returns Integer:
    If call is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with call, 80
End Process

@Reasoning
Arguments list (prefix-embedded offset 88) holding SSA value IDs in calling order. The
list itself is allocated separately from the call node so it can grow
without resizing the node; passes that mutate calls (e.g., argument
promotion, dead-arg elimination) modify the list in place.
@End Reasoning

Process called "get_call_arguments" that takes call as Integer returns Integer:
    If call is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with call, 88
End Process

@Reasoning
Cached argument count (prefix-embedded offset 96) — mirrors the arguments list length
but is stored separately so verifier checks and quick-iteration passes
do not pay the cost of a List-length call. Maintained in lockstep by
the constructor and any pass that mutates the argument list.
@End Reasoning

Process called "get_call_argument_count" that takes call as Integer returns Integer:
    If call is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with call, 96
End Process

@Reasoning
Return type (prefix-embedded offset 104). LIR lowering uses this to pick the correct
return-register handling (gpr vs xmm) at the call site. Recording the
type on the call node, rather than re-resolving from the callee
signature, lets passes mutate the call without touching a global
symbol table.
@End Reasoning

Process called "get_call_return_type" that takes call as Integer returns Integer:
    If call is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with call, 104
End Process

@Reasoning
Indirect-call flag (prefix-embedded offset 112). A non-zero result indicates the callee
field is an SSA value id rather than a function name. The flag is set
once at construction and only cleared by devirtualization when it has
proven a single target.
@End Reasoning

Process called "is_call_indirect" that takes call as Integer returns Integer:
    If call is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with call, 112
End Process

@Reasoning
Tail-call flag (prefix-embedded offset 120). Tells the LIR backend to use a tail-call
sequence (jmp instead of call) when this flag is set, freeing the
caller's stack frame before transferring control. Tail-call analysis
sets this; out-of-lining passes that destroy the tail position clear
it again.
@End Reasoning

Process called "is_tail_call" that takes call as Integer returns Integer:
    If call is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with call, 120
End Process

@Reasoning
Condition value (prefix-embedded offset 72) consumed by a branch terminator. Trivial-
branch elimination tests this against the known-constant predicate
table; jump-threading propagates known-zero/known-one conditions
backwards through the CFG to remove the branch entirely.
@End Reasoning

Process called "get_branch_condition" that takes branch as Integer returns Integer:
    If branch is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with branch, 72
End Process

@Reasoning
True-branch successor (prefix-embedded offset 80). CFG construction extracts both
successors with two separate getters rather than a tuple to avoid
allocating a transient pair object on every block visit.
@End Reasoning

Process called "get_branch_true_target" that takes branch as Integer returns Integer:
    If branch is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with branch, 80
End Process

@Reasoning
False-branch successor (prefix-embedded offset 88). Distinct from the true-branch
slot so that branch-folding passes can rewrite either edge in place
without disturbing the other.
@End Reasoning

Process called "get_branch_false_target" that takes branch as Integer returns Integer:
    If branch is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with branch, 88
End Process

@Reasoning
Sole successor of an unconditional jump (prefix-embedded offset 72). CFG simplification
uses this to identify trivial successor chains, which it then merges
into a single block when no other predecessor depends on the
intermediate block.
@End Reasoning

Process called "get_jump_target" that takes jump as Integer returns Integer:
    If jump is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with jump, 72
End Process

@Reasoning
Return value (prefix-embedded offset 72). Zero means "void return" (no value); a non-
zero value is the SSA id flowing into the function's return slot.
Return-value-numbering canonicalizes returns by extracting this id.
@End Reasoning

Process called "get_return_value" that takes ret as Integer returns Integer:
    If ret is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with ret, 72
End Process

@Reasoning
Type of the returned value (prefix-embedded offset 80). LIR uses this to choose the
return register layout. Keeping the type on the return node (rather
than re-deriving from the function signature) makes the return node
self-describing for cross-function optimizations like inlining.
@End Reasoning

Process called "get_return_type" that takes ret as Integer returns Integer:
    If ret is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with ret, 80
End Process

@Reasoning
Element type stored in an alloca (prefix-embedded offset 80). Frame-layout passes use
this together with alignment and count to compute exact slot size.
Distinct from the alloca's result type (which is "pointer to element")
because the underlying element size is what drives stack layout.
@End Reasoning

Process called "get_alloca_element_type" that takes alloca as Integer returns Integer:
    If alloca is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with alloca, 80
End Process

@Reasoning
Element count for variable-sized allocas (prefix-embedded offset 88). For fixed-size
allocas this is a constant SSA id pointing at a const node; for VLAs
it is a dynamic SSA value computed at runtime. Either way, the LIR
lowering pass treats it uniformly as an operand.
@End Reasoning

Process called "get_alloca_count" that takes alloca as Integer returns Integer:
    If alloca is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with alloca, 88
End Process

@Reasoning
Explicit alignment (prefix-embedded offset 96). Stored separately from element_type
because callers may request alignment stricter than the element's
natural alignment (e.g., 64-byte alignment for cache-line isolation).
Frame layout honors the maximum of requested and natural.
@End Reasoning

Process called "get_alloca_alignment" that takes alloca as Integer returns Integer:
    If alloca is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with alloca, 96
End Process

@Reasoning
Numeric payload of a constant (prefix-embedded offset 80). For string constants this
field is unused (zero); use is_const_string + get_const_string instead.
Constant folding hashes (value_type, constant_value) for numeric
constants to identify equal-but-separate const nodes for CSE.
@End Reasoning

Process called "get_const_value" that takes const as Integer returns Integer:
    If const is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with const, 80
End Process

@Reasoning
Type of the constant (prefix-embedded offset 88). Determines how the LIR lowering
materializes the value: integer immediates may inline into the
instruction; floats may need a constant-pool slot; strings need a
.rodata reference.
@End Reasoning

Process called "get_const_type" that takes const as Integer returns Integer:
    If const is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with const, 88
End Process

@Reasoning
String-vs-numeric discriminator (prefix-embedded offset 96). Non-zero means the constant
is a string and string_ptr (offset 104) holds the actual pointer; zero
means the numeric payload at offset 80 is the value. Choosing a flag
over two distinct opcodes keeps the constant node uniform across
optimization passes that don't care about the distinction.
@End Reasoning

Process called "is_const_string" that takes const as Integer returns Integer:
    If const is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with const, 96
End Process

@Reasoning
String-pointer payload (prefix-embedded offset 104), valid only when is_const_string is
non-zero. Points into the deduplicated string table so that two const
nodes for the same literal share a single backing string and the
constant-CSE pass can merge them.
@End Reasoning

Process called "get_const_string" that takes const as Integer returns Integer:
    If const is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with const, 104
End Process

Note: Switch Accessors

@Reasoning
Switched value (prefix-embedded offset 72) — the SSA value being matched against cases.
Switch lowering reads this to emit the comparison sequence (chain of
cmps, jump table base register, or binary-search pivot).
@End Reasoning

Process called "get_switch_value" that takes switch as Integer returns Integer:
    If switch is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with switch, 72
End Process

@Reasoning
Case list pointer (prefix-embedded offset 80). Returns the raw List of (case_value,
block_id) pairs so callers that need to iterate all cases at once (e.g.,
jump-table builder) can walk the list directly without N round trips
through the per-case accessors.
@End Reasoning

Process called "get_switch_cases" that takes switch as Integer returns Integer:
    If switch is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with switch, 80
End Process

@Reasoning
Cached case count (prefix-embedded offset 88). Mirrors the case-list length but is
maintained as a separate int64 so the verifier and density-analysis
passes can read it in O(1) without paying a List-length call.
@End Reasoning

Process called "get_switch_case_count" that takes switch as Integer returns Integer:
    If switch is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with switch, 88
End Process

@Reasoning
Default target (prefix-embedded offset 96) — the block taken when no case matches.
Switch always has a default (the parser materializes "unreachable" if
the source did not provide one) so this is never 0 except for null
inputs.
@End Reasoning

Process called "get_switch_default_block" that takes switch as Integer returns Integer:
    If switch is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with switch, 96
End Process

@Reasoning
Decodes the case value at the given index (even slot in the pair list).
Density analysis sorts cases by value to pick jump-table-vs-chain
lowering, so per-index value access is on the hot path of that decision.
@End Reasoning

Process called "get_switch_case_value" that takes switch as Integer, index as Integer returns Integer:
    If switch is equal to 0:
        Return 0
    End If
    Let cases be proc memory_get_int64 from Layout with switch, 80
    Let pair_index be index multiplied by 2
    Return proc get from List with cases, pair_index
End Process

@Reasoning
Decodes the target block at the given case index (odd slot in the pair
list). CFG construction walks all (value, block) pairs to build the
switch's successor edges; per-index block access keeps that walk
allocation-free.
@End Reasoning

Process called "get_switch_case_block" that takes switch as Integer, index as Integer returns Integer:
    If switch is equal to 0:
        Return 0
    End If
    Let cases be proc memory_get_int64 from Layout with switch, 80
    Let pair_index be index multiplied by 2 plus 1
    Return proc get from List with cases, pair_index
End Process

Note: Atomic Operation Accessors

@Reasoning
Atomic address (prefix-embedded offset 80) — the memory location being mutated. Alias
analysis treats atomic addresses as opaque (cannot be reordered with
other atomics on the same location) so this accessor is the key into
that analysis.
@End Reasoning

Process called "get_atomic_address" that takes atomic as Integer returns Integer:
    If atomic is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with atomic, 80
End Process

@Reasoning
Primary operand (prefix-embedded offset 88) — the value being added/xchg'd/etc. For
plain loads it is unused; for store-style atomics it is the value
being written; for arithmetic atomics it is the delta.
@End Reasoning

Process called "get_atomic_operand" that takes atomic as Integer returns Integer:
    If atomic is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with atomic, 88
End Process

@Reasoning
Expected-value field (prefix-embedded offset 96) — populated only for compare-and-swap
opcodes. The CAS lowering reads both expected and operand; other atomic
opcodes leave this slot at 0 to keep the node layout uniform.
@End Reasoning

Process called "get_atomic_expected" that takes atomic as Integer returns Integer:
    If atomic is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with atomic, 96
End Process

@Reasoning
Memory-ordering tag (prefix-embedded offset 104): 0=relaxed, 1=acquire, 2=release,
3=acq_rel, 4=seq_cst. LIR lowering uses this to emit the proper fence
instruction or to pick the architectural opcode (e.g., x86 lock-xadd
vs plain xadd).
@End Reasoning

Process called "get_atomic_memory_order" that takes atomic as Integer returns Integer:
    If atomic is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with atomic, 104
End Process

Note: Vector Operation Accessors

@Reasoning
First vector operand (prefix-embedded offset 80). Vector ops can be ternary (e.g.,
fused-multiply-add, blends with mask), so operands are exposed as three
separate accessors. Operand1 is the primary input for unary, binary,
and ternary vector ops alike.
@End Reasoning

Process called "get_vector_operand1" that takes vector as Integer returns Integer:
    If vector is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with vector, 80
End Process

@Reasoning
Second vector operand (prefix-embedded offset 88). Unused (zero) for strictly unary
vector ops; populated for binary and ternary forms. Vectorization
passes read operand2 to build the dependence DAG between SIMD lanes.
@End Reasoning

Process called "get_vector_operand2" that takes vector as Integer returns Integer:
    If vector is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with vector, 88
End Process

@Reasoning
Third vector operand (prefix-embedded offset 96). Populated only for ternary vector
opcodes (FMA, select). The slot is part of the standard layout so
shape-uniform walkers can read it unconditionally — they get 0 for
non-ternary ops, which is the documented sentinel.
@End Reasoning

Process called "get_vector_operand3" that takes vector as Integer returns Integer:
    If vector is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with vector, 96
End Process

@Reasoning
Vector width (prefix-embedded offset 104) — number of SIMD lanes (2, 4, 8, 16, 32, 64).
The LIR backend matches width against architecture capabilities (SSE 128
= 4-wide f32, AVX-256 = 8-wide f32, AVX-512 = 16-wide f32) to pick the
right instruction and register class.
@End Reasoning

Process called "get_vector_width" that takes vector as Integer returns Integer:
    If vector is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with vector, 104
End Process

@Reasoning
Element type (prefix-embedded offset 112) — the type of one lane of the vector. Combined
with width, this fully describes the vector layout. The backend picks
the lane size from the type's bit width when computing the total
vector size.
@End Reasoning

Process called "get_vector_element_type" that takes vector as Integer returns Integer:
    If vector is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with vector, 112
End Process

@Reasoning
Mask/shuffle data (prefix-embedded offset 120). Non-zero only for shuffle, blend, and
gather/scatter opcodes that select per-lane behavior. Encoded as a
packed byte sequence (one byte per lane index or per lane predicate)
so it fits in a single int64 for narrow vectors and as a pointer to
external storage for wider ones.
@End Reasoning

Process called "get_vector_mask_data" that takes vector as Integer returns Integer:
    If vector is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with vector, 120
End Process

Note: Exception Handling Accessors

@Reasoning
Invoke callee (prefix-embedded offset 80) — same encoding as call.callee. Inlining is
allowed to remove the exception edges if it proves the inlinee cannot
throw, in which case it would replace this node with a plain call.
@End Reasoning

Process called "get_invoke_callee" that takes invoke as Integer returns Integer:
    If invoke is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with invoke, 80
End Process

@Reasoning
Invoke arguments list (prefix-embedded offset 88). Mirrors call.arguments but lives on
the invoke node so the two opcodes share layout up through offset 104.
Argument-promotion runs over both equally.
@End Reasoning

Process called "get_invoke_arguments" that takes invoke as Integer returns Integer:
    If invoke is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with invoke, 88
End Process

@Reasoning
Invoke argument count (prefix-embedded offset 96). Cached length, kept consistent with
the list by the constructor and any argument-modifying pass.
@End Reasoning

Process called "get_invoke_argument_count" that takes invoke as Integer returns Integer:
    If invoke is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with invoke, 96
End Process

@Reasoning
Normal-return successor (prefix-embedded offset 112) — taken when the invoked callee
returns without throwing. The CFG must see this edge to keep dominance
correct, and most optimization passes treat it as the "fall-through"
side of the invoke.
@End Reasoning

Process called "get_invoke_normal_block" that takes invoke as Integer returns Integer:
    If invoke is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with invoke, 112
End Process

@Reasoning
Exception successor (prefix-embedded offset 120) — taken when the invoked callee
unwinds. Marks the entry to a landing-pad block. Cleanup-elimination
may rewire this edge, but it cannot remove it as long as the callee
may throw.
@End Reasoning

Process called "get_invoke_exception_block" that takes invoke as Integer returns Integer:
    If invoke is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with invoke, 120
End Process

@Reasoning
Exception-type field on a landing pad (prefix-embedded offset 80) — the runtime type
descriptor used to match incoming throws against this pad. The runtime
uses RTTI on the thrown value to find a matching catch.
@End Reasoning

Process called "get_landingpad_exception_type" that takes landingpad as Integer returns Integer:
    If landingpad is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with landingpad, 80
End Process

@Reasoning
Catch clauses list (prefix-embedded offset 88). Each clause is a (type_id, block_id)
pair stored as two consecutive list elements, in source order. The
runtime tests them in order, so optimizations that reorder clauses
must preserve the "first match wins" semantics.
@End Reasoning

Process called "get_landingpad_catch_clauses" that takes landingpad as Integer returns Integer:
    If landingpad is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with landingpad, 88
End Process

@Reasoning
Cached catch-clause count (prefix-embedded offset 96). Lockstep with the clauses list.
Cleanup-elimination and clause-merging both consult this count when
deciding whether the landing pad has been reduced to a single trivial
clause that can be folded out.
@End Reasoning

Process called "get_landingpad_clause_count" that takes landingpad as Integer returns Integer:
    If landingpad is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with landingpad, 96
End Process

@Reasoning
Cleanup successor (prefix-embedded offset 104) — taken when no catch clause matches but
the pad still has cleanup actions (RAII-like destructors, finally
blocks). The runtime jumps here after executing cleanup, then re-raises
the exception to continue unwinding.
@End Reasoning

Process called "get_landingpad_cleanup_block" that takes landingpad as Integer returns Integer:
    If landingpad is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with landingpad, 104
End Process

@Reasoning
Thrown SSA value (prefix-embedded offset 72). The exception object that will be raised.
Unwinding inspects this value's runtime type to find a matching landing
pad. Exception-flow analysis can fold a throw whose value flows directly
from a catch with no transformation.
@End Reasoning

Process called "get_throw_exception_value" that takes throw as Integer returns Integer:
    If throw is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with throw, 72
End Process

@Reasoning
Static type tag on a throw (prefix-embedded offset 80). Lets static analysis match
throws to catches without inspecting runtime RTTI — useful for proving
that a throw cannot escape a given function (so the callee's invoke
edges can be trimmed).
@End Reasoning

Process called "get_throw_exception_type" that takes throw as Integer returns Integer:
    If throw is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with throw, 80
End Process

Note: Debug Info Accessors

@Reasoning
SSA value id (prefix-embedded offset 72) that a debug-value annotation is shadowing. Reads
the SSA value the debug node is tracking on behalf of a source variable.
@End Reasoning

Process called "get_debug_value_id" that takes debug as Integer returns Integer:
    If debug is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with debug, 72
End Process

@Reasoning
Original source variable name (prefix-embedded offset 80). String-table pointer kept on
the debug node so dumps and debuggers can map the SSA name back to source.
@End Reasoning

Process called "get_debug_variable_name" that takes debug as Integer returns Integer:
    If debug is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with debug, 80
End Process

@Reasoning
Source file (prefix-embedded offset 88) — string pointer to the file path the variable
declaration came from. Used by debuggers to navigate to the declaration.
@End Reasoning

Process called "get_debug_source_file" that takes debug as Integer returns Integer:
    If debug is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with debug, 88
End Process

@Reasoning
Source line (prefix-embedded offset 96) — 1-based line number where the variable was
declared. Combined with file gives the precise source position.
@End Reasoning

Process called "get_debug_source_line" that takes debug as Integer returns Integer:
    If debug is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with debug, 96
End Process

@Reasoning
Source column (prefix-embedded offset 104) — 1-based column. DWARF emitters use line+col
to generate the .debug_line table that maps PC ranges to source positions.
@End Reasoning

Process called "get_debug_source_column" that takes debug as Integer returns Integer:
    If debug is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with debug, 104
End Process

@Reasoning
Debug scope id (prefix-embedded offset 112) — identifies the lexical block, function, or
namespace the variable belongs to. Scope information enables correct
variable-shadowing behavior in debuggers.
@End Reasoning

Process called "get_debug_scope_id" that takes debug as Integer returns Integer:
    If debug is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with debug, 112
End Process

@Reasoning
Location source file (prefix-embedded offset 72 in MIRDebugLocation). Distinct from
MIRDebugValue: location nodes attach to instructions, not values, and
drive stack-trace line numbers rather than variable inspection.
@End Reasoning

Process called "get_location_source_file" that takes loc as Integer returns Integer:
    If loc is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with loc, 72
End Process

@Reasoning
Location source line (prefix-embedded offset 80). Drives the .debug_line PC→line mapping
that the runtime consults for backtrace formatting.
@End Reasoning

Process called "get_location_source_line" that takes loc as Integer returns Integer:
    If loc is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with loc, 80
End Process

@Reasoning
Location source column (prefix-embedded offset 88). Column-aware backtraces let crash
dumps point at the exact expression that raised an error, not just the
containing line.
@End Reasoning

Process called "get_location_source_column" that takes loc as Integer returns Integer:
    If loc is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with loc, 88
End Process

@Reasoning
Location scope id (prefix-embedded offset 96). Different from the debug-value scope because
locations track lexical block identity for instruction-level granularity
(useful when single-stepping through inlined code).
@End Reasoning

Process called "get_location_scope_id" that takes loc as Integer returns Integer:
    If loc is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with loc, 96
End Process

@Reasoning
Inlined-at marker (prefix-embedded offset 104). When non-zero, indicates this instruction
came from an inlined function; the value is the call-site location id of
the original call. Debuggers use this to reconstruct virtual frames so
inlined code appears as a proper inline stack frame in backtraces.
@End Reasoning

Process called "get_location_inlined_at" that takes loc as Integer returns Integer:
    If loc is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with loc, 104
End Process

Note: ============================================================================
Note: Specialized Debug Printing Functions
Note: Format instructions in human-readable SSA form for optimizer debugging
Note: ============================================================================

@Implementation
Formats a phi node as: %result = phi type [%val1, block1], [%val2, block2], ...
Example: %3 = phi i32 [%1, block_0], [%2, block_1]
@End Implementation

@Reasoning
Phi node debug-print produces "%N = phi [%val, block_K], ..." in LLVM-ish
style so MIR dumps are familiar to compiler engineers and so the verifier's
error messages can quote the exact offending node. The constructed string
is heap-owned so callers must deallocate it; per-pair concatenation is
deliberate to keep the printer's allocator footprint linear in pair count
rather than super-linear.
@End Reasoning

Process called "phi_node_to_string" that takes phi as Integer returns Integer:
    If phi is equal to 0:
        Return proc string_duplicate from StringCore with "<null phi>"
    End If

    Let result_value_id be proc memory_get_int64 from Layout with phi, 72
    Let incoming_count be proc memory_get_int64 from Layout with phi, 88

    Let result_str be proc integer_to_string from StringUtils with result_value_id
    Let result be proc string_duplicate from StringCore with "%"
    Set result to proc string_concat from StringUtils with result, result_str
    Set result to proc string_concat from StringUtils with result, " = phi"
    proc deallocate from Layout with result_str

    Let i be 0
    While i is less than incoming_count:
        Let block_id be proc get_phi_incoming_block with phi, i
        Let value_id be proc get_phi_incoming_value with phi, i

        Let block_str be proc integer_to_string from StringUtils with block_id
        Let value_str be proc integer_to_string from StringUtils with value_id

        Set result to proc string_concat from StringUtils with result, " [%"
        Set result to proc string_concat from StringUtils with result, value_str
        Set result to proc string_concat from StringUtils with result, ", block_"
        Set result to proc string_concat from StringUtils with result, block_str
        Set result to proc string_concat from StringUtils with result, "]"

        proc deallocate from Layout with block_str
        proc deallocate from Layout with value_str

        If i is less than incoming_count minus 1:
            Set result to proc string_concat from StringUtils with result, ","
        End If

        Set i to i plus 1
    End While

    Return result
End Process

@Reasoning
Binop debug-print mirrors LLVM IR conventions so dumps can be diffed
across compilers when porting tests. Pulling the opcode through the base
MIRNode (rather than caching it on the binop) keeps a single source of
truth for opcode and avoids two locations to update if MIR opcode numbering
ever shifts.
@End Reasoning

@Implementation
Formats a binary operation as: %result = opcode type %left, %right
Example: %5 = add i32 %3, %4
@End Implementation

Process called "binary_op_to_string" that takes binop as Integer returns Integer:
    If binop is equal to 0:
        Return proc string_duplicate from StringCore with "<null binop>"
    End If

    Let result_value_id be proc memory_get_int64 from Layout with binop, 72
    Let left_op be proc memory_get_int64 from Layout with binop, 80
    Let right_op be proc memory_get_int64 from Layout with binop, 88

    Note: Prefix embedding: the record IS the node, opcode lives at offset 0.
    Let opcode be proc get_mir_node_opcode from MIR with binop
    Let opcode_str be proc opcode_to_string from MIR with opcode
    Let result_str be proc integer_to_string from StringUtils with result_value_id
    Let left_str be proc integer_to_string from StringUtils with left_op
    Let right_str be proc integer_to_string from StringUtils with right_op

    Let result be proc string_duplicate from StringCore with "%"
    Set result to proc string_concat from StringUtils with result, result_str
    Set result to proc string_concat from StringUtils with result, " = "
    Set result to proc string_concat from StringUtils with result, opcode_str
    Set result to proc string_concat from StringUtils with result, " %"
    Set result to proc string_concat from StringUtils with result, left_str
    Set result to proc string_concat from StringUtils with result, ", %"
    Set result to proc string_concat from StringUtils with result, right_str

    proc deallocate from Layout with opcode_str
    proc deallocate from Layout with result_str
    proc deallocate from Layout with left_str
    proc deallocate from Layout with right_str

    Return result
End Process

@Reasoning
Load debug-print uses LLVM "load ptr %addr" shorthand. The offset is
intentionally elided from the default dump when it would be 0 — explicit
zero offsets clutter the dump for the common direct-load case. Verifier
errors that need the offset use a dedicated, more verbose dump path.
@End Reasoning

@Implementation
Formats a load instruction as: %result = load type, ptr %address
Example: %10 = load i32, ptr %9
@End Implementation

Process called "load_to_string" that takes load as Integer returns Integer:
    If load is equal to 0:
        Return proc string_duplicate from StringCore with "<null load>"
    End If

    Let result_value_id be proc memory_get_int64 from Layout with load, 72
    Let address_value_id be proc memory_get_int64 from Layout with load, 80

    Let result_str be proc integer_to_string from StringUtils with result_value_id
    Let address_str be proc integer_to_string from StringUtils with address_value_id

    Let result be proc string_duplicate from StringCore with "%"
    Set result to proc string_concat from StringUtils with result, result_str
    Set result to proc string_concat from StringUtils with result, " = load ptr %"
    Set result to proc string_concat from StringUtils with result, address_str

    proc deallocate from Layout with result_str
    proc deallocate from Layout with address_str

    Return result
End Process

@Reasoning
Store debug-print follows "store value, ptr addr" so the value-then-ptr
order reads naturally in dumps and matches the LLVM convention. There is
no result_value_id to print because stores produce no SSA value, which
makes the dump shorter than for loads — a visual cue to the reader that
this is a side-effecting node, not a value-producing one.
@End Reasoning

@Implementation
Formats a store instruction as: store type %value, ptr %address
Example: store i32 %5, ptr %6
@End Implementation

Process called "store_to_string" that takes store as Integer returns Integer:
    If store is equal to 0:
        Return proc string_duplicate from StringCore with "<null store>"
    End If

    Let address_value_id be proc memory_get_int64 from Layout with store, 72
    Let value_to_store be proc memory_get_int64 from Layout with store, 80

    Let address_str be proc integer_to_string from StringUtils with address_value_id
    Let value_str be proc integer_to_string from StringUtils with value_to_store

    Let result be proc string_duplicate from StringCore with "store %"
    Set result to proc string_concat from StringUtils with result, value_str
    Set result to proc string_concat from StringUtils with result, ", ptr %"
    Set result to proc string_concat from StringUtils with result, address_str

    proc deallocate from Layout with address_str
    proc deallocate from Layout with value_str

    Return result
End Process

@Reasoning
Call debug-print elides the "%N =" prefix when result_value_id is 0 (void
call) so void calls do not waste characters on a phantom result. Arguments
are pulled from the List and printed comma-separated; the constructed
string keeps the per-argument allocation discipline of the rest of the
printer (each integer-to-string allocation is deallocated immediately
after being concatenated).
@End Reasoning

@Implementation
Formats a call instruction as: %result = call return_type @function(%arg1, %arg2, ...)
Example: %7 = call i32 @factorial(%6)
@End Implementation

Process called "call_to_string" that takes call as Integer returns Integer:
    If call is equal to 0:
        Return proc string_duplicate from StringCore with "<null call>"
    End If

    Let result_value_id be proc memory_get_int64 from Layout with call, 72
    Let callee be proc memory_get_int64 from Layout with call, 80
    Let arguments be proc memory_get_int64 from Layout with call, 88
    Let argument_count be proc memory_get_int64 from Layout with call, 96

    Let result be proc string_duplicate from StringCore with ""

    If result_value_id is not equal to 0:
        Let result_str be proc integer_to_string from StringUtils with result_value_id
        Set result to proc string_concat from StringUtils with result, "%"
        Set result to proc string_concat from StringUtils with result, result_str
        Set result to proc string_concat from StringUtils with result, " = "
        proc deallocate from Layout with result_str
    End If

    Set result to proc string_concat from StringUtils with result, "call @"

    Let i be 0
    While i is less than argument_count:
        Let arg_value_id be proc get from List with arguments, i
        Let arg_str be proc integer_to_string from StringUtils with arg_value_id

        If i is equal to 0:
            Set result to proc string_concat from StringUtils with result, "(%"
        Otherwise:
            Set result to proc string_concat from StringUtils with result, ", %"
        End If

        Set result to proc string_concat from StringUtils with result, arg_str
        proc deallocate from Layout with arg_str

        Set i to i plus 1
    End While

    Set result to proc string_concat from StringUtils with result, ")"

    Return result
End Process

@Implementation
Formats a branch instruction as: br i1 %cond, label %true, label %false
Example: br i1 %2, label %block_1, label %block_2
@End Implementation

@Reasoning
Branch debug-print uses "br i1 %cond, label %true, label %false" to mirror
LLVM IR. The verbose form (with both targets) is preferred over a compact
"branch %cond -> %true | %false" because dumps are read most often during
backend debugging, where matching LLVM convention reduces cognitive load.
@End Reasoning

Process called "branch_to_string" that takes branch as Integer returns Integer:
    If branch is equal to 0:
        Return proc string_duplicate from StringCore with "<null branch>"
    End If

    Let condition_value_id be proc memory_get_int64 from Layout with branch, 72
    Let true_block_id be proc memory_get_int64 from Layout with branch, 80
    Let false_block_id be proc memory_get_int64 from Layout with branch, 88

    Let cond_str be proc integer_to_string from StringUtils with condition_value_id
    Let true_str be proc integer_to_string from StringUtils with true_block_id
    Let false_str be proc integer_to_string from StringUtils with false_block_id

    Let result be proc string_duplicate from StringCore with "br i1 %"
    Set result to proc string_concat from StringUtils with result, cond_str
    Set result to proc string_concat from StringUtils with result, ", label %block_"
    Set result to proc string_concat from StringUtils with result, true_str
    Set result to proc string_concat from StringUtils with result, ", label %block_"
    Set result to proc string_concat from StringUtils with result, false_str

    proc deallocate from Layout with cond_str
    proc deallocate from Layout with true_str
    proc deallocate from Layout with false_str

    Return result
End Process

@Reasoning
Jump debug-print uses "jmp label %target" — short by design because the
unconditional jump is the simplest terminator and visual brevity reinforces
that. Note the deliberate use of "jmp" rather than "br" to make
unconditional vs conditional control flow visually distinct in dumps.
@End Reasoning

@Implementation
Formats a jump instruction as: jmp label %target
Example: jmp label %block_3
@End Implementation

Process called "jump_to_string" that takes jump as Integer returns Integer:
    If jump is equal to 0:
        Return proc string_duplicate from StringCore with "<null jump>"
    End If

    Let target_block_id be proc memory_get_int64 from Layout with jump, 72
    Let target_str be proc integer_to_string from StringUtils with target_block_id

    Let result be proc string_duplicate from StringCore with "jmp label %block_"
    Set result to proc string_concat from StringUtils with result, target_str

    proc deallocate from Layout with target_str

    Return result
End Process

@Reasoning
Return debug-print switches between "ret void" (no value) and "ret %N"
based on whether return_value_id is zero. The two forms share no
character with each other beyond "ret", so we build them as separate
literals rather than concatenating an optional value suffix — this keeps
the void branch allocation-free and matches the LLVM dump convention.
@End Reasoning

@Implementation
Formats a return instruction as: ret type %value or ret void
Example: ret i32 %5 or ret void
@End Implementation

Process called "return_to_string" that takes ret as Integer returns Integer:
    If ret is equal to 0:
        Return proc string_duplicate from StringCore with "<null return>"
    End If

    Let return_value_id be proc memory_get_int64 from Layout with ret, 72

    If return_value_id is equal to 0:
        Return proc string_duplicate from StringCore with "ret void"
    End If

    Let value_str be proc integer_to_string from StringUtils with return_value_id
    Let result be proc string_duplicate from StringCore with "ret %"
    Set result to proc string_concat from StringUtils with result, value_str
    proc deallocate from Layout with value_str

    Return result
End Process

@Reasoning
Alloca debug-print shows result-id and alignment. The element type is
intentionally elided from the default dump form (the alloca's result
type "%N = alloca, align K" already conveys what is being allocated
through downstream uses); when fuller info is needed, the verifier
uses a dedicated dump path that includes element type and count.
@End Reasoning

@Implementation
Formats an alloca instruction as: %result = alloca type, align N
Example: %8 = alloca i32, align 4
@End Implementation

Process called "alloca_to_string" that takes alloca as Integer returns Integer:
    If alloca is equal to 0:
        Return proc string_duplicate from StringCore with "<null alloca>"
    End If

    Let result_value_id be proc memory_get_int64 from Layout with alloca, 72
    Let alignment be proc memory_get_int64 from Layout with alloca, 96

    Let result_str be proc integer_to_string from StringUtils with result_value_id
    Let align_str be proc integer_to_string from StringUtils with alignment

    Let result be proc string_duplicate from StringCore with "%"
    Set result to proc string_concat from StringUtils with result, result_str
    Set result to proc string_concat from StringUtils with result, " = alloca, align "
    Set result to proc string_concat from StringUtils with result, align_str

    proc deallocate from Layout with result_str
    proc deallocate from Layout with align_str

    Return result
End Process

@Reasoning
Const debug-print branches on is_string: numeric constants print their
literal value, string constants print the quoted source text. The split
keeps the dump readable for both cases without forcing a single uniform
encoding ("const ptr %0x..." for strings would lose the literal text).
@End Reasoning

@Implementation
Formats a constant instruction as: %result = const value
Example: %1 = const 42
@End Implementation

Process called "const_to_string" that takes const as Integer returns Integer:
    If const is equal to 0:
        Return proc string_duplicate from StringCore with "<null const>"
    End If

    Let result_value_id be proc memory_get_int64 from Layout with const, 72
    Let constant_value be proc memory_get_int64 from Layout with const, 80
    Let is_string be proc memory_get_int64 from Layout with const, 96

    Let result_str be proc integer_to_string from StringUtils with result_value_id

    Let result be proc string_duplicate from StringCore with "%"
    Set result to proc string_concat from StringUtils with result, result_str
    Set result to proc string_concat from StringUtils with result, " = const "

    proc deallocate from Layout with result_str

    If is_string is equal to 1:
        Let string_ptr be proc memory_get_int64 from Layout with const, 104
        Set result to proc string_concat from StringUtils with result, "\""
        If string_ptr is not equal to 0:
            Set result to proc string_concat from StringUtils with result, string_ptr
        End If
        Set result to proc string_concat from StringUtils with result, "\""
    Otherwise:
        Let value_str be proc integer_to_string from StringUtils with constant_value
        Set result to proc string_concat from StringUtils with result, value_str
        proc deallocate from Layout with value_str
    End If

    Return result
End Process

@Reasoning
Switch debug-print iterates every case in order, then appends the default
clause. The "value => block" arrow notation is chosen instead of the LLVM
bracket form because it reads more naturally for the long case lists that
appear in match/dispatch-heavy Runa code. Per-case allocations are freed
immediately after concatenation to keep peak memory bounded.
@End Reasoning

@Implementation
Formats a switch instruction as: switch %value [ case1 => block1, case2 => block2, default => block_default ]
Example: switch %5 [ 0 => block_1, 1 => block_2, default => block_3 ]
@End Implementation

Process called "switch_to_string" that takes switch as Integer returns Integer:
    If switch is equal to 0:
        Return proc string_duplicate from StringCore with "<null switch>"
    End If

    Let value_id be proc memory_get_int64 from Layout with switch, 72
    Let case_count be proc memory_get_int64 from Layout with switch, 88
    Let default_block_id be proc memory_get_int64 from Layout with switch, 96

    Let value_str be proc integer_to_string from StringUtils with value_id
    Let result be proc string_duplicate from StringCore with "switch %"
    Set result to proc string_concat from StringUtils with result, value_str
    Set result to proc string_concat from StringUtils with result, " ["
    proc deallocate from Layout with value_str

    Let i be 0
    While i is less than case_count:
        Let case_value be proc get_switch_case_value with switch, i
        Let block_id be proc get_switch_case_block with switch, i

        Let case_str be proc integer_to_string from StringUtils with case_value
        Let block_str be proc integer_to_string from StringUtils with block_id

        Set result to proc string_concat from StringUtils with result, " "
        Set result to proc string_concat from StringUtils with result, case_str
        Set result to proc string_concat from StringUtils with result, " => block_"
        Set result to proc string_concat from StringUtils with result, block_str

        proc deallocate from Layout with case_str
        proc deallocate from Layout with block_str

        If i is less than case_count minus 1:
            Set result to proc string_concat from StringUtils with result, ","
        End If

        Set i to i plus 1
    End While

    Let default_str be proc integer_to_string from StringUtils with default_block_id
    Set result to proc string_concat from StringUtils with result, ", default => block_"
    Set result to proc string_concat from StringUtils with result, default_str
    Set result to proc string_concat from StringUtils with result, " ]"
    proc deallocate from Layout with default_str

    Return result
End Process

@Reasoning
Atomic-op debug-print spells out the memory-ordering tag (relaxed,
acquire, release, acq_rel, seq_cst) for readability — the numeric tag
would force the reader to remember the encoding. The operand is optional
(zero for atomic loads) so the printer guards each component with a
non-zero check, keeping dumps free of spurious "%0" placeholders.
@End Reasoning

@Implementation
Formats an atomic operation as: %result = atomic_op memory_order ptr %addr, %operand
Example: %10 = atomic_add seq_cst ptr %9, %8
@End Implementation

Process called "atomic_op_to_string" that takes atomic as Integer returns Integer:
    If atomic is equal to 0:
        Return proc string_duplicate from StringCore with "<null atomic>"
    End If

    Let result_value_id be proc memory_get_int64 from Layout with atomic, 72
    Let address_value_id be proc memory_get_int64 from Layout with atomic, 80
    Let operand_value_id be proc memory_get_int64 from Layout with atomic, 88
    Let memory_order be proc memory_get_int64 from Layout with atomic, 104

    Note: Prefix embedding: the record IS the node, opcode lives at offset 0.
    Let opcode be proc get_mir_node_opcode from MIR with atomic
    Let opcode_str be proc opcode_to_string from MIR with opcode

    Let result be proc string_duplicate from StringCore with ""

    If result_value_id is not equal to 0:
        Let result_str be proc integer_to_string from StringUtils with result_value_id
        Set result to proc string_concat from StringUtils with result, "%"
        Set result to proc string_concat from StringUtils with result, result_str
        Set result to proc string_concat from StringUtils with result, " = "
        proc deallocate from Layout with result_str
    End If

    Set result to proc string_concat from StringUtils with result, opcode_str

    Note: Memory order names
    If memory_order is equal to 0:
        Set result to proc string_concat from StringUtils with result, " relaxed"
    End If
    If memory_order is equal to 1:
        Set result to proc string_concat from StringUtils with result, " acquire"
    End If
    If memory_order is equal to 2:
        Set result to proc string_concat from StringUtils with result, " release"
    End If
    If memory_order is equal to 3:
        Set result to proc string_concat from StringUtils with result, " acq_rel"
    End If
    If memory_order is equal to 4:
        Set result to proc string_concat from StringUtils with result, " seq_cst"
    End If

    Let addr_str be proc integer_to_string from StringUtils with address_value_id
    Set result to proc string_concat from StringUtils with result, " ptr %"
    Set result to proc string_concat from StringUtils with result, addr_str
    proc deallocate from Layout with addr_str

    If operand_value_id is not equal to 0:
        Let op_str be proc integer_to_string from StringUtils with operand_value_id
        Set result to proc string_concat from StringUtils with result, ", %"
        Set result to proc string_concat from StringUtils with result, op_str
        proc deallocate from Layout with op_str
    End If

    proc deallocate from Layout with opcode_str

    Return result
End Process

@Reasoning
Vector-op debug-print uses LLVM "<width x type>" shorthand because that
form makes lane count visible at a glance — critical when diffing
vectorization passes that may change width across versions. Operand3 is
elided from the default dump (only FMA and select use it); a verifier-
mode dump path includes operand3 when present.
@End Reasoning

@Implementation
Formats a vector operation as: %result = vector_op <width x type> %op1, %op2
Example: %7 = vector_add <4 x float> %5, %6
@End Implementation

Process called "vector_op_to_string" that takes vector as Integer returns Integer:
    If vector is equal to 0:
        Return proc string_duplicate from StringCore with "<null vector>"
    End If

    Let result_value_id be proc memory_get_int64 from Layout with vector, 72
    Let operand1 be proc memory_get_int64 from Layout with vector, 80
    Let operand2 be proc memory_get_int64 from Layout with vector, 88
    Let vector_width be proc memory_get_int64 from Layout with vector, 104

    Note: Prefix embedding: the record IS the node, opcode lives at offset 0.
    Let opcode be proc get_mir_node_opcode from MIR with vector
    Let opcode_str be proc opcode_to_string from MIR with opcode

    Let result be proc string_duplicate from StringCore with ""

    If result_value_id is not equal to 0:
        Let result_str be proc integer_to_string from StringUtils with result_value_id
        Set result to proc string_concat from StringUtils with result, "%"
        Set result to proc string_concat from StringUtils with result, result_str
        Set result to proc string_concat from StringUtils with result, " = "
        proc deallocate from Layout with result_str
    End If

    Set result to proc string_concat from StringUtils with result, opcode_str

    Let width_str be proc integer_to_string from StringUtils with vector_width
    Set result to proc string_concat from StringUtils with result, " <"
    Set result to proc string_concat from StringUtils with result, width_str
    Set result to proc string_concat from StringUtils with result, " x type>"
    proc deallocate from Layout with width_str

    If operand1 is not equal to 0:
        Let op1_str be proc integer_to_string from StringUtils with operand1
        Set result to proc string_concat from StringUtils with result, " %"
        Set result to proc string_concat from StringUtils with result, op1_str
        proc deallocate from Layout with op1_str
    End If

    If operand2 is not equal to 0:
        Let op2_str be proc integer_to_string from StringUtils with operand2
        Set result to proc string_concat from StringUtils with result, ", %"
        Set result to proc string_concat from StringUtils with result, op2_str
        proc deallocate from Layout with op2_str
    End If

    proc deallocate from Layout with opcode_str

    Return result
End Process

@Reasoning
Invoke debug-print extends the call-dump form with two label suffixes
("normal:" and "exception:") so both successor edges are visible in
one line. This makes invokes immediately distinguishable from calls in
dumps — important because optimization decisions hinge on the distinction.
@End Reasoning

@Implementation
Formats an invoke instruction as: %result = invoke @function(%args) normal: block1, exception: block2
Example: %5 = invoke @may_throw(%3, %4) normal: block_2, exception: block_3
@End Implementation


Process called "invoke_to_string" that takes invoke as Integer returns Integer:
    If invoke is equal to 0:
        Return proc string_duplicate from StringCore with "<null invoke>"
    End If

    Let result_value_id be proc memory_get_int64 from Layout with invoke, 72
    Let argument_count be proc memory_get_int64 from Layout with invoke, 96
    Let normal_block_id be proc memory_get_int64 from Layout with invoke, 112
    Let exception_block_id be proc memory_get_int64 from Layout with invoke, 120
    Let arguments be proc memory_get_int64 from Layout with invoke, 88

    Let result be proc string_duplicate from StringCore with ""

    If result_value_id is not equal to 0:
        Let result_str be proc integer_to_string from StringUtils with result_value_id
        Set result to proc string_concat from StringUtils with result, "%"
        Set result to proc string_concat from StringUtils with result, result_str
        Set result to proc string_concat from StringUtils with result, " = "
        proc deallocate from Layout with result_str
    End If

    Set result to proc string_concat from StringUtils with result, "invoke @function("

    Let i be 0
    While i is less than argument_count:
        Let arg_value_id be proc get from List with arguments, i
        Let arg_str be proc integer_to_string from StringUtils with arg_value_id

        If i is greater than 0:
            Set result to proc string_concat from StringUtils with result, ", "
        End If

        Set result to proc string_concat from StringUtils with result, "%"
        Set result to proc string_concat from StringUtils with result, arg_str
        proc deallocate from Layout with arg_str

        Set i to i plus 1
    End While

    Set result to proc string_concat from StringUtils with result, ") normal: block_"

    Let normal_str be proc integer_to_string from StringUtils with normal_block_id
    Set result to proc string_concat from StringUtils with result, normal_str
    Set result to proc string_concat from StringUtils with result, ", exception: block_"
    proc deallocate from Layout with normal_str

    Let exception_str be proc integer_to_string from StringUtils with exception_block_id
    Set result to proc string_concat from StringUtils with result, exception_str
    proc deallocate from Layout with exception_str

    Return result
End Process


@Reasoning
Throw debug-print is intentionally minimal — "throw %N" — because the
thrown value's type and runtime metadata are inspected at the catch
site, not at the throw. The verbose form including exception_type is
reserved for verifier output where exception-type tracking matters.
@End Reasoning

@Implementation
Formats a throw instruction as: throw type %exception
Example: throw Exception %10
@End Implementation


Process called "throw_to_string" that takes throw as Integer returns Integer:
    If throw is equal to 0:
        Return proc string_duplicate from StringCore with "<null throw>"
    End If

    Let exception_value_id be proc memory_get_int64 from Layout with throw, 72

    Let value_str be proc integer_to_string from StringUtils with exception_value_id
    Let result be proc string_duplicate from StringCore with "throw %"
    Set result to proc string_concat from StringUtils with result, value_str
    proc deallocate from Layout with value_str

    Return result
End Process


@Reasoning
Debug-value debug-print includes the source variable name in quotes plus
the source location triple. This matches how DWARF reads in dumpers, so
debugger-format diffs against MIR dumps remain meaningful. Unknown
file paths are replaced with "<unknown>" to keep the format positionally
stable for downstream regex-based tooling.
@End Reasoning

@Implementation
Formats a debug value annotation as: debug_value %value "variable_name" at file:line:column
Example: debug_value %5 "counter" at main.runa:42:10
@End Implementation


Process called "debug_value_to_string" that takes debug as Integer returns Integer:
    If debug is equal to 0:
        Return proc string_duplicate from StringCore with "<null debug>"
    End If

    Let value_id be proc memory_get_int64 from Layout with debug, 72
    Let variable_name be proc memory_get_int64 from Layout with debug, 80
    Let source_file be proc memory_get_int64 from Layout with debug, 88
    Let source_line be proc memory_get_int64 from Layout with debug, 96
    Let source_column be proc memory_get_int64 from Layout with debug, 104

    Let value_str be proc integer_to_string from StringUtils with value_id
    Let result be proc string_duplicate from StringCore with "debug_value %"
    Set result to proc string_concat from StringUtils with result, value_str
    proc deallocate from Layout with value_str

    If variable_name is not equal to 0:
        Set result to proc string_concat from StringUtils with result, " \""
        Set result to proc string_concat from StringUtils with result, variable_name
        Set result to proc string_concat from StringUtils with result, "\""
    End If

    Set result to proc string_concat from StringUtils with result, " at "

    If source_file is not equal to 0:
        Set result to proc string_concat from StringUtils with result, source_file
    Otherwise:
        Set result to proc string_concat from StringUtils with result, "<unknown>"
    End If

    Set result to proc string_concat from StringUtils with result, ":"
    Let line_str be proc integer_to_string from StringUtils with source_line
    Set result to proc string_concat from StringUtils with result, line_str
    Set result to proc string_concat from StringUtils with result, ":"
    proc deallocate from Layout with line_str

    Let col_str be proc integer_to_string from StringUtils with source_column
    Set result to proc string_concat from StringUtils with result, col_str
    proc deallocate from Layout with col_str

    Return result
End Process

Note: ============================================================
Note: BLOCK ID ACCESSORS FOR CFG CONSTRUCTION
Note: These functions return block IDs from terminator instructions
Note: Used by cfg.runa for control flow graph construction
Note: ============================================================

@Reasoning
CFG construction needs branch successor block IDs as fully-resolved Integer
labels, not as raw MIRBranch fields. This accessor returns the true target
with -1 sentinel on null input so the CFG builder can distinguish a missing
target from block ID 0 (block 0 is the function entry block, a valid value).
@End Reasoning

Process called "get_mir_branch_true_block" that takes branch as Integer returns Integer:
    @Implementation
    Returns the true target block ID from a MIRBranch instruction.
    Returns -1 if branch pointer is null.
    @End Implementation

    If branch is equal to 0:
        Return negative 1
    End If
    Return proc memory_get_int64 from Layout with branch, 80
End Process

@Reasoning
Mirror of get_mir_branch_true_block for the false successor. Two distinct
accessors (rather than an indexed accessor) make CFG-construction code
self-documenting: "true_succ = ..., false_succ = ..." reads cleaner than
"succ_0 = ..., succ_1 = ...". Same -1 sentinel discipline for null inputs.
@End Reasoning

Process called "get_mir_branch_false_block" that takes branch as Integer returns Integer:
    @Implementation
    Returns the false target block ID from a MIRBranch instruction.
    Returns -1 if branch pointer is null.
    @End Implementation

    If branch is equal to 0:
        Return negative 1
    End If
    Return proc memory_get_int64 from Layout with branch, 88
End Process

@Reasoning
Returns the single successor of an unconditional jump for CFG construction.
The -1 sentinel preserves the convention used by the branch accessors
above so the CFG builder can treat all terminator successors uniformly
(any -1 means "skip this edge").
@End Reasoning

Process called "get_mir_jump_target_block" that takes jump as Integer returns Integer:
    @Implementation
    Returns the target block ID from a MIRJump instruction.
    Returns -1 if jump pointer is null.
    @End Implementation

    If jump is equal to 0:
        Return negative 1
    End If
    Return proc memory_get_int64 from Layout with jump, 72
End Process

@Reasoning
CFG construction needs to iterate every case in a switch to discover all
successor edges. Returning the cached count (prefix-embedded offset 88) lets the builder
size its iteration without paying the cost of inspecting the case-list
List for its length. Returns 0 on null input rather than -1 because the
"no cases" interpretation is valid for both null switches and switches
with all cases removed.
@End Reasoning

Process called "get_mir_switch_case_count" that takes switch_ptr as Integer returns Integer:
    @Implementation
    Returns the number of case clauses in a MIRSwitch instruction.
    Returns 0 if switch pointer is null.
    @End Implementation

    If switch_ptr is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with switch_ptr, 88
End Process

@Reasoning
Default-case target accessor used during CFG leader identification: every
basic-block leader must include the default destination, otherwise the
CFG misses an edge and dominance becomes wrong. Returns 0 on null input
because the leader-identification pass treats 0 as "skip" rather than
attempting to read it as a valid target.
@End Reasoning

Process called "get_mir_switch_default_target" that takes switch_ptr as Integer returns Integer:
    @Implementation
    Returns the default target (as instruction/value, not block ID) from a MIRSwitch.
    This is used for identifying leaders during CFG construction.
    Returns 0 if switch pointer is null.
    @End Implementation

    If switch_ptr is equal to 0:
        Return 0
    End If
    Note: Default block ID is at prefix-embedded offset 96
    Return proc memory_get_int64 from Layout with switch_ptr, 96
End Process

@Reasoning
Per-case target accessor used during CFG leader identification. The cases
List (prefix-embedded offset 80) is a FLAT pair List as built by
add_switch_case: case values in even slots, block IDs in odd slots. The
block ID for case N therefore lives at list index 2N+1 — the list element
IS the block ID, not a pointer to a pair record. Bounds-checks both
negative and out-of-range indices because CFG construction iterates by
index and would otherwise read past the case list.
@End Reasoning

Process called "get_mir_switch_case_target" that takes switch_ptr as Integer, case_idx as Integer returns Integer:
    @Implementation
    Returns the target block ID for a specific case in a MIRSwitch.
    Used for identifying leaders during CFG construction.
    Returns 0 if switch pointer is equal to 0 or case_idx is out of bounds.
    @End Implementation

    If switch_ptr is equal to 0:
        Return 0
    End If

    Let case_count be proc memory_get_int64 from Layout with switch_ptr, 88

    If case_idx is less than 0:
        Return 0
    End If

    If case_idx is greater than or equal to case_count:
        Return 0
    End If

    Note: Get the cases list pointer (prefix-embedded offset 80)
    Let cases_list be proc memory_get_int64 from Layout with switch_ptr, 80

    If cases_list is equal to 0:
        Return 0
    End If

    Note: Flat pair List: block ID for case N is the element at slot 2N+1
    Let block_slot be case_idx multiplied by 2 plus 1
    Return proc list_get from List with cases_list, block_slot
End Process

@Reasoning
Block-ID accessor (vs the "default_target" variant above) — uses the -1
sentinel because CFG-edge construction needs to distinguish between "no
default" and "default is block 0". Block 0 (function entry) is a legal
default target in some lowering schemes.
@End Reasoning

Process called "get_mir_switch_default_block" that takes switch_ptr as Integer returns Integer:
    @Implementation
    Returns the default block ID from a MIRSwitch instruction.
    Returns -1 if switch pointer is null.
    @End Implementation

    If switch_ptr is equal to 0:
        Return negative 1
    End If
    Return proc memory_get_int64 from Layout with switch_ptr, 96
End Process

@Reasoning
Block-ID variant of get_mir_switch_case_target, with the same bounds-check
discipline but the -1 sentinel for CFG-edge consumers. Two accessors
(target vs block) exist because CFG construction works at two layers:
leader identification (uses raw target) and edge enumeration (uses block ID).
@End Reasoning

Process called "get_mir_switch_case_block" that takes switch_ptr as Integer, case_idx as Integer returns Integer:
    @Implementation
    Returns the target block ID for a specific case in a MIRSwitch.
    Returns -1 if switch pointer is equal to 0 or case_idx is out of bounds.
    @End Implementation

    If switch_ptr is equal to 0:
        Return negative 1
    End If

    Let case_count be proc memory_get_int64 from Layout with switch_ptr, 88

    If case_idx is less than 0:
        Return negative 1
    End If

    If case_idx is greater than or equal to case_count:
        Return negative 1
    End If

    Note: Get the cases list pointer (prefix-embedded offset 80)
    Let cases_list be proc memory_get_int64 from Layout with switch_ptr, 80

    If cases_list is equal to 0:
        Return negative 1
    End If

    Note: Flat pair List: block ID for case N is the element at slot 2N+1
    Let block_slot be case_idx multiplied by 2 plus 1
    Return proc list_get from List with cases_list, block_slot
End Process

@Reasoning
Normal-successor accessor for invoke nodes, raw-target form. CFG leader
identification visits this to mark the normal-return block as a leader.
Returns 0 on null so the leader-identification driver can treat 0 as
"skip" and continue walking the function uniformly.
@End Reasoning

Process called "get_mir_invoke_normal_dest" that takes invoke_ptr as Integer returns Integer:
    @Implementation
    Returns the normal destination target (as instruction/value, not block ID) from MIRInvoke.
    Used for identifying leaders during CFG construction.
    Returns 0 if invoke pointer is null.
    @End Implementation

    If invoke_ptr is equal to 0:
        Return 0
    End If
    Note: Normal block ID is at prefix-embedded offset 112
    Return proc memory_get_int64 from Layout with invoke_ptr, 112
End Process

@Reasoning
Exception-edge accessor for invoke, raw-target form. The exception
successor block must also be a leader so exception-handling code lives
in its own block. Same 0-sentinel discipline as the normal-dest accessor.
@End Reasoning

Process called "get_mir_invoke_unwind_dest" that takes invoke_ptr as Integer returns Integer:
    @Implementation
    Returns the exception/unwind destination target (as instruction/value, not block ID) from MIRInvoke.
    Used for identifying leaders during CFG construction.
    Returns 0 if invoke pointer is null.
    @End Implementation

    If invoke_ptr is equal to 0:
        Return 0
    End If
    Note: Exception block ID is at prefix-embedded offset 120
    Return proc memory_get_int64 from Layout with invoke_ptr, 120
End Process

@Reasoning
Block-ID variant of get_mir_invoke_normal_dest. CFG edge construction
consumes block IDs (not raw targets), so this accessor exists alongside
its raw-target sibling. -1 sentinel matches the other block-ID accessors
in this section so the CFG builder's null handling stays uniform.
@End Reasoning

Process called "get_mir_invoke_normal_block" that takes invoke_ptr as Integer returns Integer:
    @Implementation
    Returns the normal destination block ID from a MIRInvoke instruction.
    Returns -1 if invoke pointer is null.
    @End Implementation

    If invoke_ptr is equal to 0:
        Return negative 1
    End If
    Return proc memory_get_int64 from Layout with invoke_ptr, 112
End Process

@Reasoning
Block-ID variant of get_mir_invoke_unwind_dest. Pairs with the normal-
block accessor to give the CFG builder both successor edges of an invoke
in the same -1-sentinel format used by the rest of this section.
@End Reasoning

Process called "get_mir_invoke_unwind_block" that takes invoke_ptr as Integer returns Integer:
    @Implementation
    Returns the exception/unwind block ID from a MIRInvoke instruction.
    Returns -1 if invoke pointer is null.
    @End Implementation

    If invoke_ptr is equal to 0:
        Return negative 1
    End If
    Return proc memory_get_int64 from Layout with invoke_ptr, 120
End Process

Note: ============================================================
Note: PHI NODE HELPER FUNCTIONS FOR SSA CONSTRUCTION
Note: ============================================================

Process called "is_phi_node" that takes inst_ptr as Integer returns Integer:
    @Reasoning
    Checks if an instruction is a phi node.

    This is used during SSA construction to identify and process phi nodes.
    Phi nodes must be at the beginning of basic blocks before all other instructions.

    We check if the instruction's opcode is MIR_PHI.

    Returns: 1 if phi node, 0 otherwise
    @End Reasoning

    Note: Null check
    If inst_ptr is equal to 0:
        Return 0
    End If

    Note: Get opcode from the record header (offset 0 — prefix embedding
    Note: guarantees this holds for plain and extended records alike)
    Let opcode be proc memory_get_int64 from Layout with inst_ptr, 0

    Note: Check if opcode is MIR_PHI (20300)
    If opcode is equal to MIR.MIR_PHI:
        Return 1
    End If

    Return 0
End Process

Process called "get_phi_variable_name" that takes phi_ptr as Integer returns Integer:
    @Reasoning
    Gets the variable name for a phi node from its metadata.

    For SSA construction, we need to track which variable each phi node represents.
    This function looks up the original source variable name from the phi node's
    metadata. If no metadata exists, returns a descriptive SSA name "phi_N".

    Returns: Variable name string pointer, or 0 on failure
    @End Reasoning

    Note: Null check
    If phi_ptr is equal to 0:
        Return 0
    End If

    Note: Check if actually a phi node
    Let is_phi be proc is_phi_node with phi_ptr
    If is_phi is equal to 0:
        Return 0
    End If

    Note: Prefix embedding: the phi record IS the node, so its metadata
    Note: lives directly in the header slot at offset 64.
    Let metadata_ptr be proc memory_get_int64 from Layout with phi_ptr, 64

    If metadata_ptr is not equal to 0:
        Note: Get variable name from metadata
        Let variable_name be proc get_metadata_variable_name with metadata_ptr

        If variable_name is not equal to 0:
            Note: Found variable name in metadata - return it
            Return variable_name
        End If
    End If

    Note: No metadata or no variable name - return descriptive SSA name
    Note: PHI nodes represent SSA values, so "phi_N" is appropriate for IR
    Let result_value_id be proc memory_get_int64 from Layout with phi_ptr, 72
    Let name_buffer be proc allocate from Layout with 64

    Note: Create string "phi_"
    proc memory_set_byte from Memory with name_buffer, 0, 112  Note: 'p'
    proc memory_set_byte from Memory with name_buffer, 1, 104  Note: 'h'
    proc memory_set_byte from Memory with name_buffer, 2, 105  Note: 'i'
    proc memory_set_byte from Memory with name_buffer, 3, 95   Note: '_'

    Note: Convert value_id to string
    Let pos be 4
    Let temp_value be result_value_id
    Let digit_count be 0

    Note: Count digits
    Let temp_copy be temp_value
    While temp_copy is greater than 0:
        Set temp_copy to temp_copy divided by 10
        Set digit_count to digit_count plus 1
    End While

    Note: Handle zero case
    If digit_count is equal to 0:
        proc memory_set_byte from Memory with name_buffer, pos, 48  Note: '0'
        Set pos to pos plus 1
    Otherwise:
        Note: Extract digits
        Let i be 0
        While i is less than digit_count:
            Let digit be temp_value modulo by 10
            proc memory_set_byte from Memory with name_buffer, pos plus digit_count minus i minus 1, digit plus 48
            Set temp_value to temp_value divided by 10
            Set i to i plus 1
        End While
        Set pos to pos plus digit_count
    End If

    Note: Null terminate
    proc memory_set_byte from Memory with name_buffer, pos, 0

    Return name_buffer
End Process

Process called "get_phi_result_type" that takes phi_ptr as Integer returns Integer:
    @Reasoning
    Gets the result type (HIRTypeInfo pointer) of a phi node.

    MIRPhiNode prefix-embedded structure (112 bytes):
    - Offsets 0-71: canonical MIRNode header (opcode at 0, value_id at 8,
      operands at 16/24/32, type_info at 40, next at 48, parent_block at
      56, metadata at 64)
    - Offset 72: result_value_id
    - Offset 80: incoming_pairs list
    - Offset 88: incoming_count
    - Offset 96: result_type (HIRTypeInfo pointer)
    - Offset 104: parent_block

    Returns: HIRTypeInfo pointer, or 0 on failure
    @End Reasoning

    Note: Null check
    If phi_ptr is equal to 0:
        Return 0
    End If

    Note: Check if actually a phi node
    Let is_phi be proc is_phi_node with phi_ptr
    If is_phi is equal to 0:
        Return 0
    End If

    Note: Get result_type from the prefix-embedded slot (offset 96)
    Let result_type be proc memory_get_int64 from Layout with phi_ptr, 96
    Return result_type
End Process

Process called "set_phi_result_value_id" that takes phi_ptr as Integer, new_value_id as Integer returns Integer:
    @Reasoning
    Sets the result value ID of a phi node.

    This is used during SSA construction renaming phase to assign fresh SSA names
    to phi node results.

    Under prefix embedding the phi record carries the value id twice:
    - Offset 8: canonical MIRNode header value_id (read by every generic
      header consumer)
    - Offset 72: the phi record's duplicated result slot (read by the
      phi-specific accessors)
    Both must be written together so the two views can never diverge.

    Returns: 1 on success, 0 on failure
    @End Reasoning

    Note: Null check
    If phi_ptr is equal to 0:
        Return 0
    End If

    Note: Check if actually a phi node
    Let is_phi be proc is_phi_node with phi_ptr
    If is_phi is equal to 0:
        Return 0
    End If

    Note: Update the canonical header value_id (offset 8)
    proc memory_set_int64 from Layout with phi_ptr, 8, new_value_id

    Note: Update the prefix-embedded result slot (offset 72) in lockstep
    proc memory_set_int64 from Layout with phi_ptr, 72, new_value_id

    Return 1
End Process

Note: ============================================================
Note: PROGRAM-LEVEL AND FUNCTION-LEVEL TRAVERSAL HELPERS
Note: ============================================================

@Reasoning
Whole-program optimization passes (inlining, devirtualization, tail-call,
verifier) need to iterate every function in a MIRProgram. Rather than each
pass re-implementing the offset-0 list load, this canonical accessor returns
the program's function List directly. Returning the live List (rather than a
copy) lets callers append to it cheaply when a pass needs to register new
functions, while the program retains canonical ownership of the list itself.
A null program returns 0 so the caller's standard "if functions is equal to 0"
guard handles the missing-program path uniformly with the empty-program path.
@End Reasoning

@Implementation
Reads the int64 at MIRProgram offset 0, which is the canonical functions-list
slot established by create_mir_program. The MIRProgram layout (set by
create_mir_program in mir.runa) places:
  offset  0: functions List
  offset  8: function_count
  offset 16: global_values List
  offset 24: global_values_count
  offset 32: string_constants List
  offset 40: string_count
  offset 48: type_table
  offset 56: reserved
The returned List is owned by the program; callers must not destroy it.
@End Implementation

@Performance_Hints
Single int64 load. Inlines to one mov on x86_64. Called once per
whole-program pass, so not on any inner loop.
@End Performance_Hints

@Security_Scope
Pure read with explicit null guard. No allocation, no mutation.
@End Security_Scope

Process called "get_program_functions" that takes program_ptr as Integer returns Integer:
    If program_ptr is equal to 0:
        Return 0
    End If
    Return proc memory_get_int64 from Layout with program_ptr, 0
End Process

@Reasoning
Profitability gating in optimization passes (inlining, devirtualization,
tail-call) is dramatically cheaper when the pass can skip functions that
contain no instructions of the relevant kind. Rather than each pass
re-implementing the walk-blocks-walk-instructions double loop with its own
opcode comparison, this canonical helper centralises the pattern and exposes
the single tuning knob (the opcode to match) as a parameter. The function
is read-only over its inputs, so it is safe to call from any phase including
post-pass profitability checks.
@End Reasoning

@Implementation
Walks the function's CFG and counts every instruction whose opcode equals
the requested target_opcode. The traversal pattern:
  1. From function pointer, read CFG pointer (MIRFunction offset 8).
  2. From CFG pointer, read blocks List (MIRCFG offset 16).
  3. For each block in the List, read its instructions List (MIRBasicBlock
     offset 16) and instruction_count (offset 24).
  4. For each instruction in that block, read its opcode (MIRNode offset 0)
     and increment the running tally on a match.
Returns 0 for a null function pointer or any structurally absent
sub-component (no CFG, no blocks list, no instructions list); 0 is the
correct count for "this function has no instructions of that kind",
matching what callers expect from a vacuous case.
@End Implementation

@Performance_Hints
O(N) in the total instruction count of the function. Called once per
function per profitability check, not in any inner optimization loop.
The cached instruction_count at MIRBasicBlock offset 24 avoids re-walking
the per-block list to compute its length.
@End Performance_Hints

@Security_Scope
Pure read with explicit null guards at every dereference level. No
allocation, no mutation, no side effects.
@End Security_Scope

Process called "count_instructions_of_type" that takes function_ptr as Integer, target_opcode as Integer returns Integer:
    If function_ptr is equal to 0:
        Return 0
    End If

    Note: Read CFG pointer from MIRFunction offset 8
    Let cfg_ptr be proc memory_get_int64 from Layout with function_ptr, 8
    If cfg_ptr is equal to 0:
        Return 0
    End If

    Note: Read blocks List from MIRCFG offset 16
    Let blocks be proc memory_get_int64 from Layout with cfg_ptr, 16
    If blocks is equal to 0:
        Return 0
    End If

    Let block_count be proc list_length from List with blocks
    Let total_count be 0
    Let block_idx be 0

    While block_idx is less than block_count:
        Let block_ptr be proc list_get from List with blocks, block_idx
        If block_ptr is not equal to 0:
            Note: Read instructions List from MIRBasicBlock offset 16
            Let instructions be proc memory_get_int64 from Layout with block_ptr, 16
            If instructions is not equal to 0:
                Note: Read cached instruction_count from MIRBasicBlock offset 24
                Let inst_count be proc memory_get_int64 from Layout with block_ptr, 24
                Let inst_idx be 0
                While inst_idx is less than inst_count:
                    Let inst_ptr be proc list_get from List with instructions, inst_idx
                    If inst_ptr is not equal to 0:
                        Note: Read opcode from MIRNode offset 0
                        Let opcode be proc memory_get_int64 from Layout with inst_ptr, 0
                        If opcode is equal to target_opcode:
                            Set total_count to total_count plus 1
                        End If
                    End If
                    Set inst_idx to inst_idx plus 1
                End While
            End If
        End If
        Set block_idx to block_idx plus 1
    End While

    Return total_count
End Process

@Reasoning
OD-2 (owner decision, 2026-06-10): heap allocation and deallocation are NOT
MIR opcodes — the canonical memory band (20200-20206) covers stack ALLOCA
and memory access only. Heap lifetime events reach MIR as direct MIR_CALL
instructions whose callee resolves to the canonical allocator surface
exported by the memory API (compiler/frontend/primitives/memory/layout.runa).
Analysis passes (null_safety, use_after_free, points_to, escape analysis)
must recognize those events by callee NAME, never by opcode. Centralizing
the name table here keeps the allocator family in one place: "allocate"
(owned heap, Tier 3) and "arena_allocate" (arena, Tier 2) produce fresh
memory; "deallocate" and "memory_free" release it. Indirect calls
(MIR_CALL_INDIRECT) carry an SSA value id, not a name pointer, in the
callee slot and are never recognized — a function-pointer call that
happens to reach an allocator is conservatively treated as an opaque call,
which is sound for every consumer because name recognition only ever ADDS
precision (non-null result, fresh location, freed pointer).
@End Reasoning

@Implementation
Reads the opcode from the canonical MIRNode header (offset 0); only
MIR_CALL (20304, direct call) qualifies. The callee name pointer lives at
prefix-embedded offset 80 — the same slot get_call_callee reads. The name
is compared byte-wise via StringCore.string_equals against each member of
the allocation family. Returns 1 on a match, 0 otherwise (including null
node and null callee).
@End Implementation

Process called "is_allocation_call" that takes inst_ptr as Integer returns Integer:
    If inst_ptr is equal to 0:
        Return 0
    End If

    Let opcode be proc memory_get_int64 from Layout with inst_ptr, 0
    If opcode is not equal to MIR.MIR_CALL:
        Return 0
    End If

    Let callee be proc memory_get_int64 from Layout with inst_ptr, 80
    If callee is equal to 0:
        Return 0
    End If

    If proc string_equals from StringCore with callee, "allocate" is equal to 1:
        Return 1
    End If

    If proc string_equals from StringCore with callee, "arena_allocate" is equal to 1:
        Return 1
    End If

    Return 0
End Process

@Reasoning
Deallocation half of the OD-2 callee-name recognition rule. A direct
MIR_CALL whose callee name is "deallocate" or "memory_free" releases the
heap memory referenced by its FIRST argument (both canonical deallocators
take the pointer as argument 0). use_after_free keys its FREED-state
transition and double-free detection on this predicate; escape and
lifetime passes use it to bound heap object lifetimes.
@End Reasoning

@Implementation
Mirror of is_allocation_call with the deallocator name family. Opcode must
be MIR_CALL (20304); the callee name pointer at prefix-embedded offset 80
is compared against "deallocate" and "memory_free". Returns 1 on a match,
0 otherwise (including null node and null callee).
@End Implementation

Process called "is_deallocation_call" that takes inst_ptr as Integer returns Integer:
    If inst_ptr is equal to 0:
        Return 0
    End If

    Let opcode be proc memory_get_int64 from Layout with inst_ptr, 0
    If opcode is not equal to MIR.MIR_CALL:
        Return 0
    End If

    Let callee be proc memory_get_int64 from Layout with inst_ptr, 80
    If callee is equal to 0:
        Return 0
    End If

    If proc string_equals from StringCore with callee, "deallocate" is equal to 1:
        Return 1
    End If

    If proc string_equals from StringCore with callee, "memory_free" is equal to 1:
        Return 1
    End If

    Return 0
End Process
