Note: Test f-string interpolation

Process called "main" returns Integer:
    Let name be "Alice"
    Let age be 30
    Let score be 95

    Note: Test simple f-string with one variable
    Display f"Hello, {name}!"

    Note: Test f-string with multiple variables
    Display f"Name: {name}, Age: {age}"

    Note: Test f-string with text before, between, and after variables
    Display f"Student {name} scored {score} points"

    Return 0
End Process
