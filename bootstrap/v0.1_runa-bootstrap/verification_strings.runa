Note: Verification of string operations

Process called main returns Integer:
    Note: String literals
    Let str1 be "Hello"
    Let str2 be "World"

    Note: String concatenation
    Let combined be string_concat(str1, str2)

    Note: String length
    Let len1 be string_length(str1)
    Let len2 be string_length(str2)

    Note: String character access
    Let first_char be string_char_at(str1, 0)

    Note: String substring
    Let sub be string_substring(str1, 1, 3)

    Note: Return combined length (should be 10)
    Return string_length(combined)
End Process