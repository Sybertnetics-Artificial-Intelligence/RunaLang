Process called "main" that takes argc as Integer, argv as Integer returns Integer:
    Let true_val be 1
    Let false_val be 0
    
    Let and_test1 be 0
    If true_val and true_val:
        Set and_test1 to 1
    End If
    
    Let and_test2 be 1
    If true_val and false_val:
        Set and_test2 to 0
    End If
    
    Let or_test1 be 0
    If true_val or false_val:
        Set or_test1 to 1
    End If
    
    Let or_test2 be 0
    If false_val or true_val:
        Set or_test2 to 1
    End If
    
    Let or_test3 be 1
    If false_val or false_val:
        Set or_test3 to 0
    End If
    
    Let complex_test be 0
    If true_val and (true_val or false_val):
        Set complex_test to 1
    End If
    
    Return and_test1 + and_test2 + or_test1 + or_test2 + or_test3 + complex_test
End Process