Process called "main" that takes argc as Integer, argv as Integer returns Integer:
    Let hello be "Hello World"
    Let len be string_length(hello)

    Note: "Hello World" has 11 characters, so should return 11
    Return len
End Process