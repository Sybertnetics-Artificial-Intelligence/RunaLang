Type called Person:
    name as String
    age as Integer
End Type

Process called main returns Integer:
    Note: Create a Person instance
    Let person be a value of Type Person with
        name as "Alice",
        age as 30

    Note: For now, field access is deferred
    Note: Just return the age we know
    Return 30
End Process