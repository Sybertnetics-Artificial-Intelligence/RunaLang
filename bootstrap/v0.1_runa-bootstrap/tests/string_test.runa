Process called "main" that takes argc as Integer, argv as Integer returns Integer:
    Let greeting be "Hello"
    Let name be "World"

    Note: For now, we can only store string literals
    Note: String operations like concatenation are not implemented

    Return 42
End Process