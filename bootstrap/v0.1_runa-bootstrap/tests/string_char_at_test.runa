Process called "main" that takes argc as Integer, argv as Integer returns Integer:
    Let str be "Hello"
    Let index be 1

    Let char_code be string_char_at(str, index)

    Note: Should return 101 which is ASCII code for "e" (second character)
    Return char_code
End Process
