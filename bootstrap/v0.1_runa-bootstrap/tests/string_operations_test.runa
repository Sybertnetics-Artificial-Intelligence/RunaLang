Process called "main" that takes argc as Integer, argv as Integer returns Integer:
    Let test_str be "Hello World"
    Let hello be "Hello"
    Let world be "World"

    Note: Test string_length
    Let len1 be string_length(test_str)
    Let len2 be string_length(hello)

    Note: Test string_compare
    Let cmp1 be string_compare(hello, hello)
    Let cmp2 be string_compare(hello, world)

    Note: Test string_char_at
    Let char_h be string_char_at(hello, 0)
    Let char_e be string_char_at(hello, 1)

    Note: Test string_substring
    Let substr be string_substring(test_str, 6, 5)

    Note: len1 should be 11, len2 should be 5
    Note: cmp1 should be 0 (equal), cmp2 should be non-zero
    Note: char_h should be 72 (ASCII 'H'), char_e should be 101 (ASCII 'e')
    Note: Return a value that we can verify

    Return len1 + len2
End Process