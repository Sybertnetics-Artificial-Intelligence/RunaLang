Process called "main" that takes argc as Integer, argv as Integer returns Integer:
    Let true_val be 1
    Let false_val be 0
    
    Let and_test be 0
    If true_val and true_val:
        Set and_test to 1
    End If
    
    Let or_test be 0  
    If true_val or false_val:
        Set or_test to 1
    End If
    
    Return and_test + or_test
End Process