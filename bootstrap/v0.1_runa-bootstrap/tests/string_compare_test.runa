Process called "main" returns Integer:
    Note: Testing string comparison
    Let str1 be "Hello"
    Let str2 be "Hello"
    Let str3 be "World"

    Let result1 be string_compare(str1, str2)
    Let result2 be string_compare(str1, str3)

    Note: Equal strings should return 0, different strings should return non-zero
    Note: Return 0 if first test passed (equal strings), 1 if it failed
    If result1 is equal to 0:
        Return 0
    End If
    Return 1
End Process
