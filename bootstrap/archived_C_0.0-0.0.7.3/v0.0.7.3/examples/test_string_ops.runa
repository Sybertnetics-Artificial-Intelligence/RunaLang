Process called "main" returns Integer:
    Let text be "Hello World"
    Let len be string_length(text)
    Print len

    Let first_char be string_char_at(text, 0)
    Let first_ascii be ascii_value_of("H")
    Print first_ascii

    Let substr be string_substring(text, 6, 5)
    Print substr

    Let are_equal be string_equals(substr, "World")
    Print are_equal

    Let is_h_alpha be is_alpha("H")
    Print is_h_alpha

    Let is_5_digit be is_digit("5")
    Print is_5_digit

    Let is_space_whitespace be is_whitespace(" ")
    Print is_space_whitespace

    Return 0
End Process