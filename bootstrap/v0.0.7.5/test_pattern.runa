Process called "test_pattern" takes stats as Integer returns Integer:
    memory_set_integer(stats, 16, 30)
    memory_set_integer(stats, 24, 5)

    Let average_chain_length be 0
    If 1 is greater than 0:
        Set average_chain_length to 100
    End If
    memory_set_integer(stats, 32, average_chain_length)

    Let load_factor be 50
    If 1 is greater than 0:
        Set load_factor to 75
    End If
    memory_set_integer(stats, 40, load_factor)

    Return 1
End Process