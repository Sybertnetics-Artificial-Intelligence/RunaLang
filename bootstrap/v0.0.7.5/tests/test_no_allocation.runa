# Test with no memory allocation at all
# Just print statements to isolate the bug

Process called "main" takes argc as Integer, argv as Integer returns Integer:
    Print "Testing without allocation..."
    Print "Just print statements"
    Print "No function calls"
    Print "Should work fine"
    Return 0
End Process