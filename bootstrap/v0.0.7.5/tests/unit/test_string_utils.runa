Process called "main" takes argc as Integer, argv as Integer returns Integer:
    # Test string_utils functions
    Let str1 be "hello"
    Let str2 be "world"
    Let result be string_concat(str1, str2)
    If string_equals(result, "helloworld"):
        Return 0
    Otherwise:
        Return 1
    End If
End Process