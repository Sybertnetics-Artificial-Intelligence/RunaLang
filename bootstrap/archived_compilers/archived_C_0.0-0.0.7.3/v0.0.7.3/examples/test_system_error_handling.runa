Process called "main" takes argc as Integer, argv as Integer returns Integer:
    Print "=== Testing System Error Handling ===\n"

    # Test assert with false condition (this should fail)
    Print "Testing assert with false condition...\n"
    assert(0, "This assertion should fail!")

    # This line should never be reached
    Print "ERROR: This should not be printed!\n"
    Return 1
End Process