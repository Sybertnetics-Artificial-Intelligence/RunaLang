Note: Test context-sensitive tokens can be used as variable names
Note: Tests: count, pointer, error, array, and existing context-sensitive tokens

Process called "main" returns Integer:
    Note: Test NEW context-sensitive tokens
    Let count be 1
    Let pointer be 2
    Let error be 3
    Let array be 4

    Display count
    Display pointer
    Display error
    Display array

    Note: Test EXISTING context-sensitive tokens
    Let at be 5
    Let index be 6
    Let key be 7
    Let length be 8
    Let in be 9
    Let where be 10
    Let an be 11
    Let containing be 12
    Let from be 13
    Let each be 14

    Display at
    Display index
    Display key
    Display length
    Display in
    Display where
    Display an
    Display containing
    Display from
    Display each

    Note: Test they can be used in expressions
    Let sum be count plus pointer plus error plus array
    Display sum

    Note: Test they can be reassigned
    Set count to 100
    Set pointer to 200
    Set error to 300
    Set array to 400

    Display count
    Display pointer
    Display error
    Display array

    Return 0
End Process
