Note: v0.2 Type Checker - Bootstrap Stub
Note: Per specification Section 2.4

Import "parser" as Parser

Process called "check" that takes ast as ASTNode returns ASTNode:
    Note: For bootstrap phase, just pass through AST without type checking
    Return ast
End Process

Note: Wrapper function for v0.1 compatibility
Process called "TypeChecker_check" that takes ast as ASTNode returns ASTNode:
    Return check(ast)
End Process