Note:
Chinese Catalog
:End Note

Process called "to_canonical" returns Dictionary[String, String]:
    Return dictionary containing
        "过程 名为" as "Process called",
        "类型 名为" as "Type called",
        "令" as "Let",
        "定义" as "Define",
        "设置" as "Set",
        "返回" as "returns",
        "参数" as "that takes",
        "如果" as "If",
        "否则" as "Otherwise",
        "除非" as "Unless",
        "当" as "When",
        "匹配" as "Match",
        "对于" as "For",
        "当…时" as "While",
        "返回值" as "Return",
        "显示" as "Display",
        "等于" as "equals",
        "大于" as "is greater than",
        "小于" as "is less than",
        "大于或等于" as "is greater than or equal to",
        "小于或等于" as "is less than or equal to",
        "连接" as "joined with",
        "乘以" as "multiplied by",
        "除以" as "divided by",
        "减" as "minus",
        "加" as "plus",
        "字典 包含" as "dictionary with",
        "列表 包含" as "list containing",
        "索引" as "at index",
        "键" as "at key"

Process called "from_canonical" returns Dictionary[String, String]:
    Return dictionary containing
        "Process called" as "过程 名为",
        "Type called" as "类型 名为",
        "Let" as "令",
        "Define" as "定义",
        "Set" as "设置",
        "returns" as "返回",
        "that takes" as "参数",
        "If" as "如果",
        "Otherwise" as "否则",
        "Unless" as "除非",
        "When" as "当",
        "Match" as "匹配",
        "For" as "对于",
        "While" as "当…时",
        "Return" as "返回值",
        "Display" as "显示",
        "equals" as "等于",
        "is greater than" as "大于",
        "is less than" as "小于",
        "is greater than or equal to" as "大于或等于",
        "is less than or equal to" as "小于或等于",
        "joined with" as "连接",
        "multiplied by" as "乘以",
        "divided by" as "除以",
        "minus" as "减",
        "plus" as "加",
        "dictionary with" as "字典 包含",
        "list containing" as "列表 包含",
        "at index" as "索引",
        "at key" as "键"
