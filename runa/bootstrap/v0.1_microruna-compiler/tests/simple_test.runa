Note: Simple test to verify length_of bug is fixed

Process called "test_string_length" that takes text as String returns Void:
    Let text_length be length_of(text)
    Display text_length
End Process

Let test_message be "This is a test message that should not cause corruption"
test_string_length(test_message)