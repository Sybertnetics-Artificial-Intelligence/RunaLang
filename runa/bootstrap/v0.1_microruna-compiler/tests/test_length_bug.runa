Note: Test to isolate string length corruption bug

Process called "test_length_corruption" that takes source as String returns Void:
    Let source_length be length_of(source)
    Display source_length
End Process

Let test_string be "Hello World"
test_length_corruption(test_string)