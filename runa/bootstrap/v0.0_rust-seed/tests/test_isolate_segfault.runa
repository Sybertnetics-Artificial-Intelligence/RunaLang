Note: Test to isolate which builtin function is causing segfaults
Process called "test_char_at_only" returns String:
    Let test_str be "hello"
    Let result be char_at(test_str, 1)
    Return result
End Process

Process called "test_to_string_only" returns String:
    Let num be 42
    Let result be to_string(num)
    Return result
End Process

Process called "test_concat_only" returns String:
    Let str1 be "hello"
    Let str2 be " world"
    Let result be concat(str1, str2)
    Return result
End Process

Process called "test_substring_only" returns String:
    Let test_str be "hello world"
    Let result be substring(test_str, 0, 5)
    Return result
End Process

Note: Main function tests one at a time
Process called "main" returns Integer:
    Let result1 be test_char_at_only()
    Display result1

    Let result2 be test_to_string_only()
    Display result2

    Note: Comment out these to test if char_at and to_string work
    Note: Let result3 be test_concat_only()
    Note: Display result3

    Note: Let result4 be test_substring_only()
    Note: Display result4

    Return 0
End Process