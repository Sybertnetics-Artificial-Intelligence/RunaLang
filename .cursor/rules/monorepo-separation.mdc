---
description: 
globs: 
alwaysApply: true
---
CRITICAL: This monorepo is temporary for development convenience only.

Runa and Hermod will be separated into independent repositories
Write all code as if already in separate repos
No direct file imports between Runa and Hermod
Use proper package imports (e.g., import runa not import ../runa)
Document all cross-project dependencies clearly
Hermod accesses Runa through its public API only


The monorepo exists solely so Hermod can develop with native Runa support. Plan for separation from day one.