Note: Test comprehensive character operations

Process called "main" that takes x as Integer returns Integer:
    Note: Test is_digit - ASCII '5' = 53, 'a' = 97
    Let result1 be is_digit(53)
    Let result2 be is_digit(97)

    Note: Test is_letter - ASCII 'A' = 65, '3' = 51
    Let result3 be is_letter(65)
    Let result4 be is_letter(51)

    Note: Test is_whitespace - ASCII ' ' = 32, 'x' = 120
    Let result5 be is_whitespace(32)
    Let result6 be is_whitespace(120)

    Note: Test to_uppercase - ASCII 'a' = 97, 'Z' = 90
    Let result7 be to_uppercase(97)
    Let result8 be to_uppercase(90)

    Note: Test to_lowercase - ASCII 'A' = 65, 'z' = 122
    Let result9 be to_lowercase(65)
    Let result10 be to_lowercase(122)

    Note: Calculate return value based on results
    Note: Expected: 1+0+1+0+1+0+65+90+97+122 = 377
    Let sum be 0
    If result1:
        Set sum to sum plus 1
    End If
    If result2:
        Set sum to sum plus 1
    End If
    If result3:
        Set sum to sum plus 1
    End If
    If result4:
        Set sum to sum plus 1
    End If
    If result5:
        Set sum to sum plus 1
    End If
    If result6:
        Set sum to sum plus 1
    End If

    Set sum to sum plus result7
    Set sum to sum plus result8
    Set sum to sum plus result9
    Set sum to sum plus result10

    Return sum
End Process