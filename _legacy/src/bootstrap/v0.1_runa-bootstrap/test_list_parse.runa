Type called "Token":
    token_type as String
    value as String
End Type

Process called "tokenize" that takes source as String returns List[Token]:
    Let tokens be a list containing nothing
    Return tokens
End Process

Process called "main" returns Integer:
    Let result be tokenize("test")
    Return 0
End Process