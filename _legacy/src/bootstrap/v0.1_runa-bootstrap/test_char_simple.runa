Note: Test simple character operations

Process called "main" that takes x as Integer returns Integer:
    Note: Test is_digit with '5' (ASCII 53) - should return 1 (true)
    Let result be is_digit(53)
    If result:
        Return 100
    Otherwise:
        Return 200
    End If
End Process