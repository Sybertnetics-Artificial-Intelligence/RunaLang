Process called main returns Integer:
    Note: Create a list of numbers
    Let numbers be a list containing 10, 20, 30

    Note: Sum all numbers using For Each
    Let sum be 0
    For Each num in numbers:
        Set sum to sum plus num
    End For

    Note: Should return 60 (10+20+30)
    Return sum
End Process