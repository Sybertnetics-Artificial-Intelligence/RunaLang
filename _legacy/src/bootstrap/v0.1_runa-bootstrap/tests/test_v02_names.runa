Note: Test v0.2 function name compatibility

Process called main returns Integer:
    Note: Test that we can call the wrapper functions
    Let source be "Let x be 42"

    Note: This should call our new Lexer_tokenize wrapper
    Let tokens be Lexer_tokenize(source)

    Note: This should call our new Parser_parse wrapper
    Let ast be Parser_parse(tokens)

    Note: This should call our new TypeChecker_check wrapper
    Let typed_ast be TypeChecker_check(ast)

    Note: This should call our new CodeGen_generate wrapper
    Let assembly be CodeGen_generate(typed_ast)

    Return 0
End Process

Note: Mock implementations for testing (simplified types for v0.1)
Process called Lexer_tokenize that takes source as String returns String:
    Return "mock_tokens"
End Process

Process called Parser_parse that takes tokens as String returns String:
    Return "mock_ast"
End Process

Process called TypeChecker_check that takes ast as String returns String:
    Return ast
End Process

Process called CodeGen_generate that takes ast as String returns String:
    Return "mock_assembly"
End Process