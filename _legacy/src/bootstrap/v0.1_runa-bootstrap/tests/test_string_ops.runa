Process called main returns Integer:
    Note: Test string operations

    Note: Test string_concat
    Let hello be "Hello, "
    Let world be "World!"
    Let greeting be string_concat(hello, world)

    Note: Test string_length
    Let len be string_length(greeting)

    Note: Test string_char_at
    Let first_char be string_char_at(greeting, 0)

    Note: Test string_substring
    Let sub be string_substring(greeting, 0, 5)

    Note: Return length as basic verification
    Return len
End Process