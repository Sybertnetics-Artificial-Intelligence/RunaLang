Note: Test comprehensive string operations

Process called "main" that takes x as Integer returns Integer:
    Note: Test string_concat
    Let str1 be "Hello"
    Let str2 be " World"
    Let combined be string_concat(str1, str2)

    Note: Test string_length
    Let total_length be string_length(combined)

    Note: Test string_char_at (should be 'H' = 72)
    Let first_char be string_char_at(combined, 0)

    Note: Test string_substring (should be "Hello")
    Let hello_part be string_substring(combined, 0, 5)
    Let hello_len be string_length(hello_part)

    Note: Return sum to verify all operations worked
    Note: 11 (total_length) + 72 (H) + 5 (hello_len) = 88
    Return total_length plus first_char plus hello_len
End Process