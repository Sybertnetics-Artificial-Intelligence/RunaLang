Note: Test all character operations individually

Process called "main" that takes x as Integer returns Integer:
    Note: Test is_digit(53) - '5' should return 1
    Let d1 be is_digit(53)

    Note: Test is_letter(65) - 'A' should return 1
    Let l1 be is_letter(65)

    Note: Test is_whitespace(32) - ' ' should return 1
    Let w1 be is_whitespace(32)

    Note: Test to_uppercase(97) - 'a' should return 65 ('A')
    Let u1 be to_uppercase(97)

    Note: Test to_lowercase(65) - 'A' should return 97 ('a')
    Let l2 be to_lowercase(65)

    Note: Sum everything: 1 + 1 + 1 + 65 + 97 = 165
    Let sum be 0
    If d1:
        Set sum to sum plus 1
    End If
    If l1:
        Set sum to sum plus 1
    End If
    If w1:
        Set sum to sum plus 1
    End If
    Set sum to sum plus u1
    Set sum to sum plus l2

    Return sum
End Process