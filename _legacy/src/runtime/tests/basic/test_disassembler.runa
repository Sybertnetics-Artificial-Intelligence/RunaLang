process test_function(x, y)
    let result = x + y
    return result

process test_math(a, b)
    let sum = a + b
    let diff = a - b
    return sum

let x = 10
let y = 5
let z = x + y
let w = x - y

if x is greater than y
    let result = x
otherwise
    let result = y

while x is greater than 0
    x = x - 1

for i from 0 to 5
    let temp = i

let func = test_function
let answer = func(x, y) 