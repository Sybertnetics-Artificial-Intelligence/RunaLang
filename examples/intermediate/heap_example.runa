Note: Intermediate Heap example using natural Runa syntax

Let h be create_heap()
Let values be list containing 5, 2, 8, 1, 7

For each v in values:
    Call push with heap as h and value as v

Display "Heap after pushes:" with message h
Display "Heap is empty?" with message heap_is_empty with heap as h
Display "Heap length:" with message heap_length with heap as h
Display "Peek min value:" with message peek with heap as h

Let min1 be pop with heap as h
Display "Popped min value:" with message min1
Display "Heap after pop:" with message h

Let min2 be pop with heap as h
Display "Popped next min value:" with message min2
Display "Heap after second pop:" with message h 